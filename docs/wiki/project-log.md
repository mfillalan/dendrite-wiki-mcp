# Project Log

This page records meaningful project and wiki changes in chronological order.

## 2026-05-17

- Hardened the Codex installer for moved workspaces: the Codex plugin `.mcp.json` wrapper is now rewritten when content changes instead of only when missing, so `init --ide codex --mode built` repairs stale absolute `cwd` paths for VS Code Codex extension setups.
- Added `dendrite-wiki verify-install --json` so external setup scripts, CI jobs, and future plugin installers can consume the same doctor plus MCP smoke result through a stable machine-readable shape.
- Added profile-specific next-step output after `dendrite-wiki init`, so operators see the exact restart/trust/custom-agent action for Claude, Codex, Cursor, Copilot, Continue, Windsurf, Antigravity, Grok, or the broad `all` profile before starting the first agent session.
- Added `dendrite-wiki init --verify` so the one-command install path can seed configs and immediately run the read-only doctor plus MCP smoke test. This reduces first-run operator steps while preserving `verify-install` as a standalone check.
- Added conservative installer auto-detection: `dendrite-wiki init --ide auto` and `--profile auto` now select a single obvious workspace client when one project-local signal is present, otherwise they fall back to `all`. This gives operators a lower-friction install path without changing the default broad setup behavior.
- Extended `dendrite-wiki verify-install` from a config health check into an agent-surface smoke test: it now starts the local MCP server, lists tools, verifies required memory/wiki tools, and calls `wiki_context` without writing generated artifacts. Added installer coverage for the smoke path and updated docs so first-run verification proves the agent can actually connect.
- Hardened the "just works after install" path: Grok is now a first-class project-local installer target with `.grok/config.toml`, `.grok/skills/dendrite-wiki/SKILL.md`, and `.grok/hooks/dendrite-ritual.json`; `doctor` recognizes Grok configs and stays read-only by default instead of refreshing telemetry artifacts; added `dendrite-wiki verify-install` as the read-only onboarding check. Updated installer/doctor tests and MCP installation docs to keep the installer, docs, and health model aligned.

## 2026-05-03

- Tightened the agent orientation docs so new sessions now have an explicit startup order, `wiki_context` usage rule, and `memory_handoff` guidance for continuation between agent sessions.
- Added explicit session handoff capture with `memory_handoff`, and `wiki_context` now carries recent handoff notes into the next agent session instead of relying on generic memory recall alone.
- Extended memory hygiene with deterministic contradiction review, so `memory_review` can now flag active memories that share the same subject/context but make opposing claims.
- Extended memory hygiene with deterministic near-duplicate grouping, so `memory_review` now catches paraphrased repeated lessons instead of only exact text matches.
- Extended the AI Memory Companion roadmap tracker with a short “Next 3 Passes” section so the page shows the immediate remaining sequence, not just overall status.
- Turned the AI Memory Companion roadmap into the canonical live progress tracker and synced the project-plan checklist so the remaining work is visible without reconstructing it from recent commits.
- Added deterministic cleanup actions for memory review findings: stale and unsupported records can now be archived from the maintenance inbox, and duplicate groups can archive older exact-match memories while keeping the newest record.
- Added a deterministic review gate for maintenance-inbox memory promotions: apply stays blocked until the canonical target page already exists, and stdio coverage now proves both the blocked and approved paths.
- Added stdio MCP coverage for memory-promotion maintenance actions, including inbox exposure plus draft and apply execution through `wiki_execute_maintenance_action` on an isolated problem fixture.
- Added stable maintenance-inbox actions for promotion-ready memory findings, including a real apply path that can write the promoted wiki content and project-log entry through the existing maintenance executor.
- Extended `memory_promote` so accepted promotions can now update a target wiki page and append a project-log entry, not just return a draft.
- Added a draft-only `memory_promote` flow that suggests a target wiki page, proposed markdown, source refs, and undo guidance for selected memories without mutating files yet.
- Extended the maintenance inbox page and MCP payload to include memory-hygiene findings beside wiki lint and proposal state, so the first memory-review rules now surface in the same operator review flow.
- Added `memory_review` as the first deterministic memory-hygiene surface, so stale, unsupported, duplicate, and promotion-ready project-local memories can be inspected before maintenance-inbox integration.
- Integrated project-local memory recall into `wiki_context`, so the main agent briefing now includes relevant remembered lessons alongside pages, claims, guidance, and recent log entries.
- Started the first AI memory companion implementation slice with a project-local memory store under `local-data`, plus `memory_remember`, `memory_recall`, and `memory_forget` MCP tools and stdio integration coverage.
- Added the AI Memory Companion Roadmap as the next product track, focused on extracting useful DendriteMCP memory patterns into project-local recall, memory hygiene, session handoff, and memory-to-wiki promotion without importing game or task-management concepts.
- Clarified the DendriteMCP relationship in the lessons page with a plain-English comparison of what this repo translated, intentionally dropped, and has not built yet, including a direct comparison to Karpathy's LLM Wiki and the still-missing richer memory layer.
- Expanded the architecture wiki page with the current module map, runtime flow, storage model, generated-doc pipeline, maintenance system, review bridge, and install/init behavior so the project has a real technical overview instead of the original seed template.
- Published a privacy and telemetry disclosure page, a public sample payload artifact, and the first Supabase schema artifact so the current upload contract is documented, inspectable, and stable before wider opt-in use.
- Added the first sanitized telemetry upload path with a local audit artifact, a `dendrite-wiki telemetry upload` command, Supabase env-based routing, bounded retry, and a browser-visible last-payload preview on the telemetry status page.
- Added a local telemetry status surface with `dendrite-wiki telemetry status|opt-in|opt-out`, a seeded consent/status page, and a browser-readable artifact that keeps sharing off by default while making the event stream inspectable.
- Added automatic local benchmark event capture for normal MCP usage, wrote a local JSONL event stream plus a browser-readable summary artifact, and wired the benchmark report's maintenance panel to the new event data.
- Started Phase 6 with a typed synthesis-provider contract, environment-based provider selection, and a read-only `wiki_synthesize_proposals` MCP tool for bounded proposal explanations.
- Kept `none` as the default synthesis provider, added focused `ollama` support, and documented the new provider surface, env variables, and tool behavior.
- Completed the first-version Phase 6 synthesis surface with `agent` handoff prompts plus read-only `wiki_synthesize_claims` and `wiki_synthesize_guidance` tools for stale-claim explanations and guidance distillation.
- Completed the first-version Phase 8 search surface with explainable `wiki_search` ranking, a `wiki_graph` MCP tool, generated graph/search JSON, graph-backed context ranking, and an ignored local SQLite FTS artifact.
- Clarified the project tracker so every phase now distinguishes shipped first-version behavior from remaining hardening, and converted the next work into a concrete checklist led by Phase 7 review-flow polish.
- Completed the Phase 7 review-flow polish checklist with structured proposal review metadata, browser-visible rationale and before/after snippets, latest-action audit metadata, project-log entries for accepted apply actions, documented undo guidance, and focused inbox tests.
- Added Phase 8 hardening coverage with a larger scale fixture, explicit omitted-page explanations in `wiki_context`, and generated-artifact tests that verify refreshed search and graph output changes with the wiki corpus.
- Added MCP stdio integration coverage for `wiki_write` and `wiki_log` against an isolated fixture copy so write/log behavior is validated through the real server transport.
- Extended claim source parsing so wiki claims can carry typed `file:`, `command:`, and `decision:` provenance alongside normal wiki page links.
- Added a browser graph-neighborhood view on the Search Graph And Scale page backed by the generated `wiki-search-index.json` artifact.
- Finished the remaining hardening checklist with page metadata parsing, a generated guidance lifecycle view, archive-target review metadata, target-workspace MCP resolution coverage, and a configured OpenAI-compatible cloud synthesis provider.
- Reframed the project plan around the commercial MCP/plugin product vision, resolved the remaining design questions as explicit product decisions, and aligned the product vision and phase briefing docs with the completed first-version checklist.
- Started the productization and benchmark track with an npm package CLI, workspace initializer for MCP and agent guidance files, benchmark snapshot artifacts, and this repository as the first dogfood target.
- Added a visual creator guide that explains the package install flow, MCP runtime, wiki maintenance model, generated artifacts, benchmark baseline, and current self-sustaining limits from the verified implementation.
- Made `dendrite-wiki init` seed a starter wiki for first-run projects and added an explicit operator workflow page so the human review and maintenance role is documented as a concrete daily loop.
- Improved the seeded wiki templates so first-run projects get stronger default sections, starter metadata, and a clearer first-session documentation flow instead of bare placeholders.
- Added selective `init --profile` support so users can install only the integration files they actually need instead of always writing every IDE and agent surface.
- Expanded the installer with verified Codex, Continue, Windsurf, and Antigravity targets, while keeping `all` limited to workspace-local configs so user-home MCP files are only written when explicitly requested.
- Added an opt-in benchmark telemetry product design that keeps local benchmark value first, treats aggregate sharing as explicit consent, and frames the commercial story around transparent craft software rather than surveillance-style productivity tracking.
- Added a commercialization plan covering the free local core, paid Pro/Team/service options, licensing recommendation, business setup checklist, legal readiness, and ethical marketing path.
- Added a release-readiness roadmap that sequences the next work: local benchmark report first, telemetry second, public-release/legal package third, small beta fourth, paid services before subscriptions, and stability before moving to the next project.
- Added a local benchmark report page backed by a generated benchmark history artifact, updated the CLI and workspace seed flow to expose it, and kept prior latest snapshots as the baseline seed when the history file is created.

## 2026-05-02
- Bootstrapped `dendrite-wiki-mcp` as a new sibling project inspired by DendriteMCP and Karpathy's LLM Wiki.
- Chose a documentation-first direction: living wiki pages, browser UI, local-first MCP tools, and no game/quest layer.
- Added deterministic wiki linting for missing H1 headings, missing summaries, and orphan pages, exposed through the MCP `wiki_lint` tool.
- Added fixture-backed smoke tests for wiki page listing, search, lint findings, and slug path safety, and included them in `npm run check`.
- Added a browser-facing installation page for connecting `dendrite-wiki-mcp` from another project's `.vscode/mcp.json`.
- Removed leftover DendriteMCP workspace prompt and hook bootstrap files so the repo only carries wiki-specific MCP configuration.
- Added an MCP stdio smoke test that lists tools and calls the wiki read, search, and lint handlers through the real server entrypoint.
- Clarified the product direction: project-local wiki per repository, no required local LLM, deterministic memory hygiene first, optional synthesis providers later.
- Added an initial deterministic `wiki_context` briefing tool that ranks relevant pages, includes recent project-log entries, and carries lint findings for task setup.
- Added deterministic source-backed claim parsing so `wiki_context` can surface claim text, status, and linked page sources from a `## Claims` section.
- Added a deterministic `stale-claim` lint rule so non-current claims are flagged before they are trusted in a briefing.
- Added deterministic `openQuestions` generation in `wiki_context` so non-current claims show up as explicit review prompts in the briefing payload.
- Added deterministic `unsupported-claim` linting so claims without linked sources are flagged and `wiki_context` asks for supporting evidence explicitly.
- Added deterministic project guidance file inventory so `wiki_context` can surface local agent entry files like `AGENTS.md` and `.github/copilot-instructions.md`.
- Added deterministic `oversized-guidance` linting so overgrown project instruction files are flagged before they become prompt clutter.
- Added deterministic `duplicate-guidance` linting so repeated guidance content across entry files is flagged before it drifts into conflicting copies.
- Added deterministic `stale-guidance-reference` linting so broken markdown links inside guidance files are flagged before agents follow dead setup paths.
- Added deterministic `conflicting-guidance` linting so positive and negative directives on the same normalized rule are flagged across guidance files.
- Extended `wiki_context.openQuestions` so guidance lint findings can surface directly in a task briefing instead of requiring a separate lint read.
- Added deterministic `unrouted-guidance` linting and updated the main entry files to point into canonical wiki pages instead of carrying workflow detail locally.
- Removed the last workspace-level legacy DendriteMCP setup wording from agent instructions and cleaned leftover hook/setup artifacts from the workspace.
- Added deterministic `wiki_proposals` output for duplicate-guidance merge suggestions so redundant entry files can be reviewed before archiving or consolidation.
- Added deterministic archive-target suggestions to `wiki_proposals` so duplicate guidance merge proposals explain where redundant entry files should move after consolidation.
- Added deterministic `dormant-skill` linting so unlinked project skill files can be surfaced as inactive guidance instead of silently accumulating.
- Added deterministic route-to-wiki proposals for oversized guidance files so long entry files can be trimmed toward the canonical docs pages they already reference.
- Added deterministic pending-review page writing for current proposals so merge and routing suggestions can be inspected as normal wiki pages before any cleanup is applied.
- Added stale pending-review cleanup for generated proposal pages so outdated deterministic reviews are pruned while manual review notes remain untouched.
- Added stable review page metadata to proposal output so clients can link proposals to their deterministic pending-review page paths before materializing them.
- Added low-risk auto-apply for `route-guidance` proposals so an oversized guidance file can be rewritten into a short wiki-routing entry file without touching higher-risk merge proposals.
- Added MCP stdio coverage for `wiki_apply_proposal` so the low-risk route-guidance apply path is tested through the real server transport, not just through store helpers.
- Added low-risk auto-apply for `merge-guidance` proposals so duplicate entry files can be rewritten into short pointers to the canonical guidance file and wiki pages instead of being deleted.
- Added MCP stdio coverage for merge-guidance auto-apply so both low-risk proposal types are now tested through the real server transport.
- Applying a proposal now refreshes generated pending-review pages immediately, so the applied review page and any newly stale generated review pages disappear without waiting for a later `wiki_write_proposals` run.
- `wiki_apply_proposal` now reports which generated review pages were removed and which ones remain active, so apply results are easier to understand without re-reading proposal state separately.
- Generated pending-review pages now show a clearer current-state and after-apply summary so the change is easier to understand before running an apply action.
- `wiki_proposals` now includes short current-state and after-apply summaries directly in the JSON so clients can explain a proposal without opening its review page first.
- Added a Proposal Workflow wiki page so agents and operators have one short explanation of how proposal listing, review-page writing, apply, and cleanup fit together.
- Added MCP stdio coverage for non-empty `wiki_proposals` output so the new preview summary fields are tested through the real server transport as well.
- Linked the Proposal Workflow page from the README so the maintenance proposal flow is easier to find from the repo landing page.
- Added a generated Maintenance Inbox page in the docs UI so active proposals and lint findings can be reviewed together from the browser, and wired it into the normal wiki refresh flow.
- Fixed claim extraction so fenced markdown examples do not get treated as live `## Claims` sections, which removes false stale-claim findings from the maintenance inbox.
- Grouped the Maintenance Inbox by proposal kind and lint rule bucket, and added short next-step guidance so the page can scale once real maintenance work starts to accumulate.
- Added a `wiki_maintenance_inbox` MCP tool that returns the grouped maintenance state as structured JSON, so agents can consume the inbox data without scraping generated markdown.
- Added per-item action hints to `wiki_maintenance_inbox` so clients can drive review-page reads, proposal apply calls, wiki page reads, and lint reruns from the inbox payload itself.
- Added stable action IDs to `wiki_maintenance_inbox` action hints so future clients can dispatch inbox actions without relying on display labels.
- Added `wiki_execute_maintenance_action` so a client can execute a stable inbox action ID directly instead of reimplementing inbox action dispatch outside the server.
- Added normalized `resultKind` values to `wiki_execute_maintenance_action` responses so clients can branch on action results without inferring payload shape from the wrapped tool.
- Added a one-line `resultSummary` to `wiki_execute_maintenance_action` responses so clients can render action outcomes without decoding each result payload first.
- Refreshed the MCP installation guide so it documents the current tool surface, stable build versus live development setup, and how a target project's `.vscode/mcp.json` should point back to this repo.
- Added a browser-facing Maintenance Review page that renders the generated inbox snapshot as grouped proposal and lint cards, using the same stable action metadata that MCP clients can execute.
- Added a local `wiki:action` runner that executes a stable maintenance action ID from the repo root, so the read-only review board can still hand off directly into a real local execution path.
- The local `wiki:action` runner now refreshes generated docs after execution and writes a latest-result JSON artifact, so the browser review board can show the most recent local maintenance action outcome after a page reload.
- Expanded the review board's latest-action panel so it shows action/source metadata, derived result details for each result kind, and the raw JSON payload for deeper inspection.
- The review board now rechecks the generated inbox and latest-result artifacts in place, with a manual refresh button plus lightweight polling so a full browser reload is no longer required after a local action run.
- Added an optional local review bridge so the browser board can execute available maintenance actions directly when `npm run review-bridge` is running, while still falling back to the copyable local runner commands.
- Added isolated endpoint coverage for the optional review bridge so health checks, direct action execution, and artifact writes are now tested without mutating the shared fixture tree.
- The review bridge now requires an explicit confirmation for `apply-proposal` actions, and the browser board reflects that with a confirmation prompt and distinct button label.
- The review bridge now also requires a startup token on every execute request, and the browser board stores that token locally so direct actions are no longer exposed to any localhost page that can guess the endpoint.
- The review bridge now returns structured error codes for execute failures, and the browser board uses those codes to handle invalid tokens, confirmation misses, and stale action IDs without depending on message text.
- The review bridge now gives each startup token a bounded lifetime by default, exposes that expiry through `/health`, and rejects expired tokens with a distinct error code so the board can disable stale direct-action runs before they fail ambiguously.
- The review bridge now also exposes a per-startup session identifier through `/health`, and the browser board clears any saved token immediately when that session changes so bridge restarts are handled proactively instead of after the next execute failure.
- The review bridge no longer sends wildcard CORS headers. It now only accepts browser origins from the local docs dev and preview hosts by default, with an environment override for other local setups.
- Allowed review-bridge preflight responses now advertise a short `Access-Control-Max-Age`, so repeated browser-side action runs do not need a fresh preflight on every click while still staying bounded.
- The browser review board's bridge auth and session-state helpers now live in a pure TypeScript module with focused tests, so saved-token parsing, session mismatch handling, expiry checks, and structured bridge error messaging are validated directly instead of only through component compilation.
- The review board's bridge health reconciliation now also lives in the same pure helper module with focused tests, which fixes the previous accidental recursion in the session-mismatch check and directly validates the same-session versus changed-session behavior.
- Added a dedicated Review Bridge wiki page so the local HTTP bridge contract, safety model, environment overrides, and validation story now have one stable handoff page instead of being spread across the review page and the project log.
- Refreshed the Project Plan and Phase Briefings pages so they now reflect the current repo state: first milestone backlog complete, Phases 3-5 and 7 mostly complete, and Phase 6 plus Phase 8 as the next major work tracks.
- Fixed `scripts/refresh-wiki.ts` to preserve the existing newline style in `docs/index.md`, which stops Windows from leaving the index dirty with mixed line endings after a refresh.
- Wired wiki catalog refresh into `npm run check` so new wiki pages like Proposal Workflow show up in the generated index without a separate manual refresh step.

## 2026-05-04

- Strengthened the AI memory companion workflow hooks: every installer-seeded and live guidance template (AGENTS.md, copilot, vscode prompt/instructions, claude command, cursor rule, agent skill, seed agent-workflow) now describes the session-start handoff-aware briefing and the session-end memory_handoff step, and the installer ships parallel session-start, session-handoff, and benchmark hook manifests under .github/hooks/ for harnesses that support lifecycle hooks.

- Polished the deterministic memory ranking and promotion drafts: memory_recall and the wiki_context handoff path now apply explainable stale, unsupported, and inactive-status penalties (with explicit reasons in the recall payload) so older or weakly-sourced memories no longer crowd out fresh, source-backed lessons, and memory_promote drafts now include a per-memory provenance line covering kind, recall count, and sources so reviewers can judge each promotion candidate inline.

- Added a recall-quality benchmark layer: every snapshot now includes a `recall` block with probe count, top-1 and top-5 hit counts, miss count, mean reciprocal rank, and average reason count. The new `src/wiki/recall-benchmark.ts` module reads a `local-data/recall-probes.json` override when present and otherwise auto-derives one self-recall probe per active non-handoff memory (using its summary as the query and the memory itself as the expected hit). Added focused tests for both probe sources, extended the existing snapshot test, and documented the probe file format and metric definitions on the Benchmarking page.

- Added a Recall Quality panel to the local Benchmark Report Vue component. The panel surfaces the probe source (auto-derived vs `local-data/recall-probes.json`), the latest evaluated probe / top-1 hit / top-5 hit / miss counts as a stat grid, and per-metric trend rows for top-1 hits, top-5 hits, miss count, mean reciprocal rank, and average ranking-reason count. The Vue component now treats `recall` as optional so older history artifacts that predate the field render a graceful "run another snapshot" placeholder instead of crashing.

- Made recall probes portable across operators: the probe schema now accepts `expectedTags`, `expectedRelatedFiles`, and `expectedRelatedPages` alongside the existing per-machine `expectedMemoryIds`, and a recalled memory satisfies a probe via any of those matchers. Each probe result reports which matcher fired (`memory-id` / `tags` / `related-files` / `related-pages`). Auto-derived probes also seed the portable matchers from the source memory, so a probe set generated on one machine is meaningful on another. Added a dogfood seed at `local-data/recall-probes.json` for this repo (gitignore now exempts that single file) covering recurring orientation questions about server tool registration, memory-store internals, memory promotion, recall benchmark internals, review bridge token, session handoff workflow, and installer guidance files. Updated the Benchmarking page with the new schema and the gitignore exemption pattern.

- Added a `dendrite-wiki recall:bootstrap` CLI command that scaffolds a starter `local-data/recall-probes.json`. With memories present the command emits one probe per active non-handoff memory using its summary as the query and the memory's tags, related files, and related pages as portable matchers (machine-local IDs are intentionally omitted so the generated file can be committed and shared). When the memory store is empty it falls back to a documented template with three placeholder probes the operator should edit. The command refuses to overwrite an existing file without `--force` and accepts `--output` to target a custom path. Updated the Benchmarking page with the bootstrap workflow.

- Promoted project-local memory mem_ade7dcd2-176b-49df-97d3-bf4336af1479 into ai-memory-companion-roadmap.

- Promoted project-local memory mem_a7d829c2-9115-4c30-879c-bfa16997ede7 into architecture.

## 2026-05-05

- Long dogfood session shipped 14+ commits and finally exercised the full memory promotion + maintenance review loop end-to-end. Key fixes: review bridge was silently returning 404 for all memory action IDs (findMaintenanceInboxAction wasn't being passed memoryFindings); apply-memory-promotion never marked the source memory superseded so the inbox kept re-flagging it forever; inbox apply-availability gate diverged from the actual draft/apply target-resolution logic; project-log was a bad default fallback target (replaced with architecture). Improvements: embedded the review bridge as same-origin Vite middleware so the token UI is gone for the docs:dev path; SSE push channel for the inbox notification badge so updates arrive in <1s instead of 10s polling; added a new top-nav notification badge that shows the inbox count with severity-based color; full UI/UX redesign of the maintenance review board around a single prioritized work list with inline drill-down (replacing three near-identical sections with verbose per-card code blocks). Captured 6 durable memories covering the bridge memoryFindings contract, the auto-supersede pattern, the logic-divergence pitfall, the same-origin embedded-handler architecture, the SSE+fs.watch+debounce push pattern, and the agent-drift observation that even strict SessionStart hooks don't prevent memory-discipline lapses across long sessions.

- - Fixed inbox notification badge spacing in the top nav: it was clinging to the light/dark mode toggle with no separator while leaving an apparent gap of whitespace to its right. Root cause was VitePress's `nav-bar-content-after` slot placement — the slot lands inside `.content-body` after the appearance toggle but VitePress's `.menu + .appearance::before` style dividers and `.social-links { margin-right: -8px }` flush-right convention only target named theme components, not slot content. Patched `docs/.vitepress/theme/components/InboxNavBadge.vue` to add a matching `::before` 1px vertical divider, `margin-left: 16px`, and `margin-right: -8px` scoped to `min-width: 768px` so mobile keeps the natural `column-gap: 0.5rem`.

- - Replaced the standalone top-right Inbox notification pill with inline count badges teleported onto the `Inbox` and `Review Board` nav links themselves (both desktop `VPNavBarMenu` and mobile screen menu). Rationale per user feedback: the standalone pill was a redundant second visual cue pointing to the same page as existing nav items; putting the count on the link the user actually needs to click is one cue, not two, and naturally directs attention to the click target. Implementation in `docs/.vitepress/theme/components/InboxNavBadge.vue` uses Vue Teleport with selector `a.VPNavBarMenuLink, a.VPNavScreenMenuLink` matched by href suffix, plus a `MutationObserver` on `.VPNav` to keep targets correct when the mobile screen menu opens.

- Started the paid-tier build campaign with two new artifacts. (1) New canonical wiki page `paid-tier-roadmap` defines the build order: Pro features as enhanced free polish first, Services revenue second, license-key gating third, Team tier last. Captures 5 Pro features (P1-P5), 4 Team features (T1-T4), and 3 Services offerings (S1-S3) with status tracker and per-feature build acceptance criteria. (2) First Pro feature shipped: `dendrite-wiki report:export` CLI command generates a self-contained HTML benchmark report (no external dependencies, inline CSS, inline SVG sparklines, embedded data) at `docs/public/benchmark-report.html` by default. Report includes hero summary, baseline-vs-latest metric cards with delta indicators, health trend sparklines for documentation debt and claim count, recall quality panel, and full snapshots table. Custom output path via `--output`, custom title via `--title`. Exposed `readBenchmarkHistory()` from src/wiki/benchmark.ts as the public read helper. Test coverage: 1 multi-phase test in test/report-export.test.ts covering empty state, populated state, and custom output path (single test rather than three because store.ts captures process.cwd() at module-load time, see new warning memory). All 78 tests passing. Real report at 8.3KB for 3 snapshots — emailable, attachable, self-contained.

- Shipped Pro-tier feature P2: `dendrite-wiki doctor` command for project-health audit. Runs 10 checks across three severity levels: critical (no wiki directory, no index page, no MCP client config), warnings (lint findings present, pending proposals, memory contradictions, stale benchmark >14 days, no benchmark history, stale project log >7 days, no git repository), info (MCP clients configured, project stats line). Every critical finding includes a concrete `fix` command — the test enforces this as a product invariant. Two output modes: human-readable text (default) and `--json` for tooling. Exits 1 when any critical finding is present, 0 otherwise. Architecture uses two-phase checks: cheap skeleton checks run first, deeper checks (lintWikiPages, listWikiProposals, reviewProjectMemories, readBenchmarkHistory) only run if the basic skeleton exists, all wrapped in .catch() fallbacks so the command never crashes on uninitialized projects. Smoke-tested on this repo: surfaces the expected 1 lint finding (AGENTS.md oversized-guidance) and 1 pending proposal accurately. 79 tests passing. Updated paid-tier-roadmap status tracker: P2 now Done.

- Major architecture pivot: shipped universal MCP-side ritual enforcement to fix the agent-discipline drift confirmed across two dogfood sessions (warning memories mem_7d531792 and mem_5480f5cc). Before this pass, enforcement relied on a SessionStart hook that fired only at session start, and agents reliably drifted mid-session. New approach: src/wiki/ritual-state.ts maintains session ritual state (wikiContextCalled, lastMemoryRememberAt, toolCallsSinceLastMemoryRemember, handoffCalled) for the lifetime of the MCP server process; src/server.ts wraps every tool callback through wrapToolResponse(toolName, baseText) which appends a RITUAL CHECKPOINT footer as a second text content block when reminders are active. Three deterministic reminder rules: no-wiki-context (URGENT, fires on second tool call if wiki_context not yet called), no-recent-memory-remember (NUDGE, fires after 8+ tool calls with no memory_remember when meaningful work has happened), long-session-no-handoff (INFO, fires after 15+ tool calls with no memory_handoff). This layer works in every MCP client (Claude Code CLI/extension, Cursor, Copilot, Codex, Continue, Windsurf, Antigravity, Zed) because every spec-compliant client surfaces tool response content to the agent. Per-client hook hardening (UserPromptSubmit re-injection, PreToolUse warnings, custom Copilot agent file, Codex hooks.json) deferred to next pass. Research-backed: Agent subtasks audited Claude Code, Cursor, Copilot, Codex, Continue, Windsurf, Antigravity, Zed enforcement features 2026-05-05; full capability matrix in new wiki page agent-enforcement-architecture. Test coverage: 8 ritual-state tests + 1 jsonContent helper fix in mcp-server.test.ts to handle the new two-block response shape. 87 tests passing, docs build clean.

- Layered Claude Code per-client enforcement on top of the universal MCP-side ritual layer. New CLI subcommand `dendrite-wiki ritual:hook` reads the persisted ritual state from local-data/ritual-state.json (now written sync after every recordToolCall and resetRitualState in src/wiki/ritual-state.ts) and emits Claude-Code-compatible hook JSON when reminders are active. Stays quiet on healthy sessions so it does not pollute every user message. Wired into the dogfood loop's .claude/settings.json UserPromptSubmit hook list — fires on every user message, not just on context compaction. Smoke-tested with a fake stale state showing no-wiki-context urgency: hook correctly emitted the JSON injection. Same hook command will work for Codex once we add the .codex/hooks.json writer to install.ts in a later pass. Cursor and Copilot need different protocols and remain deferred. New helpers exposed: readPersistedRitualState() and computeRemindersForState() — pure functions any external script can call. 92 tests passing.

- Extended the per-client enforcement layer to Codex. install.ts now writes (1) .codex/hooks.json mirroring Claude Code's hooks structure (SessionStart with the full ritual reminder, PostToolUse on wiki_context with the per-pass capture nudge, UserPromptSubmit calling npx -y dendrite-wiki ritual:hook), and (2) a codex_hooks feature flag in .codex/config.toml under a [features] section so Codex actually fires the hooks (the flag is required by Codex). New ensureCodexFeatureFlag helper appends the flag idempotently, and an install test now asserts the second install run writes nothing and that the [features] section appears exactly once. Hit one idempotency bug along the way: blank-line padding before [features] got stripped on re-run by writeCodexConfig, breaking equality. Fix was to write the new section adjacent to the previous one without blank-line padding (TOML allows it). Also updated buildClaudeSettings to include the same npx ritual:hook UserPromptSubmit hook so future installs get the layered defense out of the box. 92 tests passing.

- Extended per-client enforcement to Cursor. New ritual cursor-hook CLI subcommand outputs Cursor-shaped JSON (permission/agentMessage) rather than Claude Codes hookSpecificOutput shape. Hook always allows the call (never blocks) but injects an agentMessage with active ritual reminders when state shows gaps. install.ts cursor profile now writes .cursor/hooks.json with beforeMCPExecution registered, since beforeSubmitPrompt cannot inject prompts in Cursor (observe-only). Smoke-tested with stale state showing the no-wiki-context urgency: hook correctly emitted the Cursor JSON. install test extended to assert .cursor/hooks.json exists for both all and cursor-only profiles, and that the registered command invokes ritual cursor-hook. 92 tests passing. Three of four hook-capable clients are now wired with consistent enforcement: Claude Code, Codex, Cursor. Copilot custom agent file remains the last remaining per-client work.

- Closed the per-client enforcement loop with GitHub Copilot in VS Code. install.ts now writes a custom Copilot agent file at .github/agents/dendrite.agent.md (preview feature behind chat.useCustomAgentHooks) with three lifecycle hooks in frontmatter: sessionStart for the cold-start ritual contract, userPromptSubmitted invoking npx -y dendrite-wiki ritual hook for state-aware mid-session reminders, and postToolUse on wiki context for the per-pass capture nudge. The .agent.md body documents the three manual steps a user must complete (enable preview setting, restart VS Code, select the dendrite agent) for the hooks to fire — and notes that the universal MCP-side ritual checkpoint footer is the always-on fallback for users who skip those steps. New copilot-agent install asset added to copilot-vscode and all profiles. Install test asserts the file exists with the expected frontmatter shape and documents the required preview setting. All four hook-capable clients are now wired with consistent enforcement: Claude Code, Codex, Cursor, Copilot. 92 tests passing.

- Polish pass closing the enforcement campaign. Updated agent-enforcement-architecture.md status table to mark every per-client track Done (Claude Code, Codex, Cursor, Copilot all wired); added new Done rows for state persistence and the two ritual:* CLI subcommands; documented the two remaining open follow-ups (Cursor schema verification, Copilot agent format verification). Also rewrote AGENTS.md to: (a) reference the universal MCP-side ritual checkpoint footer and the per-client hook layer with a pointer to agent-enforcement-architecture.md, (b) include a defensive note about not putting literal close tags in wiki_log entries since VitePress parses markdown as Vue, (c) come in at 38 lines under the 40-line oversized-guidance threshold. Lint findings dropped from 1 to 0. 92 tests passing. The enforcement track is now materially complete; remaining work is observation and verification rather than new code.

- - Prepped repo for first public alpha publish to npm. Added inaugural CHANGELOG.md grouping the 50 commits leading to 0.1.0 into Living wiki & MCP server, AI memory companion, Recall-quality benchmark, Maintenance review loop, Multi-client installer, Agent ritual enforcement, Privacy & telemetry, and Paid-tier scaffolding sections. Tightened README hero so the first three lines land for a stranger (problem statement + local-first solution promise as the bold tagline rather than a buried branded sentence). Fixed suspect package.json files field: changed `dist` to `dist/src` so the published tarball stops shipping `dist/docs/.vitepress/*` and `dist/scripts/*` build artifacts that no runtime code references — verified safe via grep of src/ for __dirname/fileURLToPath/import.meta.url (none) and confirmed install.ts embeds all seeded content as strings. Tarball: 34 files / 90.3kB → 27 files / 87.3kB. Also added CHANGELOG.md to the files field. Drafted annotated tag message for v0.1.0. Did NOT push to git or run the actual `npm publish` — operator is reviewing diffs and the dry-run output before either action.

- - Tightened three strategy wiki pages for public-release safety and fixed an underlying VitePress escape bug. paid-tier-roadmap.md, commercialization-plan.md, and release-readiness-roadmap.md were leaking pricing splits, per-tier feature gating, and operator-self-talk into a public wiki — all three were trimmed to keep build-order reasoning, license-decision rationale, trust posture, and roadmap pointers, while moving specific dollar amounts, free-vs-paid splits, marketing copy, sales motion, and business-formation checklists to private operator notes. Net change: 380 lines deleted, 72 added across the three files. While validating with `npm run docs:build`, surfaced a pre-existing bug: the maintenance-inbox generator emitted operator-supplied memory bodies and summaries directly into markdown, and any memory containing literal `<name>` (or any other angle-bracketed text) tripped VitePress's Vue parser with "Element is missing end tag." Fixed at the sink: added `escapeMarkdownForVue()` helper in src/wiki/maintenance-inbox.ts that HTML-escapes `<`/`>` to `&lt;`/`&gt;` before they reach the `####` heading and the `>` blockquote, and added a regression test asserting both heading and body escape. Also fixed a dead-link issue in the trimmed commercialization-plan.md (`../../LICENSE` resolves outside the docs/ tree; switched to a GitHub URL). `npm run check` now passes end-to-end: wiki:refresh → tsc → 93 tests → docs:build (4.79s) — was 92 tests before the new escape regression test landed.

- - Built the promotion-preview diff modal end-to-end based on dogfood feedback from the operator using the Maintenance Review board on this very repo. New `previewProjectMemoryPromotion()` in src/wiki/memory-promotion.ts is a pure function that returns target-page metadata, currentContent, proposedContent, a unified-diff string (via the `diff` npm package, ^9.0.0, added as a runtime dep), the resolved section heading and its rendered HTML anchor, and the skippedBecauseUnchanged flag. `applyProjectMemoryPromotion()` was refactored to call preview internally so the apply path and the preview path share a single source of truth for proposed content. New POST `/preview/memory-promotion` endpoint on the review bridge (mirrored at `/__review-bridge/preview-promotion` for the embedded same-origin deployment), token-gated in standalone mode and same-origin in embedded mode. Token-check logic was extracted to a shared `checkBridgeToken()` helper so the new endpoint and the existing execute endpoint use the same missing/invalid/expired branch tree. New `PromotionPreviewModal.vue` component renders the diff with line-level coloring (+ green, - red, @@ blue, meta italic), shows the resolved target page as a clickable link with section anchor, surfaces draft-time warnings inline, and emits an apply event the board handles via the existing `runActionViaBridge` path with confirmation skipped (the modal IS the confirmation UI). Promotion-ready memory items in MaintenanceReviewBoard.vue now show "Preview promotion" as the primary button instead of running apply directly. Tests: existing review-bridge health assertion updated for the new `previewPromotionPath` field; new top-level test covers missing-memoryIds 400, missing-token 401, happy-path response shape (including specific assertions on the unified-diff format), and idempotency. 94 tests pass (was 93). Wiki docs updated: review-bridge.md adds the Preview Promotion Contract section and updates the health-contract example; maintenance-review.md adds the Promotion Preview operator guide. The escape-bug regression test from the prior commit also continues to pass through this change.

- Promoted project-local memory mem_7f54e2f8-0c99-4bad-9eb5-9193c96a14e2 into agent-enforcement-architecture.

- Promoted project-local memory mem_fac57340-154d-4bb0-9c07-330014147ec7 into agent-workflow.

- Promoted project-local memory mem_6cf2c199-3710-4704-a5f1-55a30cb2b44a into ai-memory-companion-roadmap.

- Designed two new product tracks based on a TLDR Applied AI job-description framing pass: (1) **Skills As Memory** (free-tier — new wiki page docs/wiki/skills-as-memory.md, M7 phase added to ai-memory-companion-roadmap) — skills as a new memory kind with scope schema, two-phase fetch via wiki_context skill summaries + wiki_skill_load, hook-injected enforcement, three-tier promotion path (memory → skill → wiki page), composing with Anthropic's native .claude/skills/. Build phases S1–S7. (2) **Team Tier Architecture** (new wiki page docs/wiki/team-tier-architecture.md, T5–T8 added to paid-tier-roadmap) — hosted node + steward agent + pull-only reporting dashboard. Steward classifies merges into high/medium/low confidence; reviewer has final say. Goal: empower or eliminate scrum-master role by making engineering work legible without engineer-written status updates. Six design-decision memories captured. Next implementation focus per operator: free-tier skills layer (S1).

- Resolved all open design questions in skills-as-memory.md before S1 implementation. Most consequential: deterministic-only matching for v1 free tier (no local LLM dependency), justified by (a) audit of predecessor dendrite-mcp showing deterministic guard-rails — not the LLM — delivered the bulk of matching-quality wins; (b) our two-phase fetch design has the frontier coding agent doing semantic reranking on the candidate list, eliminating the need for a second LLM; (c) dendritemcp-lessons explicitly lists heavy background model dependency as a pattern to avoid. Borrowed guard-rails from predecessor's audit commit ff27e93: scope hard-filters before scoring, recency demotion, token bigram bonuses. Other resolutions: ship all 5 scope dimensions in v1, same store with kind discriminator, hard-reject skills without scope, top-3 budget per briefing, hook failures log-and-continue. Starting S1 implementation next: skill memory kind + scope schema.

- S1 (Skills As Memory — Skill Memory Kind) shipped: added 'skill' to ProjectMemoryKind enum, added ProjectMemoryScope schema with all 5 dimensions (filePatterns, frameworks, languages, taskKeywords, matchMode default 'any'), added optional scope field on ProjectMemoryRecord and RememberProjectMemoryInput, hard-reject validation via new ProjectMemorySkillScopeError class for kind:'skill' without any scope dimension. Server tool surface (memory_remember) accepts skill kind + scope, returns clean error response on validation failure. 8 new tests in test/memory-skill-kind.test.ts cover scope persistence, hard-reject paths, default match mode, round-trip through store, and unknown-kind fallback. Bonus fix: extended escapeMarkdownForVue from maintenance-inbox to memory-promotion's buildPromotionMarkdown — the same VitePress 'Element is missing end tag' regression that 19e87b7 fixed had a second sink in the promotion emit path that broke docs:build whenever a promoted memory contained literal angle brackets like .github/agents/&lt;name&gt;.agent.md. Regression test added. Full check pipeline green: 103 pass, 0 fail, docs build clean. Next: S2 — skill matching and ranking (wiki_skills_list tool with deterministic scope hard-filters + recency demotion + token bigram bonuses, no local LLM dependency).

- S2 (Skills As Memory — matching and ranking) shipped: new src/wiki/skill-matching.ts module with recallProjectSkills(context, root) function, glob-to-regex converter (supports `**/`, `/**/`, `**`, `*`, `?`), file-extension-to-language inference (~30 extensions mapped), and deterministic scoring. Conservative scope hard-filters: skill declared a dimension AND input contradicts → exclude; skill declared a dimension AND no input → conservative keep (matchMode='any') or skip (matchMode='all'). Per-dim weights: filePatterns +10, languages/frameworks +5; task keywords scored by ngram size (trigram +7, bigram +5, unigram +2 — borrowed from dendrite-mcp's bigram-bonus pattern). Recall+source bonuses capped at 3. Recency demotion -3 when last activity older than 30 days. New wiki_skills_list MCP tool surfaces ranked skill candidates with explainable reasons[] arrays. 14 new tests covering glob semantics, language inference, hard-exclude, conservative keep, matchMode='all' enforcement, bigram bonus, auto-inference from files, maxItems default of 3, recency demotion, archived/non-skill exclusion. Updated mcp-server.test.ts tool roster. Full check green: 117 pass, 0 fail. Next: S3 — surface skill summaries in wiki_context briefing for free.

- S3 (wiki_context skill surfacing) and S4 (wiki_skill_load tool) shipped together. S3: WikiContextResult gained a skills: RecalledProjectSkill[] field; buildWikiContext now calls recallProjectSkills internally with top-3 default (configurable via maxSkills option, max 20); briefing markdown gains a 'N matching skill(s) included; call wiki_skill_load(id) for full content' line when skills are present; wiki_context tool surface accepts maxSkills, relatedFiles, languages, frameworks options for richer skill matching. S4: new ProjectSkillNotFoundError class, loadProjectSkill(id, root) function in skill-matching.ts that returns the full skill record AND increments recallCount/lastRecalledAt atomically (so heavily-used skills rank higher next time); new wiki_skill_load MCP tool returns clean error JSON for unknown id, non-skill kind, or non-active status. 4 new tests in skill-load-and-context.test.ts. Updated mcp-server.test.ts roster (now 24 tools). Full check green: 121 pass, 0 fail, docs build clean. Skills layer is now end-to-end functional from the agent's perspective: call wiki_context → get skill summaries → call wiki_skill_load(id) → get skill body. Next: S5 — promotion workflow (memory→skill candidate detection in maintenance review).

- S5 (Skills As Memory — promotion workflow) shipped. S5a detection: new 'skill-promotion-ready' review finding kind in reviewProjectMemories, with optional inferredScope on each finding. inferSkillScopeFromMemory derives a ProjectMemoryScope from a memory's relatedFiles (via inferFilePatternsFromRelatedFiles + extension→language map) and tags (via FRAMEWORK_TAG_HINTS / LANGUAGE_TAG_HINTS allowlists, plus blocked-terms-aware taskKeyword extraction capped at 5). Returns undefined when no signal exists, so memories without file/tag context don't get spurious promotion candidates. S5b promotion: new promoteMemoryToSkill(memoryId, options, root) function — uses inferred scope by default, accepts operator-provided scope override, atomically creates a kind:'skill' memory and marks the source memory superseded (matching the wiki promotion pattern). preserveSourceMemory option keeps source active when explicitly requested. Throws ProjectMemorySkillScopeError when no scope inferable and none provided; throws plain Error for already-skill or missing-source cases. New memory_promote_skill MCP tool returns clean error JSON for all failure modes. ProjectMemoryReviewResult.summary gains skillPromotionReady count. maintenance-inbox.ts memoryReviewKindOrder/Titles extended with the new kind so the inbox UI can render it. 10 new tests in skill-promotion.test.ts cover scope inference (file patterns, languages from extensions, frameworks/languages from tags, no-signal returns undefined), review surfacing with inferredScope, recall-count threshold, atomic promotion (default and operator-override), preserveSourceMemory, error paths. Updated mcp-server.test.ts roster (now 25 tools) and review summary type. Full check green: 131 pass, 0 fail, docs build clean. Free-tier skills layer is now functional end-to-end: memory_remember → recall → memory_review surfaces skill-promotion-ready → memory_promote_skill → wiki_context surfaces it → wiki_skills_list / wiki_skill_load. Next: S6 (hook-injected enforcement) and S7 (skill wiki page directory).

- S6 (hook-injected enforcement) and S7 (skills wiki page directory) shipped, completing the Skills As Memory free-tier campaign (S1–S7 all done). S6: new `dendrite-wiki skills:hook` CLI command reads tool_input JSON from stdin (Claude Code passes { tool_name, tool_input: { file_path, description } }), runs deterministic skill matching against the file, returns hookSpecificOutput.additionalContext with up to 3 matching skill summaries. Per design: hook NEVER blocks Edit/Write — exits 0 silently on any error including malformed JSON, missing file_path, or matching errors. Wired as PreToolUse hook on `Edit|Write|MultiEdit` in buildClaudeSettings. Standalone `dendrite-wiki-skills.json` manifest also shipped under `.github/hooks/` for non-Claude harnesses. 5 new tests in skills-hook.test.ts cover: matching skill returns additionalContext; no-match exits silent; missing file_path exits silent; malformed JSON exits 0 (never blocks); empty stdin exits 0. S7: created `docs/wiki/skills/` directory with index.md describing the three-tier promotion path and frontmatter schema for future promoted-skill pages. No pages live there yet — the directory is the home for mature skill promotions when teams want canonical human-edited versions. Updated skills-as-memory.md, ai-memory-companion-roadmap.md M7 row, and paid-tier-roadmap.md S1–S7 row to 'Shipped'. Full check green: 136 pass, 0 fail, docs build clean. End-to-end skills layer functional: agent works on a file → PreToolUse skills:hook surfaces matching skills → agent calls wiki_skill_load(id) → recall counter increments → memory_review eventually surfaces high-recall lessons as skill-promotion candidates → memory_promote_skill creates new scope-bound skill from inferred or operator-provided scope.

- Guidance-layer sweep: brought every shipped agent-instruction file in line with the new skills layer. Updated install.ts builders (buildAgentsFile, buildCopilotInstructions, buildVsCodeInstructions, buildVsCodePrompt, buildCursorRule, buildClaudeCommand, buildAgentSkill, buildCopilotAgent) to teach the skills workflow: when to capture skill-shaped memories with `kind: 'skill'` + scope, how to load surfaced skills via `wiki_skill_load`, what `memory_promote_skill` does, and how the `PreToolUse` skills hook works. Added matching PreToolUse skills hook to buildCodexHooks (`Edit|Write|MultiEdit` matcher → `npx -y dendrite-wiki skills:hook`). Added `preToolUse` event to buildCopilotAgent's hooks frontmatter. Updated buildClaudeSettings SessionStart additionalContext to mention `wiki_skill_load`. Synced the dogfood repo's local copies (.agents/skills/dendrite-wiki/SKILL.md fully rewritten; AGENTS.md gained step-2 update + new Skills Layer section; .github/copilot-instructions.md gained skills bullets; .github/instructions/dendrite-wiki.instructions.md and .github/prompts/dendrite-wiki-session.prompt.md and .claude/commands/dendrite-wiki-session.md all rewritten; .claude/settings.json gained PreToolUse hook and SessionStart text update). Updated README.md feature list (22→25 tools, added Skills Layer bullet) and Use it section (added wiki_skill_load step + skill-capture guidance). Added CHANGELOG.md [Unreleased] section enumerating all S1–S7 work + the bonus VitePress fix. Extended test/install.test.ts to assert the new PreToolUse skills hook lands in claude settings, the standalone skills hook manifest exists, and the agent SKILL.md mentions the new tools. Cursor was intentionally NOT updated with a PreToolUse equivalent — Cursor's hook protocol only supports beforeMCPExecution/beforeShellExecution, neither suitable for file-edit interception. Cursor users still get skills via wiki_context. Full check green: 136 pass, 0 fail, docs build clean. Ready to cut v0.1.0-alpha.1.

- Memory Trails campaign shipped — three deterministic patterns ported from predecessor dendrite-mcp after structured audit. (1) Edge reinforcement with lazy evaporation: new src/wiki/memory-edges.ts module with project-memory-edges.json store. recallProjectMemories and recallProjectSkills now reinforce edges from each surfaced item to a normalized query fingerprint (sorted dedup'd significant tokens). Lazy on-demand evaporation at read time (weight × (1 - 0.005)^hours_since) sidesteps the predecessor's tokio-scheduler dependency. New queries get Jaccard-similarity-weighted bonuses from edges with overlapping fingerprints (≥30% threshold), capped at +5 per candidate, surfaced as 'memory trail: reinforced N× across M matching queries (last X days ago)' in recall reasons[]. wiki_skill_load gained optional taskHint param and reinforces with heavier +0.10 amount (vs +0.05 for passive surfacing). (2) LRU+TTL cache on wiki_context (src/wiki/context-cache.ts): 256 entries, 30-min TTL, evicted by oldest lastHitAt. Invalidated on writeWikiPage, appendProjectLog, rememberProjectMemory, forgetProjectMemory, markProjectMemoriesSuperseded, promoteMemoryToSkill — but NOT on recall-counter bumps which would defeat the cache. (3) Page-drift Jaccard lint (src/wiki/page-drift.ts): new 'page-drift' WikiLintRule. For each non-project-log page, compares title+first-paragraph token set against recent project-log entries mentioning the page slug; Jaccard < 0.5 with ≥2 matching entries raises a review-now finding. New wiki page memory-trails.md documents the design and the predecessor patterns deliberately NOT ported (mycelial growth ran broken for months in dendrite-mcp; Ollama-required passes; XP/dormancy game mechanics; Physarum path-flux). Updated CHANGELOG and README. Tests: 14 memory-edges, 9 context-cache, 10 page-drift — all green. Full check: 169 pass, 0 fail, docs build clean. Net diff vs prior baseline: 169 vs 136 = 33 new tests for the campaign.

## 2026-05-06

- 2026-05-05: Competitive audit vs claude-mem (thedotmack). Captured honest gap analysis: claude-mem wins on auto-capture (PostToolUse), hybrid semantic search (Chroma+FTS5), live web UI, multi-IDE installer flags, plugin marketplace presence, and sharp tagline. Dendrite wins on git-diffable markdown wiki (uninstall = keep docs), explainable ranking with reasons[], 5-dim scoped skills + promotion path, source-backed claims, recall benchmark with public probes, maintenance inbox + review bridge, no required Bun/uv/Chroma deps, and bipartite-projection shadow mode with kill-switch metric. Top recommended slices: (1) PostToolUse raw-observations feeder stream feeding promotion candidates, (2) sharp landing tagline + comparison page leaning into "memory you can review in a PR", (3) optional embedding-backed semantic recall (Node-only, no Python), (4) public Recall Quality leaderboard, (5) skill export/sharing feature that exploits markdown-canonical moat.

- 2026-05-05: Drafted Competitive Feature Roadmap (vs claude-mem) at docs/wiki/competitive-feature-roadmap.md. Seven phases C1-C7: auto-capture foundation (PostToolUse raw observations + deterministic classifier + cluster promotion), marketing surface (tagline, comparison page, public Recall Quality leaderboard, uninstall-test callout), multi-harness reach (--ide flags + plugin marketplace listing), compression+live viewer (synthesis-provider for cluster compression + VitePress /live route), optional semantic recall (pure-JS @xenova embeddings, off by default, kill-switch metric), moat features (skill export/import + GitHub Action PR-bot for context-on-diff), small wins (per-language modes + explicit private flag). Recommended start: C1 slice 1 (raw-observations capture + retention + opt-out). Page mirrors ai-memory-companion-roadmap structure with a phase tracker, success metrics table, kill-switch rules, and an explicit deliberately-not-doing list to prevent scope creep.

- Promoted project-local memory mem_d112bfa8-9fa7-4993-b6af-522ca265cd7d into ai-memory-companion-roadmap.

- Promoted project-local memory mem_fe33df9e-ef14-42ab-beb9-96a3aa02a05c into review-bridge.

- 2026-05-05: Shipped C1 slice 1 (auto-capture foundation, smallest unit). New module src/wiki/raw-observations.ts captures one record per matched PostToolUse to local-data/raw-observations.jsonl with deterministic tool→kind classifier (edit/read/command/search/web/other), DENDRITE_RAW_OBSERVATIONS=off opt-out, and lazy line-cap retention (default 5000, override via DENDRITE_RAW_OBSERVATIONS_MAX_LINES). New CLI subcommands: `dendrite-wiki observations:capture` (PostToolUse hook, reads stdin, silent on every error path so it never blocks the agent) and `dendrite-wiki observations:list [--limit N]`. Installer now writes .github/hooks/dendrite-wiki-observations.json plus PostToolUse Edit|Write|MultiEdit|Bash entries inside Claude settings.json and Codex hooks.json. Strictly separated from the curated memory store — observations do NOT enter wiki_context recall; cluster-based promotion to memory candidates ships in slice 2. 13 new tests pass (191 total green); tsc clean. Files: src/wiki/raw-observations.ts (new), src/cli.ts (+observations:capture/list, help text), src/install.ts (+buildObservationsHookManifest, +PostToolUse entries in Claude/Codex), test/raw-observations.test.ts (new), test/install.test.ts (+observations hook assertion).

- 2026-05-05: Shipped C1 slice 2 (cluster detection + maintenance-inbox surfacing). New `detectRawObservationClusters` in src/wiki/raw-observations.ts groups raw observations by (kind, normalized target) — case-insensitive, slash/whitespace/trailing-slash normalized — and surfaces a cluster when ≥3 occurrences are seen across ≥2 distinct sessions (defaults configurable via options or `--min`/`--sessions`/`--window-days` CLI flags). Optional `windowDays` filters to recent activity only. Inbox snapshot grew an `observationClusters` field plus `observationClusterCount` status; markdown page got a new "Active Observation Clusters" section with suggested-source-link column (file:/command: prefix by kind). Wired into both production callers: `refreshGeneratedWikiDocs` (for the markdown + JSON pipeline) and the MCP `wiki_maintenance_inbox` tool. New CLI subcommand `dendrite-wiki observations:clusters [--min N] [--sessions M] [--window-days D]` for direct inspection. Fixed two pre-existing test fixtures whose deepEqual snapshots needed the new `observationClusterCount` and `observationClusters[]` fields. 199/199 tests pass; tsc clean. Slice 3 (one-click promote-cluster-to-memory action) deliberately deferred until real dogfood reveals which clusters operators actually want to promote.

- 2026-05-05: Shipped C2 (marketing surface). New tagline "The memory layer that becomes your wiki / Memory you can review in a PR. Recall you can explain. A wiki that outlives the tool." landed in README, with a "What makes Dendrite different" comparison matrix and an explicit "uninstall test" callout positioned above the feature list. New wiki page docs/wiki/comparison-claude-mem.md gives an honest fair-tone side-by-side: separate "where claude-mem wins" and "where Dendrite wins" sections, choose-each tool decision lists, and links to both projects so readers can verify everything. New wiki page docs/wiki/recall-quality-public.md publishes the recall benchmark methodology, current metrics surface (top-1, top-5, MRR, miss count, average reason count), portability rules for matchers, and an explicit kill-switch discipline statement. Catalog auto-regenerated to include both new pages. README also updated to mention auto-capture (C1), observation clusters, and observations:list/clusters CLI commands. 199/199 tests pass; tsc clean; docs build clean.

- 2026-05-05: Shipped C3 (multi-harness reach, smallest unit). New `--ide &lt;name&gt;` flag in `dendrite-wiki init` is the friendlier surface for the existing `--profile` mechanism; aliases include claude-code, cursor, codex, continue, windsurf, gemini-cli, copilot-vscode, vscode (gemini-cli → antigravity profile, claude-code → claude profile, etc). Unknown values produce a useful error listing known aliases. README install section now leads with `--ide` examples; `--profile` remains supported. 3 new spawn-based tests cover the alias mapping, the gemini-cli → antigravity routing, and the unknown-IDE error path. Discovered and fixed a TDZ ordering bug while wiring this: the alias map had to be hoisted above the top-level `const [command, ...args]` execution because `readProfile()` is called from the if/else chain that runs before later const declarations are initialized. 202/202 tests pass; tsc clean. Plugin marketplace listing (the second half of C3) is deferred to a separate slice — it requires Anthropic marketplace manifest research that's not blocking.

- 2026-05-05: Shipped C6 slice 1 (skill export/import — cross-project skill portability). New module src/wiki/skill-portability.ts implements `exportSkillById`, `writeSkillExport` (writes to local-data/skill-exports/&lt;slug&gt;.skill.md by default), `importSkillFromFile`, and `importSkillFromMarkdown`. Bundle format is markdown with YAML frontmatter (kind, summary, exportedFrom, exportedAt, exportSchemaVersion, originalRecallCount) plus the human-readable skill body, plus a fenced ```json``` metadata block carrying scope/tags/relatedFiles/relatedPages/sources. New CLI subcommands `dendrite-wiki skill:export &lt;id&gt; [--output path]` and `dendrite-wiki skill:import &lt;path&gt;`. Imported skills get a fresh memory id, status=active, recallCount=0, and the import path appended as a `file:` source for provenance. Round-trip preserves scope and content; machine-local state (id/timestamps/recall counts) is intentionally regenerated. Typed `SkillPortabilityError` with codes (SKILL_NOT_FOUND, NOT_A_SKILL, SKILL_MISSING_SCOPE, NOT_A_SKILL_BUNDLE, BUNDLE_MISSING_FRONTMATTER, BUNDLE_MISSING_JSON_BLOCK, BUNDLE_INVALID_JSON, BUNDLE_INVALID_METADATA, BUNDLE_MISSING_SCOPE, BUNDLE_SCOPE_EMPTY) for clean error handling. 10 new tests cover round-trip, format invariants, file IO, and every error code path. 212/212 tests pass; tsc clean. This is a feature claude-mem cannot match without rewriting their storage layer — markdown-canonical skills are inherently shareable, vector blobs are not. Slice 2 of C6 (GitHub Action PR-bot for context-on-diff comments) is the next natural extension.

- 2026-05-05: Shipped C7 slice 1 (private memory flag with skill-export interlock). Added optional `private?: boolean` to ProjectMemoryRecord — when true, the memory participates normally in recall, ranking, and review (operator still sees it locally) but skill:export refuses with typed error code SKILL_IS_PRIVATE, and any future bulk-share feature will refuse as well. The `memory_remember` MCP tool gained an optional `private` parameter with documented contract. Storage normalizer round-trips the flag without materializing it on records that lack it (clean shape — `private` is absent rather than `false`, so deep-equal comparisons stay tight). 3 new tests cover round-trip persistence, the explicit-true case, and the default-undefined case; the existing skill-export test suite gained a SKILL_IS_PRIVATE refusal test. 215/215 tests pass; tsc clean. The interlock with C6 slice 1 is the design point: skill export is the FIRST share path that exists in the codebase, so the privacy story is now ENFORCED rather than aspirational.

- 2026-05-05: Shipped C1 slice 3 (one-click promote-observation-cluster-to-memory action). Closes the C1 loop — clusters surfaced in slice 2 now have a runnable apply path. New action kind `create-memory-from-cluster` (action ID format `cluster:&lt;kind&gt;:&lt;safe-target&gt;:create-memory-from-cluster`) routes to the `memory_remember` tool with a TEMPLATE body that explicitly tells the operator to edit the text before relying on it. The template includes the cluster's recurrence stats (kind, target, observation count, distinct sessions, last-seen timestamp) plus a list of plausible patterns (setup gotcha, refactoring target, frequently-broken integration, workflow worth promoting to a skill). Auto-attaches `tags: ['from-observation-cluster']`, `sources: [&lt;file:|command:&gt;target]`, and for edit/read clusters `relatedFiles: [target]`. Wired the new resolved-action source type `{ type: 'observation-cluster'; clusterKind; target }` through findMaintenanceInboxAction and added a `memory_remember` case to the executeMaintenanceAction switch (with a new `remembered-from-cluster` result kind). 2 new tests cover the action hint shape and findMaintenanceInboxAction resolution. 216/216 tests pass; tsc clean. The C1 phase is now functionally complete: capture (slice 1) → cluster (slice 2) → promote (slice 3) all wired end-to-end.

- 2026-05-05: Exposed skill_export and skill_import as MCP tools (small follow-up to C6 slice 1). Agents can now call skill_export(skillId) → returns the markdown bundle as a string, and skill_import(bundleMarkdown, sourceUri) → creates a fresh skill memory with the imported scope and provenance line. Both wrappers return typed-error JSON on SkillPortabilityError (preserves the error code so callers can branch). The MCP server tool count grew from 25 to 27. Discoverability assertion in mcp-server.test.ts covers the registration; underlying logic is already covered by 10 tests in skill-portability.test.ts. 216/216 tests pass; tsc clean.

- 2026-05-05: Shipped C4 slice 1 (live observation viewer in VitePress). New refresh-time artifact docs/public/raw-observations-recent.json carries the latest 200 observations plus current cluster count. New Vue component docs/.vitepress/theme/components/LiveObservations.vue renders the stream with kind-based filtering, target search, distinct-session and observation counts, and per-row badges by kind/outcome. New wiki page docs/wiki/observation-stream.md mounts the component and documents the schema, opt-out env vars, and the strict-separation contract (raw stream NEVER enters wiki_context recall — only feeds the maintenance-inbox cluster surface). Component registered in docs/.vitepress/theme/index.ts. Also fixed a pre-existing footgun in appendProjectLog that was caused by my own previous log entries: project log entries containing literal angle-bracketed tokens like cluster:&lt;kind&gt; broke the docs build because Vue's compiler interpreted them as custom tags. Fix: appendProjectLog now html-escapes &lt; and &gt; before writing (matches the existing escape pattern in maintenance-inbox.ts and memory-promotion.ts). Hand-edited the broken historical entries to unblock the docs build. 216/216 tests pass; docs build clean. Slice 2 of C4 (optional LLM compression of observation clusters via the existing synthesis-provider config) remains as the next slice.

- 2026-05-05: Shipped C4 slice 2 (observation cluster compression — operator-driven LLM handoff). New module src/wiki/observation-compressor.ts builds DETERMINISTIC handoff prompts for recurring clusters; no LLM is called from the module. Output is structured text the operator pastes into Claude/GPT/local-model to get a draft "candidate memory text" back, plus a CONFIDENCE label and ambiguity notes. Matches the existing `agent` synthesis-provider pattern (synthesis.ts already established this convention). New CLI subcommand `dendrite-wiki observations:compress [--target substring] [--max N] [--recent N] [--min N] [--sessions M]` emits one prompt per qualifying cluster, with a tail of recent observations included for context. The prompt asks the LLM for: (1) the likely durable lesson behind the recurrence, (2) plain-language statement, (3) explicit "avoid reproducing every observation — surface insight not data", (4) ambiguity-honesty if the cluster could be several lessons. 4 new tests cover prompt shape, target filter, threshold filtering, and maxClusters cap. 220/220 tests pass; tsc clean. C4 phase is now complete: live viewer (slice 1) + compression handoff (slice 2). The deferred work — actually wiring an LLM call directly — stays optional and operator-controlled, matching the project's no-required-LLM principle.

- 2026-05-05: Shipped C5 slice 1 (optional semantic recall via OpenAI-compatible REST endpoint, SHADOW MODE only). New module src/wiki/embedding-provider.ts: provider resolution from DENDRITE_EMBEDDINGS_OPENAI_API_KEY (default endpoint https://api.openai.com/v1/embeddings, default model text-embedding-3-small, both env-overridable), HTTP-only embedTexts implementation with timeout + abort signal, lazy memory-embedding cache at local-data/memory-embeddings.json keyed by sha256(text) so embeddings are reused across recall calls and across edits as long as text is unchanged. cosineSimilarity helper. recallProjectMemories now computes shadow-mode cosine for query+candidate pairs when the provider is enabled and surfaces the metric on each result via a new shadowSemanticCosine field plus a `[shadow] semantic similarity X.XXX via configured embedding provider — not yet applied to ranking` reason line. Critically: failures in the embedding fetch NEVER break recall (try/catch swallows everything; the deterministic score continues unchanged). Off by default — operators without the env var see zero behavior change and zero network calls. Same kill-switch discipline as the bipartite-projection shadow mode: ranking will be wired to use cosine only after measured lift on the recall benchmark. NO `@xenova/transformers` dependency added — pure REST, no native binaries, no model download. 11 new tests cover provider resolution (disabled/ready/custom env), cosine math edge cases, hash stability, embedTexts fetch contract + auth header + body shape, lazy cache hit/miss/mixed scenarios, recall integration in 3 modes (provider-on returns shadow cosine, provider-off makes zero fetches, fetch-failure does not break recall). 231/231 tests pass; tsc clean.

- 2026-05-05: Shipped C6 slice 2 (context-for-diff CLI — first half of the PR-bot moat play). New module src/wiki/diff-context.ts aggregates wiki pages, memories, and skills relevant to a list of changed files, deduplicating across files so a single memory or page only appears once across the whole diff. Output is markdown suitable for a GitHub PR comment, terminal review, or any other surface. New CLI subcommand `dendrite-wiki context-for-diff [--files path...] [--query text]` accepts paths via flag OR via stdin (one per line) so the natural pipeline is `git diff --name-only main...HEAD | dendrite-wiki context-for-diff`. The command uses the same buildWikiContext + recallProjectMemories + recallProjectSkills surface the in-editor agent uses, so PR reviewers see the same project knowledge the agent does. Path normalization handles Windows-style backslashes. 6 new tests cover empty-list result, memory+skill aggregation matching changed files, dedup across files, markdown shape (header + counts + per-file sections), empty-list markdown copy, and Windows path normalization. 237/237 tests pass; tsc clean. The deferred half is the GitHub Action manifest that wraps this CLI for auto-commenting on PRs — that requires a separate published Action and CI workflow examples, both of which can be authored once we have a real signal someone wants to deploy it.

- 2026-05-05: Shipped C6 slice 3 (GitHub Action wrapping the context-for-diff CLI). New composite action at .github/actions/dendrite-context/action.yml that downstream repos consume via `uses: mfillalan/dendrite-wiki-mcp/.github/actions/dendrite-context@main`. The Action: resolves base ref (PR base SHA → origin/main → HEAD~1 fallback chain), computes the changed file list with `git diff --name-only --diff-filter=ACMR`, pipes paths through `npx -y dendrite-wiki-mcp@<dist-tag> context-for-diff`, then publishes the result via configurable comment-mode (auto-detects `pr` on pull_request events, `summary` otherwise; explicit `stdout` mode also supported). Inputs: base-ref, head-ref, query, comment-mode, dist-tag, node-version. Outputs: changed-files-count, context-markdown-path. Required permissions: contents:read + pull-requests:write (the latter only for `pr` mode). Documented end-to-end at docs/wiki/github-action-pr-context.md with a copy-paste workflow YAML for the downstream repo. The deferred sticky-edit behavior (replace previous Dendrite comment instead of stacking) is intentionally not built — it's a noise-only optimization that should ship after real usage shows it's worth doing. 237/237 tests pass; docs build clean. C6 phase is now functionally complete (slice 1 export/import + slice 2 CLI + slice 3 Action).

- 2026-05-05: Shipped C5 slice 2 (kill-switch metric instrumentation for shadow semantic recall). The shadowSemanticCosine field that slice 1 attached to each recalled record now aggregates into the benchmark snapshot as three new fields under recall: shadowSemanticSeenProbeCount (probes where at least one candidate had a cosine), shadowSemanticAverageCosine (avg across all candidates with one), shadowSemanticAverageTopCosine (avg cosine of the deterministic top-1 candidate — sanity check whether the deterministic winner is also semantically related to the query). The fields persist in docs/public/dendrite-benchmark-history.json so trends are inspectable across runs. Surfaced in the browser-visible BenchmarkReport.vue panel as a dashed-border "shadow strip" inside the Recall Quality Trend, only rendered when shadowSemanticSeenProbeCount > 0 (zero-state hidden so unconfigured operators don't see noise). 2 new tests cover the aggregation: provider-on path verifies seen-count > 0 and cosines in [-1,1], provider-off path verifies all three counts stay at 0 and zero fetches fire. 239/239 tests pass; docs build clean. The kill-switch loop is now closed — without this slice the C5 metric existed but was invisible; now operators with the env var set can watch the trend page and decide whether the cosine signal is worth wiring into ranking.

- 2026-05-05: Shipped C7 slice 2 (per-language modes scaffolding). New module src/wiki/i18n.ts: DENDRITE_LANG env var (default 'en', BCP-47 region suffix stripped → 'en-US' becomes 'en'), translate(key, values, opts) helper with English baseline + Spanish sample bundle so the routing has a real second path. Missing keys fall back to English; missing-everywhere keys return the key itself rather than throwing — agent-facing surfaces would prefer to render the literal than fail. Storage stays English-only by design: memory bodies, wiki pages, claims, project-log entries are NEVER translated; only operator-facing message text (cluster templates, ritual reminders) routes through the i18n table. Routed the observation-cluster memory template through i18n as proof-of-plumbing — it's the most visible operator-facing string in the C1 loop. 7 new tests cover env var resolution (default, suffix-strip), localized + fallback paths, missing-key behavior, and the registered-language list. 246/246 tests pass; tsc clean. The framework is the deliverable — additional translations can be PR-able forks once an operator asks for one. Documented in the i18n.ts header comment.

- 2026-05-05: Shipped C3 slice 2 (plugin marketplace listing — research-grounded plan, deferred deploy). New wiki page docs/wiki/plugin-marketplace-listing.md captures the full Claude Code marketplace + plugin manifest schemas pulled directly from code.claude.com/docs/en/plugin-marketplaces and /plugins-reference, with draft .claude-plugin/marketplace.json and .claude-plugin/plugin.json contents tailored to Dendrite (npm dist-tag alpha, full hook stack mirroring the existing installer's .claude/settings.json output, category="memory", explainable-recall + local-first + markdown-canonical tags). Three explicit verification checks queued before the JSON files actually land at the repo root: (1) schema validation against a live Claude Code instance for fields the docs don't enumerate (homepage/repository/license), (2) hook command quoting in the plugin-manifest form vs the inline-settings form, (3) `source: "."` semantics for plugins colocated with their marketplace. Once verified, the README install section gains a third option block, comparison-claude-mem.md updates the marketplace row from "Planned" to "Available", and the competitive roadmap marks C3 slice 2 as Done. The honest framing: this is a plan with drafted manifests, not a deploy, because shipping untested config files would mislead users. C3 is now functionally as complete as it can be without a Claude Code instance handy. 246/246 tests still pass; docs build clean.

- Bio-inspired audit vs shipped Dendrite (2026-05-06, from Gemini deep-research proposals): hierarchical 3-layer retrieval already in spirit (wiki_search L0 / wiki_context L1 / wiki_skill_load+wiki_read L2); slime-mold link reinforcement = Memory Trails (shipped 2026-05-05); HRR rejected as duplicating C5's @xenova path with extra complexity and re-inviting predecessor's silent-failure pattern; success-based synaptic tagging is the one real gap — observation-classifier does not propagate session-outcome (passing tests / green build / clean commit) back to cluster ranking, so reviewer attention currently treats verified-knowledge clusters and dead-end-debugging clusters equally. Next slice plan: (1) synaptic-tagging slice — session-outcome inference up-weights tagged clusters in maintenance inbox; (2) extend Memory Trails to reinforce page→query edges in wiki_context (closes open question 3 on memory-trails); (3) document L0/L1/L2 tiering in architecture page; (4) note HRR rejection in memory-trails 'Deliberately Not Done' section so the bio-inspired-paper rediscovery loop terminates.

- Bio-inspired audit follow-up shipped (2026-05-06): (1) architecture page now documents the L0/L1/L2 retrieval tiering explicitly (wiki_search/wiki_skills_list = L0, wiki_context/memory_recall = L1, wiki_read/wiki_skill_load = L2); (2) memory-trails page adds HRR to 'deliberately NOT done' with reasoning; (3) Memory Trails extended to reinforce page→query edges in wiki_context — bonus surfaces in shadow mode as `[shadow] page recall trail: ...` reasons on each WikiContextPage but is NOT yet added to ranking score, kill-switch metric pattern (open question 3 on memory-trails resolved); (4) **Synaptic-tagging slice shipped** — new src/wiki/session-outcome.ts classifies each session as verified-success/likely-error/inconclusive using deterministic rules over verification commands (npm test, pytest, cargo test, tsc, jest, git commit, etc.), aggregateClusterTag rolls per-session verdicts up to clusters in src/wiki/raw-observations.ts, detectRawObservationClusters now sorts verified-success clusters above inconclusive above likely-error so reviewer attention compounds on learned-and-verified knowledge instead of unresolved debugging firehoses, maintenance-inbox snapshot+page render the synaptic Tag column with badges. Full test suite green (259 tests), build clean, wiki refreshed. New tests: 10 in session-outcome.test.ts, 2 sort/render integration tests in raw-observations.test.ts, 1 page-edge round-trip in memory-edges.test.ts.

- Maintenance review board grouping shipped (2026-05-06): MaintenanceReviewBoard.vue now groups the flat work-items list into collapsible sections by categoryLabel ("Lint · Review Now", "Memory · Promotion Ready", "Memory · Skill Promotion Ready", etc). Each group header shows item count + an "N urgent" red chip when applicable. Default rule: groups with at least one urgent item start expanded, others collapsed; if nothing is urgent the first group expands so the inbox doesn't open fully closed. Added "Expand all" / "Collapse all" toolbar. User-toggled state is preserved across the 5s auto-refresh by signature-based reseeding (only reseeds when group composition actually changes). Verified live at port 5177 with 74 items: collapses into 6 categories with only the 17 urgent page-drift findings open by default. Build clean, full test suite still 259/259 green.

- Lint resolve-actions slice shipped (2026-05-06): three new one-click actions wired into the maintenance review board to close the diagnostic-only gap. (1) `Snooze 30 days` on page-drift findings — new local-data/page-drift-snoozes.json store with lazy expiry pruning, lint pass skips snoozed slugs (resolves the user's 17 page-drift pile that was mostly noise from today's busy session). (2) `Insert H1 from slug` on missing-h1 findings — title-cased deterministic insert after frontmatter; idempotent. (3) `Archive skill file` on dormant-skill findings — moves the file into a sibling archive/ directory; gated behind the bridge confirmation (mirrors apply-proposal/apply-memory-promotion). Files: src/wiki/page-drift-snoozes.ts (new), src/wiki/store.ts (insertH1FromSlug + archiveGuidanceFile + lint snooze skip), src/wiki/maintenance-actions.ts (three new tool dispatchers + readOptionalNumberArgument helper), src/wiki/maintenance-inbox.ts (3 new action kinds + buildLintActions wiring + buildLintActionId widened), src/wiki/maintenance-runner.ts (project-log emission for inserted-h1/archived-guidance-file), src/wiki/review-bridge.ts + MaintenanceReviewBoard.vue (added archive-guidance-file to confirmation gate). Tests: 11 new — 8 in test/page-drift-snoozes.test.ts covering snooze persistence/expiry/idempotency, plus insert-h1 and archive-guidance-file integration tests in test/maintenance-actions.test.ts. Full suite 270/270, build clean, vitepress build clean. Verified live at port 5177: every one of the user's 17 page-drift findings now shows `Snooze 30 days` as the primary button. Deliberately did NOT ship `mark-claim-reviewed` (no lint rule reads last-reviewed frontmatter so the action wouldn't clear anything), `insert-summary-placeholder` (creates fake content that re-flags later), or `orphan-page → archive` (risky — can't distinguish abandoned content from work-in-progress drafts).

- Page-drift distinct-days gate shipped (2026-05-06): src/wiki/page-drift.ts now requires activity to recur across at least MIN_DISTINCT_DAYS_FOR_DRIFT_CHECK=2 distinct date headings within the recency window before drift fires. extractRecentEntriesMentioningPage return shape changed from string[] to { entries: string[]; distinctDays: number } so callers can distinguish session-noise (concentrated burst on a single day) from real drift (off-topic vocabulary spread across multiple working days). The gate is overridable via PageDriftDetectorOptions.minDistinctDays for projects that want stricter checks or the legacy single-day behavior. Live result on this dogfood repo: lint review-now bucket dropped from 17 page-drift findings to 9 — the 8 that disappeared were all pages mentioned only in today's bio-inspired-audit session (single-day burst). The 9 that remain are pages with legitimate multi-day mentions and are now an actually tractable review queue. Tests: 272/272 pass with multi-day fixtures across the page-drift test suite plus a new explicit single-day-burst suppression test and a minDistinctDays-override test. The Snooze 30 days button stays useful for genuine multi-day drift the operator chooses not to act on right now — it's no longer the only way to acknowledge session-noise.

- Edit-summary slice shipped (2026-05-06): page-drift findings now have a real resolve path beyond Snooze — when the operator decides drift is genuine, the work-item details panel renders an inline textarea pre-populated with empty draft + explanatory copy + a Save button. Submission flows through a new wiki_edit_summary action with confirmation gate (review-bridge.ts + Vue component agree on the gate); the bridge accepts a narrow summaryDraft scalar in the request body that is consumed only by the edit-page-summary case in maintenance-actions.ts (no general arg-override map — narrow surface prevents an operator from rewriting other action arguments). The editPageSummary helper in store.ts replaces the contiguous run of non-blank non-heading lines after the H1 + frontmatter, idempotent if the new text matches the existing summary. Files: src/wiki/store.ts (editPageSummary helper + EditPageSummaryResult type), src/wiki/maintenance-actions.ts (wiki_edit_summary case + ExecuteMaintenanceActionOptions.summaryDraft pass-through), src/wiki/maintenance-runner.ts (RunMaintenanceActionOptions.summaryDraft pass-through), src/wiki/review-bridge.ts (summaryDraft field + edit-page-summary in requiresBridgeConfirmation), src/wiki/maintenance-inbox.ts (edit-page-summary kind + tool + buildLintActions branch on page-drift + wider buildLintActionId union), MaintenanceReviewBoard.vue (summaryEditorState ref + isPageDriftLintItem/findEditSummaryAction/submitSummaryEditor + inline textarea UI + CSS + actionNeedsConfirmation update + runActionViaBridge argumentOverrides param). Also recovered store.ts and maintenance-inbox.ts after the IDE-buffer-overwrite incident — VS Code held stale buffers from session-start that overwrote on-disk state when the user switched focus; user closed the buffers without saving and recovery completed via git checkout HEAD + manual re-apply of today's edits. Full suite 272/272 still passing, build clean, vitepress build clean. Verified live at port 5177: expanding any page-drift finding shows the Rewrite first paragraph editor, save button stays disabled until non-empty draft is entered.

- Trust-gated auto-promotion shipped (2026-05-06): the brain-analogy slice. New module src/wiki/auto-promote.ts with isAutoPromoteEnabled (reads DENDRITE_AUTO_PROMOTE env var: on/true/1/yes/enable variants), findAutoPromotableMemories (pure function — no I/O — testable gate), and autoPromoteMemories (does the I/O sweep). Gate criteria: status=active, kind in {lesson,fact,warning} (skills excluded — separate promote-memory-to-skill flow), recallCount &gt;= 20, ≥1 typed-provenance source (file/command/decision/wiki — observation/raw kinds explicitly rejected), no contradiction-kind review finding referencing the memory, target page resolves to first existing relatedPages slug. Per-sweep cap of 10 promotions to prevent churn. CLI command `dendrite-wiki memory:auto-promote [--dry-run]` available always; --dry-run shows candidates without writing, apply requires DENDRITE_AUTO_PROMOTE=on. scripts/refresh-wiki.ts runs the sweep automatically when the env var is on, BEFORE refreshGeneratedWikiDocs so regenerated artifacts reflect the post-promotion state. Critical: only fires from explicit `npm run wiki:refresh`/CLI, NOT from per-action refreshes the maintenance-runner triggers — keeps cadence operator-controlled. Writes still produce normal git diffs (page edit + project-log append + memory record marked superseded), preserving the auditability principle. Dry-run on this repo found 6 high-trust candidates: VitePress Vue-tag-escape lesson (recall=144), store.ts repoRoot module-load timing (recall=137), VitePress middleware mounting (recall=63), markdown-as-Vue (recall=48), findMaintenanceInboxAction reviewProjectMemories caller (recall=28), benchmark snapshot field migration (recall=22) — all targeting existing pages with typed source-backing. Tests: 12 in test/auto-promote.test.ts covering each gate criterion plus env-flag parsing. Full suite 284/284, build clean.

- Documentation pass for the today's UX wave (2026-05-06): three wiki pages updated to bring the canonical record in line with shipped code. (1) ai-memory-companion-roadmap.md gets a new M8 row in the Progress Tracker — Trust-Gated Auto-Promotion shipped, with the gate criteria, env-var design, per-sweep cap, and auditability note. (2) maintenance-review.md gets two new sections: 'Lint Resolve Actions' (table of rule → primary action: page-drift→snooze/rewrite, missing-h1→insert-h1, dormant-skill→archive-skill, with the deliberate omission of auto-fix for stale-claim/unsupported-claim explained) and 'Trust-Gated Auto-Promotion' (gate criteria, three usage modes, never-fires-from-per-action-refresh principle); the existing 'What The Operator Actually Does' loop is updated to reference the new actions. (3) comparison-claude-mem.md §2 (Promotion path) gets a paragraph noting the opt-in auto-promote mode strengthens the moat — git diff stays the review surface, click cost drops to zero. Initial VitePress build failed because I used markdown link syntax to point at source files (`[name.ts](../../src/wiki/name.ts)`) which VitePress tried to resolve as page imports; converted those to plain backtick code spans instead. Final state: 284/284 tests, vitepress build clean (6.91s), wiki:refresh clean.

- Accepted maintenance action lint:page-drift:docs/wiki/ai-memory-companion-roadmap.md:edit-page-summary. Rewrote first paragraph of ai-memory-companion-roadmap.md (was 225 chars, now 552 chars). Changed paths: docs/wiki/ai-memory-companion-roadmap.md.

## 2026-05-07

- Accepted maintenance action lint:page-drift:docs/wiki/ai-memory-companion-roadmap.md:edit-page-summary. Rewrote first paragraph of ai-memory-companion-roadmap.md (was 552 chars, now 585 chars). Changed paths: docs/wiki/ai-memory-companion-roadmap.md.

- Promoted project-local memory mem_aa4a4d2b-0117-4664-b364-fa2a28c9e3da into ai-memory-companion-roadmap.

- Polished review board completion UX: replaced toasts with stacked frosted-glass top-right cards (title + message + tinted-disc icon + draining hairline progress) and added a per-item ink-stroke checkmark seal overlay that holds 1.4s before TransitionGroup leave gracefully collapses the resolved item. Reduced-motion media query updated to jump to final state instead of leaving the seal invisible.

- Promoted project-local memory mem_1ffc82f0-c846-400b-bfab-6561e46f076b into ai-memory-companion-roadmap.

- Promoted project-local memory mem_4673b3fb-fc2c-4d7a-a607-2e8a9e7a30be into agent-enforcement-architecture.

- Promoted project-local memory mem_8776bf58-2130-48c1-99b9-698d9a75cba0 into ai-memory-companion-roadmap.

- Preview parity slice landed: wiki proposals (route-guidance + merge-guidance) and memory→skill promotions now show a preview before apply, mirroring the existing memory-promotion preview. Added previewWikiProposal + previewMemoryPromoteToSkill (pure functions), two new bridge endpoints (/preview/wiki-proposal, /preview/memory-promote-skill), generalized PromotionPreviewModal.vue to a single discriminated-target surface, wired both new flows into MaintenanceReviewBoard.vue. The modal IS the confirmation surface (apply uses skipConfirm:true). 294/294 tests pass; docs build clean.

- Review board reframe landed: groups changed from source-kind ("Lint · Review Now", "Memory · Promotion Ready", "Proposal · merge-guidance") to purpose verbs (Promote · Graduate work upward / Reconcile · Fix divergence / Quiet · Acknowledge & dismiss). Group order is fixed Promote→Reconcile→Quiet. Purpose is derived from the primary action's kind. Hero tagline + stat tiles also use the verbs (X to promote · X to reconcile · X to quiet, plus urgent). Per-item eyebrow keeps the source kind ("Memory · Promotion Ready" etc.) so context is still legible inside each verb group. 294/294 tests pass; docs build clean; verified on the running review board (53 items grouped 32 promote / 2 reconcile / 19 quiet).

- Accepted maintenance action proposal:pending-review/route-guidance-agents-md:apply-proposal. Applied route-guidance proposal pending-review/route-guidance-agents-md and updated 1 path. Changed paths: AGENTS.md.

- Fixed two bugs in the review-board apply flow that made it feel like the page reset after every click. (1) Modal stayed open for the full ~1.5s completion-overlay hold + refresh because applyFromPreviewModal awaited all of runActionViaBridge before closing. Added an `onAccepted` callback that fires the instant the bridge POST returns 200; modal closes there, the per-item Done overlay carries the affirmation in place. (2) reseedExpandedGroupsIfNeeded was clobbering the operator's manual expand/collapse choices whenever a purpose group became empty (its key dropped from groupedWorkItems' signature). Switched to a constant 'seeded' signature so reseed is a one-time bootstrap. Apply now feels reactive: click → modal closes → row underneath shows ✓ Done in place → row animates out → next item slides up → operator stays anchored.

- Promoted project-local memory mem_106beeed-8d4c-4917-9d12-672cc910ef6e into ai-memory-companion-roadmap.

- Consolidated the review-board UX onto a single decision surface: click any row → unified item-detail modal that shows the full context (rationale + memory text or lint message or proposal snippets), the diff/record-card preview where applicable, and EVERY available action as a labeled button with a one-line description. Rows on the board collapsed to triage-only summaries (eyebrow + title + subtitle + caret) — no inline action buttons, no expand-to-see-details, no separate confirmation dialog. The maintenance-inbox.md wiki page (a 1,300-line text dump that duplicated the board) was collapsed to a thin redirect stub; nav now routes the "Inbox" link to /review-board too. 292/292 tests pass; docs build clean. Verified live: promotion-ready memory shows preview diff + Apply/Draft buttons; Quiet items show full memory context + Archive button. Known regression: page-drift items lost the inline AI drift-resolver multi-step flow; the standard snooze/insert-h1/read-page actions still work via the modal action panel — re-implementing the AI fix flow inside the modal is a follow-up slice.

- Reskinned the review board in JRPG menu aesthetic (Stella Glow / Octopath Traveler / Triangle Strategy reference). Hero replaced its filled stat-tile cards with a sleek horizontal stat strip — light-weight tabular-nums, tiny colored tick marks above each number, hairline cap separators with diamond ornaments at the edges. Group headers became section dividers: uppercase light-weight verbs, inline small-caps taglines, hairlines below, accent dashes to the left of each verb in the per-purpose color (no card chrome). Work-item rows stripped to list entries: no card backgrounds, no left-edge color bars, no rounded corners — just hairline separators between rows, hover gets a 3% wash and 2px caret slide. Color discipline tightened: meaning lives on small accents (urgent ticks, per-purpose verb dashes), not sprayed across panel backgrounds. Build clean, modal still works, board now reads as a focused triage sheet rather than a card list.

- Two small but important UX corrections on the board: (1) added 1.5rem horizontal padding to `.board` so the JRPG-minimalist content stops clinging to the VitePress sidebar — every hero, group header, last-action stripe, and row now indents consistently. (2) Replaced the duplicate top-nav entries ("Inbox" + "Review Board" both pointing at /review-board) with a single right-aligned Inbox link rendered into the `nav-bar-content-after` slot via InboxNavBadge.vue, complete with the live SSE-driven count badge (urgent tone when contradiction or review-now lint findings are present, pending tone otherwise). The teleport-into-existing-link gymnastics from the prior version are gone — the link is rendered directly. Build clean; verified live with the badge showing 51 pending items.

- Reskinned the review board with a tactical command-center aesthetic (anime mil-sim / Helldivers / Lost Light reference). Five new motifs: (1) diagonal red-striped tape header above the page title with a small-caps "OPERATIONS" label; (2) bold italic display title — *Review*Board with the first word in serif italic; (3) horizontal tab bar (All / Promote / Reconcile / Quiet) replacing the per-purpose accordion — active tab gets a red 2px underline and a solid red count pill; (4) small monospace rank chip on the right of each row carrying urgency + verb identity (!! red for urgent, P green for promote, R blue for reconcile, Q slate for quiet); (5) subtle cross-grid background pattern via inline SVG data-uri at 6% opacity. The activeTab ref filters the work-item list; if the operator drains the active tab to 0 items, a watcher snaps them back to 'all' so the board never goes empty. Build clean; verified live across All and Quiet tabs.

- Restyled the work-item rows as a tactical roster strip mirroring the JRPG personnel reference. Each row is now a single tall horizontal strip with seven discrete columns: 32px category-letter avatar (M/L/P, color-tinted by source kind), 32px solid-red action badge with crossed-X glyph, name with bold-italic first segment + lighter italic rest (split on first colon, em-dash, or word-boundary near char 22-30), italic red role label naming the primary action ("Apply Promotion", "Snooze Drift", "Archive Memory", "Promote to Skill"), white level pill ("Level: 36" using recall count for memory items / affected-path count for proposals), prominent 2.1rem black rank chip with white letter (SS yellow for urgent items, A/B/C black with a thin colored verb-tone underline for promote/reconcile/quiet), and a › caret. Hover dims a 3% wash across the row. Builds cleanly, verified live; rows now read at a glance as personnel-roster entries instead of generic list items.

- Five row-readability fixes on the tactical roster: (1) avatar tile widened from 32px to 3.4rem and re-labeled with self-identifying 3-letter codes — MEM / LINT / PROP — replacing the cryptic single-letter M/L/P; (2) rank chip replaced its arbitrary A/B/C/SS letters with semantic action-verb arrows — ↑ promote, ↻ reconcile, − quiet, ! urgent (yellow box) — so the row's verb is readable without a legend; (3) level pill became contextual: "Recalled 36×" for memory items (recall count), "5 paths" for proposals (affected-path count), filename for lint findings — replaces the generic "Level: X" that didn't tell the operator what the number meant; (4) the easily-confused promote pair now reads as parallel "Promote to Wiki" vs "Promote to Skill" role labels, with the destination noun making the difference obvious; (5) red action badge swapped its X glyph (which read as cancel/error) for a crossed-swords SVG (two diamond-blade shapes rotated +/-45° with hilt details) — engagement glyph rather than alarm glyph, matching the screenshot reference. Build clean; verified live across All / Promote / Quiet tabs.

- Replaced the uniform red crossed-swords action badge with per-action-kind icons in distinct colors. Eleven icon keys mapped from primary action: promote-wiki (green up-arrow-into-book), promote-skill (teal star), draft (light-green pencil), capture (green plus-in-circle), apply-proposal (blue check-in-circle), rewrite (indigo pencil + underline), insert-h1 (cyan capital H), archive (slate filing-box), snooze (slate crescent moon), diagnostic (grey magnifying-glass), urgent (red exclamation triangle — overrides everything else). Icon SVG paths live in an ACTION_ICON_PATHS lookup; the row template renders one &lt;svg&gt; with v-html on a wrapper &lt;g&gt;, the [data-action] attribute drives the badge background color via per-key CSS rules. Operator can now distinguish Promote-to-Wiki from Promote-to-Skill from Archive Memory at a glance, before reading the role label. Build clean; verified live across All / Promote / Quiet tabs — green doc-up vs slate archive-box vs teal-promote-skill all render distinctly.

- Fixed the skill-promotion modal layout — Source Memory + New Skill cards were getting visually crushed because nothing forced the record section to claim available vertical space. Four targeted changes: (1) `.modal-record-section` got `flex: 1; min-height: 0; overflow: auto` so it grows into the panel and scrolls internally if cards overflow; (2) shared `.modal-target / .modal-warnings / .modal-effects-section / .modal-actions-panel` paddings tightened from 0.85rem to 0.7rem and pinned `flex-shrink: 0` so they take only their natural height; (3) record-card padding shrunk from 0.95rem×1rem to 0.7rem×0.85rem and the source-memory full-text max-height pulled from 12rem to 9rem; (4) the three "What apply will do" effects bullets rewrote from long sentences to single-clause lines so the panel doesn't dominate vertically. Side-effect: discovered + recovered a corrupt local-data/project-memory-edges.json (duplicated trailing JSON from a concurrent writer race — separate bug worth investigating, but fixed in place by truncating to the first complete document so docs:build could proceed).

- Drafted [API Reference Generation Roadmap](./api-reference-roadmap.md) — design doc for auto-generating per-file TSDoc → markdown wiki pages under `docs/wiki/api/`, using the TypeScript Compiler API directly (no typedoc). 7 phases A1–A7; MVP is A1–A6, A7 is interface-only future-proofing for Python/Rust/Go. Output gets `lifecycle: generated` frontmatter, manifest-driven orphan cleanup, lint exemption, VitePress sidebar group, new `wiki_generate_api_reference` MCP tool + `dendrite-wiki docs:api` CLI. Pitched as the "documentation every dev hates to write — automated, reviewable, printable" feature for the OCD-binder-on-a-shelf audience; explicitly not built for the agent.

- A1 (single-file API reference extractor proof) shipped on `feat/api-reference-generation`. Modules: src/wiki/api-extractor/{types,extract,render}.ts. Test fixture at test/fixtures/api-extractor/sample.ts with committed expected JSON + markdown for regression. 7 dedicated tests (extraction shape, render shape, determinism × 2, @internal filtering, @deprecated flagging, unknown-tag preservation) all green. Full suite remains 299/299, tsc build clean. Notable design pivots during implementation: (1) ditched ts.createProgram in favor of ts.createSourceFile with setParentNodes:true — A1 needs no type checker, and Program-loaded files don't set parent pointers so getJSDocCommentsAndTags / Node.getText / printer all break; (2) read .jsDoc directly off declarations and take the LAST entry (immediately-preceding block) since file-level JSDoc gets attached to the first statement and would otherwise double-render. Phase A1 acceptance criteria from the roadmap all met.

## 2026-05-08

- A2 (multi-file walker + manifest + orphan cleanup) shipped on `feat/api-reference-generation`. New modules: src/wiki/api-extractor/walk.ts (Node 20 readdir-recursive walker with custom glob→regex converter, default exclude includes test/d.ts/internal/_internal/node_modules, plus a file-level @internal-tag escape hatch via a 2KB head read) and src/wiki/api-reference.ts (orchestrator with sha256 contentHash, idempotent writeIfChanged, defensive orphan cleanup that refuses to delete any slug not under `api/` prefix, dryRun support). Manifest schema v1 at docs/public/api-reference-manifest.json. Determinism: pages contain no clock-derived fields; only the manifest stamps `generatedAt`. 7 new orchestrator tests against an isolated tmpdir project (writes per-file, no-export skip, low-coverage warning, idempotence, contentHash matches sha256(pageBody), orphan cleanup, dryRun, walker @internal exclusion). Full suite 306/306, build clean. All A2 acceptance criteria from the roadmap met. Ready for A3 (cross-reference resolution).

- A3 (cross-reference resolution) shipped on `feat/api-reference-generation`. Two-pass orchestration: pass 1 extracts every source into a `pendingRefs` list (no rendering yet), pass 2 builds a global symbol index `Map&lt;name, LinkIndexEntry[]&gt;` then renders each page with a per-page resolver closure. Resolution rules per the roadmap: same-page `{@link}` → bare `#anchor`; unique cross-file → relative `./path.md#anchor`; multi-match disambiguates by shared module-path prefix; otherwise renders display text + `&lt;!-- ambiguous link: X --&gt;` HTML comment. Unresolvable targets render as plain text (no markdown link). Both unresolved + ambiguous cases push warnings into `ApiReferenceResult.warnings` (new kinds: `unresolved-link`, `ambiguous-link`). `@example` tags are deliberately excluded from resolution since their bodies are code. Renderer's `applyLinkResolution(text, undefined)` is a no-op so the A1 fixture and pure-renderer use cases are untouched. anchorFor() now exported from render.ts so renderer-TOC anchors and orchestrator-link anchors share one canonical source. 5 new A3 tests, full suite 311/311, build clean. All A3 acceptance criteria met. Ready for A4 (CLI subcommand + MCP tool).

- A4 (CLI subcommand + MCP tool surface) shipped on `feat/api-reference-generation`. New `dendrite-wiki docs:api [--dry-run] [--paths &lt;glob&gt;...] [--format human|json]` subcommand wired into src/cli.ts with --paths multi-positional parsing matching the existing context-for-diff pattern. Exit code 1 only on extraction-error warnings; low-coverage / unresolved-link / ambiguous-link warnings are informational. New `wiki_generate_api_reference` MCP tool registered in src/server.ts with zod input `{ paths?: string[]; dryRun?: boolean }`, returns full ApiReferenceResult JSON, captures a wiki_write benchmark event when actual page writes happened (skipped on dryRun and zero-change runs). New `docs:api` npm script. Tool list assertion in test/mcp-server.test.ts updated. 5 new integration tests in test/api-cli.test.ts: CLI human-format end-to-end, CLI --dry-run no-write, CLI --format json parseability, MCP tool happy path, MCP tool dryRun. Hit a Windows spawn EINVAL on execFile + npx.cmd; fixed with `shell: true` (harmless on POSIX). Full suite 316/316, build clean. All A4 acceptance criteria met. Ready for A5 (VitePress nav + lint exemption + wiki:refresh integration).

- A5 (lint exemption + wiki:refresh wiring + VitePress sidebar) shipped on `feat/api-reference-generation`. WikiPageLifecycle gained a `generated` value; parsePageLifecycle recognizes it; lintWikiPages now early-continues when page.metadata?.lifecycle === 'generated' so generated pages never produce missing-h1, missing-summary, orphan-page, unsupported-claim, stale-claim, or page-drift findings. refreshGeneratedWikiDocs (wiki:refresh entry) now calls refreshApiReference() before listWikiPages so the page catalog and search index see the freshly generated pages. VitePress config conditionally appends an "API Reference" sidebar group built from docs/public/api-reference-manifest.json — collapsed by default, omitted entirely when the manifest is missing or empty. New fixture test/fixtures/generated-lifecycle-wiki/ + lint exemption test in wiki-store.test.ts; existing scale-wiki refresh test extended to plant src/sample-feature.ts and assert the API page + manifest land as part of refreshGeneratedWikiDocs (page count assertions bumped 7→8 and 8→9 to account for the new generated page). Caught a docs:build break: the design doc's nested ```markdown / ```ts code fences were prematurely closing each other and Vue was parsing the angle-bracket placeholders as tags — fixed by switching the outer fence to four backticks and rewriting `&lt;placeholder&gt;` to `[placeholder]`. Full suite 317/317, build clean, docs:build clean. All A5 acceptance criteria met. Ready for A6 (dogfood pass on this repo).

- A6 (dogfood pass) shipped on `feat/api-reference-generation`. Ran `npm run docs:api` against this repo: 39 sources scanned, 37 API pages generated under docs/wiki/api/, 2 sources skipped (no exports). 36 low-coverage warnings as expected — this codebase has zero JSDoc culture today; the renderer remains useful as a signature catalog and the warnings act as a forcing function for future TSDoc adoption. Two renderer polish items based on what looked anemic: (1) interface bodies now render their full member list via printNode + leading-export-strip — empty `interface Foo` lines were uninformative; now readers see the actual shape; (2) function parameter default values are stripped in `renderParameter()` because `= {}` and `= process.env` are implementation detail, not signature. A1 fixtures regenerated; dogfooded output is markedly cleaner. Added the architecture.md cross-link section: docs/wiki/architecture.md "Generated Docs Pipeline" now lists the api/ tree as one of the generated artifacts and links to api-reference-roadmap.md plus the live api/wiki/i18n example, satisfying the "non-generated wiki page links to a generated page" acceptance. wiki_search for "i18n bundle translate" returns api/wiki/i18n as the top-ranked result (score 39) — generated pages are first-class citizens of the search index and the wiki graph. Full suite 317/317, build clean, docs:build clean. All A6 acceptance criteria met. Ready for A7 (designed-in language pluggability).

- A7 (designed-in language pluggability) shipped on `feat/api-reference-generation`. New `LanguageExtractor` interface in src/wiki/api-extractor/language-extractor.ts: { id, detect(rootDir), walk(rootDir, options?), extract(sourcePath, options?) }. Built-in `typeScriptExtractor` in src/wiki/api-extractor/typescript-extractor.ts wraps the existing extract+walk modules; detect() returns true if tsconfig.json, package.json, OR src/ exists (high-recall to preserve A1-A6 behavior). refreshApiReference now iterates `options.extractors ?? builtInLanguageExtractors` and dispatches to the FIRST extractor whose detect() returns true; subsequent extractors are not consulted. New no-extractor-claims path returns an empty result and still runs orphan cleanup against the previous manifest, so a project that switches stacks doesn't strand stale pages. 2 new dispatch tests: one verifies order (alpha=false→beta=true; gamma never queried), the other verifies the no-claim path emits empty results with no warnings. Architecture.md and the roadmap status table updated. Adding Python/Rust/Go support is now a drop-in 200-line module implementing this interface — zero orchestrator changes needed. Phase A7 acceptance criteria met: refactor was transparent (24/24 prior tests passed unchanged), stub extractor proves dispatch picks the right one. Full suite 319/319 (+2), build clean, docs:build clean. All 7 roadmap phases (A1-A7) DONE. Branch ready for review/merge.

- File-level TSDoc pass complete on `feat/api-reference-generation`. Added one ~10-15 line file-level docstring to every TypeScript module under `src/` (35 files). Followed the existing template established by `i18n.ts` and `ritual-state.ts`: one-line summary + paragraph(s) covering what the module owns, what it talks to, and what design constraints shape it. Re-ran `dendrite-wiki docs:api`: now 39 source files have meaningful prose at the top of their generated API page (was 1 before — only i18n.md had a body comment). Two issues hit during the pass: (1) literal `*/` in glob patterns inside JSDoc bodies prematurely terminates the comment block — fixed by rewriting prose around `**/*.ts` style references; (2) bare `&lt;word&gt;` placeholders (like `&lt;id&gt;`, `&lt;path&gt;`, `&lt;subcommand&gt;`) trip VitePress's Vue parser even inside backticks when they cross line boundaries — fixed by switching to descriptive prose ("the CLI's skill:export subcommand takes a memory id"). Also tightened the walker's `@internal` self-filter regex to require tag-position match (`^[ \t]*\*?[ \t]*@internal\b`) so prose mentions of `@internal` in docstrings no longer cause modules to self-skip. Final state: 41 sources scanned, 39 pages written, 0 deleted, full suite 319/319, build clean, docs:build clean.

- Python LanguageExtractor shipped on `feat/api-reference-generation` — second built-in language alongside TypeScript, validating the A7 pluggability surface with a real cross-language case. New module src/wiki/api-extractor/python-extractor.ts: detect() requires both a Python project signal (pyproject.toml/setup.py/setup.cfg/requirements.txt) AND a usable Python 3.9+ interpreter on PATH (`python3` then `python` candidate order, version verified via `--version`); walk() defaults to `**/*.py` excluding tests/.venv/build/dist/__pycache__/site-packages/node_modules; extract() spawns the interpreter once per file with the helper script piped via stdin (avoids Windows cmd.exe 8KB cmdline limit) and parses the JSON the helper emits. Embedded Python helper uses standard-library `ast` to walk top-level def/async def/class/Assign/AnnAssign/TypeAlias nodes, drops underscore-prefixed names per Python's privacy convention, maps Enum subclasses to kind=enum, recognizes PEP 613/695 type aliases plus a PascalCase-with-lowercase heuristic (so SCREAMING_CASE constants stay variable, not type-alias). pythonExtractor registered FIRST in builtInLanguageExtractors so a hybrid Python+Node project gets its Python sources documented; pure-TS projects with no Python signals continue to dispatch to typeScriptExtractor. 5 new integration tests (skip if Python unavailable on the runner): detect-positive, detect-negative, every-symbol-kind extraction, refreshApiReference dispatches to Python on a Python project, all-private file produces zero symbols. Full suite 324/324 (+5), build clean, docs:build clean. README and architecture.md updated. Future Rust/Go extractors are now obviously a 200-line drop-in following the same pattern.

- Phase B1a (tree-sitter generic extractor + Rust as proof) shipped on `feat/api-reference-generation`. Added `web-tree-sitter@^0.26.8` as a runtime dep. Vendored Rust grammar at upstream tag v0.24.2 under `vendor/tree-sitter/rust/` (1.1MB WASM + tags.scm + LICENSE, sha256 pinned in NOTICE). New module src/wiki/api-extractor/tree-sitter-extractor.ts: per-language config table (id/extensions/projectSignals/vendorSubdir/walkOptions/captureKindMap/docCommentRule/isPublic), upward vendor-root resolver that works under both tsx and built dist/, lazy WASM loading with module-level cache, runs each grammar's `queries/tags.scm`, walks for @definition.* captures, looks up corresponding @name capture, builds clean signatures by stripping the body node from the source slice, walks backward through preceding sibling comment nodes for adjacent doc-comment association, dedupes overlapping matches by (startIndex, endIndex). Rust public-symbol predicate inspects `visibility_modifier` child for "pub" prefix; file-level `//!` outer-doc collected at file head. treeSitterExtractor registered FIRST in builtInLanguageExtractors so a Rust+Node hybrid lands on Rust. Five new integration tests against a Cargo fixture (skip cleanly if vendor WASM missing): detect-positive, detect-negative, every-supported-Rust-symbol-kind extraction with public/private filtering and doc comments, full refreshApiReference dispatch + page-on-disk verification, all-private file → empty symbol list. One small fix on first run: imported Query as type-only when it needs to be a value too. NOTICE file lists vendored grammar with sha256/license/tag, README + architecture.md updated. Future Go/Java/Ruby/C/C++/PHP are config-table additions, not new modules. Full suite 329/329 (+5), build clean, docs:build clean.

- Phase B1b shipped on `feat/api-reference-generation` — six new long-tail languages (Go, Java, Ruby, C, C++, PHP) light up via the tree-sitter framework introduced in B1a. All grammars vendored from first-party github.com/tree-sitter org repos at pinned tags (Go v0.25.0, Java v0.23.5, Ruby v0.23.1, C v0.24.2, C++ v0.23.4, PHP v0.24.2), all MIT-licensed, sha256-pinned in NOTICE. tree-sitter-extractor.ts gained: per-language `bodyNodeTypes` set so signature rendering strips bodies cleanly across grammars (Java's class_body, Ruby's body_statement, C's compound_statement, etc.), an upgraded doc-comment walker that uses `previousNamedSibling` + walks up to ancestors when the captured node is a deeply-nested inner node (e.g., the C grammar's `function_declarator` is a child of `declaration` where the doc comment lives), per-language `isPublic` predicates (Go's capitalized-first-letter, Java's `public` modifier, PHP's `private`/`protected`-filter, C/C++'s non-static, Ruby's everything-is-public). Three iteration cycles: (1) initial run had Ruby/C failing on doc-comment association — fixed by switching from `previousSibling` to `previousNamedSibling` + ancestor walk-up; (2) C still failed because the walk-up returned the first sibling regardless of type — fixed by requiring it be a comment to short-circuit, otherwise continuing up; (3) C++ test had two same-named symbols (class Widget + Widget() constructor) collapsing through `Map(name → symbol)` — fixed test logic to lookup by (name, kind) tuple, which is the honest assertion. New parameterized test/tree-sitter-languages.test.ts table-drives 12 tests (2 per language: extraction + dispatch). Full suite 341/341 (+12), build clean, docs:build clean. README and architecture.md updated with per-language coverage table. Adding the next tree-sitter language is now a fixture-file change + config-table entry, no orchestrator code changes needed. Total install footprint after B1a+B1b: web-tree-sitter runtime (~200KB) + 7 grammar WASM files totaling ~5MB, lazy-loaded.

- B1c shipped on `feat/api-reference-generation` — 3 more languages, total now 12 (TS + Python + Rust + Go + Java + Ruby + C + C++ + PHP + C# + Swift + Lua). Vendored grammars: tree-sitter-c-sharp@v0.23.5 (5.4MB, MIT, Brunsfeld+Guard+Qureshi), tree-sitter-swift@0.7.2-pypi (3.5MB, MIT, alex-pinkus, community), tree-sitter-lua@v0.5.0 (55KB!, MIT, Munif Tanjim, vetted-community org). Each with sha256-pinned NOTICE entry. Per-language configs added to LANGUAGES array: C# uses `public` modifier check + `///` XML-doc + `/** */`, Swift accepts `public` or `open` (filters internal/private/fileprivate, treats no-modifier as not-public per Swift's `internal` default) + `///` and `/** */`, Lua uses a non-`local` predicate that checks both the captured node's text AND the source lookback for the `local` keyword. Test driver gained a `WASM_FILENAME_OVERRIDES` map because C# publishes its WASM as `tree-sitter-c_sharp.wasm` (npm package-name convention forbids hyphens). 6 new test cases via the parameterized table-driver. **Bash and Kotlin removed from this pack** — both grammars publish only `highlights.scm`, no `tags.scm`. Adding them requires either writing our own tags query (~30 lines of Scheme each) or sourcing one from an editor's runtime queries (Helix, Neovim). Documented as a follow-up. Full suite 347/347 (+6), build clean, docs:build clean. README and architecture.md updated. Total install footprint after B1a+B1b+B1c: web-tree-sitter runtime (~200KB) + 10 grammar WASM files (~14MB total — C# is the heavyweight at 5.4MB, Lua the lightweight at 55KB), all lazy-loaded.

- B1d shipped on `feat/api-reference-generation` — five more languages (Scala, Elixir, OCaml, Kotlin, Bash) bring the total to 15. Three use upstream tags.scm directly (Scala v0.26.0, Elixir v0.3.5, OCaml v0.24.2). Two ship with locally-authored tags.scm files because upstream grammars publish only highlights.scm: Kotlin (fwcd v0.3.8) gets a 4-pattern tags query covering class/object/type-alias/function; Bash (v0.25.1) gets a single-pattern query for function_definition. License compliance: 14 grammars MIT, Elixir Apache-2.0 — both compatible with project's Apache-2.0. Locally-authored tags.scm files are flagged in NOTICE under each grammar's entry. Two framework changes during the work: (1) COMMENT_NODE_TYPES expanded to include `multiline_comment` since Kotlin's grammar uses that instead of block_comment; (2) findStartingDocCursor now descends into the last children of a non-comment preceding sibling because fwcd's Kotlin grammar quirk absorbs trailing `/** */` blocks into the preceding `package_header` node — without this fix, Kotlin doc comments wouldn't attach. Also new: `requireExtensionPresent` config field that triggers a content-based detect for languages without project markers. Bash uses it to claim only directories containing actual .sh/.bash files; this also fixed a regression where Bash's empty projectSignals was claiming arbitrary directories. Full suite 357/357 (+10), build clean, docs:build clean. Three commits stacked on the branch: fefe1c7 (9 lang core), f6ba4de (C#/Swift/Lua, +3), 683245f (Scala/Elixir/OCaml/Kotlin/Bash, +5). Total install: web-tree-sitter runtime (~200KB) + 15 grammar WASM files lazy-loaded.

## 2026-05-09

- Added PreToolUse blocking + Stop blocking + smart PostToolUse marking in .claude/hooks/ to close the agent-discipline gap the agent-enforcement-architecture page documented as "visibility, not enforcement." Edits to Edit/Write/MultiEdit/NotebookEdit are now denied until wiki_context has been called for the current Claude Code session_id (per-chat, not per-MCP-process), and Stop is denied when edits happened without wiki_log. The PostToolUse marker for wiki_context skips the mark on error responses so the "result too large" path can no longer silently bypass the gate. Layered alongside (not replacing) the existing non-blocking skills:hook and ritual:hook nudges. Verified end-to-end with six smoke tests covering fresh-session deny, success-path mark, allow after mark, edit count tracking, Stop block, and error-path no-mark. State persisted to .claude/claude-code-ritual-state.json (gitignored). Follow-up: src/install.ts still seeds the older non-blocking-only .claude/settings.json for downstream projects; should be updated to also write the .claude/hooks/ scripts so downstream installs benefit from the same enforcement.

- Extended ritual enforcement to every supported environment that runs `dendrite-wiki init`. Three layers: (1) universal MCP-side gate in src/wiki/ritual-state.ts getRitualGateRejection() refuses 16 writing/applying tools (memory_remember, wiki_write, wiki_log, wiki_apply_proposal, etc.) until wiki_context has been called this MCP-session — works in every client (Cursor/Continue/Windsurf/Antigravity/Zed included). DENDRITE_DISABLE_RITUAL_GATE=1 env bypass keeps existing integration tests green. (2) Per-client PreToolUse + Stop blockers wired into Claude Code (.claude/settings.json), Codex (.codex/hooks.json), and Copilot custom agent (.github/agents/dendrite.agent.md) frontmatter; all reference the same .claude/hooks/*.mjs scripts. (3) src/install.ts ships the four hook scripts (lib.mjs, pre-edit-block.mjs, post-tool-mark.mjs, pre-stop-block.mjs) inlined as build*Hook() template strings; writeRitualHookScripts() copies them into downstream projects' .claude/hooks/. test/install.test.ts now asserts the inlined strings stay byte-for-byte identical to .claude/hooks/*.mjs (LF-normalized) — drift test fails loud. test/ritual-state.test.ts gains 5 gate tests covering deny-before-context, allow-read-only, allow-after-context, env-bypass, and full coverage of all 16 gated tool families. 378/378 tests pass; build clean.

- Opened experimental branch `retro-editor-experiment` and drafted [Retro Editor Experiment Roadmap](./retro-editor-roadmap.md) — a 7-slice plan (R0–R7) to add an in-browser markdown editor themed around pre-WYSIWYG word processors (Amber Terminal, WordPerfect 5.1, Selectric Print) for the documentarian-who-prints-and-binders audience. Markdown stays canonical; saves go through a new `/pages/write` review-bridge endpoint that calls `writeWikiPage` directly. Stretch goal: `dendrite-wiki binder:export` PDF compile via headless Chrome. Decision gate after R1–R3 dogfood week before committing to merge or graduating to the paid-tier track.

- R1 of retro-editor experiment shipped on `retro-editor-experiment` branch. Three retro themes (Amber Terminal / WordPerfect 5.1 / Selectric Print) selectable via a nav dropdown next to the Inbox badge, persisted in localStorage as `dendrite-ui-theme`, with an early-paint inline script in `head` to avoid flash of default theme. Themes work via `--vp-c-*` and `--vp-nav-bg-color` / `--vp-sidebar-bg-color` overrides on `html[data-dendrite-theme=...]`, plus webfonts (VT323, IBM Plex Mono, Special Elite, Cutive Mono) loaded from Google Fonts. Selectric Print includes a `@media print` block with page-break rules for the future binder PDF. Verified end-to-end via agent-browser screenshots of all four themes. Known polish gaps for R1.1: search-box and some nav chrome elements still use VitePress defaults in non-Modern themes. See [Retro Editor Roadmap](./retro-editor-roadmap.md).

- R2 of retro-editor experiment shipped on `retro-editor-experiment` branch. Adds a read-only in-browser markdown editor: new `GET /__review-bridge/pages/read?slug=` endpoint in `src/wiki/review-bridge.ts` returning `{slug, content, mtime, hash, bytes}` (sha256 + mtime are the precondition token R3's save path will check); `WikiEditor.vue` mounts CodeMirror 6 (markdown lang + line numbers + active-line highlight + history + search keymap) as a full-screen overlay with theme-token-driven styling, a WordPerfect-style status bar (SLUG / MODE / LINES / WORDS / function-key hints), and stubs for F2 Save (deferred to R3) and F5 Reveal Codes; `EditPageButton.vue` is a floating pill mounted via `Layout.vue`'s `layout-bottom` slot, self-suppresses on non-`wiki/*` pages. Verified end-to-end across Modern / Amber Terminal / WordPerfect 5.1 themes — editor inherits each theme's palette and font automatically. Print stylesheet hides the button. New deps: `codemirror`, `@codemirror/lang-markdown`, `@codemirror/state`, `@codemirror/view`, `@codemirror/commands`, `@codemirror/language`, `@codemirror/search`. See [Retro Editor Roadmap](./retro-editor-roadmap.md).

- R3 of retro-editor experiment shipped on `retro-editor-experiment` branch. The in-browser editor now writes back to disk through `POST /__review-bridge/pages/write` (body: `{slug, content, ifMatch: {mtime, hash}}`), which calls `writeWikiPage` in `src/wiki/store.ts`, appends an operator-authored project-log entry, and fires a `wiki_updated` benchmark event with new trigger `browser-editor` (added to `DendriteBenchmarkEventTrigger`). Concurrent edits surface as 409 with a side-by-side conflict resolver in `WikiEditor.vue` showing your-version vs current-on-disk byte counts, hashes, and content excerpts, plus three explicit choices (Cancel / Discard mine, reload from disk / Keep mine, overwrite disk) — never a silent overwrite. F2 and Ctrl+S keybindings wired. Verified end-to-end via agent-browser: typed an edit, pressed F2, saw SAVED toast + git diff + project-log entry + benchmark event, then forced a concurrent disk write and confirmed the 409 modal renders correctly with both versions visible. See [Retro Editor Roadmap](./retro-editor-roadmap.md).

- R4 of retro-editor experiment shipped on `retro-editor-experiment` branch. The in-browser editor now offers `[[`-triggered wiki-link autocomplete: typing `[[arch` shows a ranked popover (Architecture / Agent Enforcement Architecture / search-index / Search Graph And Scale / Team Tier Architecture for that query), and Enter replaces `[[arch` with the full markdown link `[Architecture](./architecture.md)` matching the existing wiki link style. New `GET /__review-bridge/pages/list` endpoint returning `{pages: [{slug, title}], count}` powers it; ranking is substring/prefix scoring on slug+title, top 12 candidates. Status bar gained an `LN/COL` cell tracking cursor position via `EditorView.updateListener`. New dep: `@codemirror/autocomplete` (the package set is now `codemirror`, `@codemirror/{state,view,lang-markdown,commands,language,search,autocomplete}`). Verified end-to-end via agent-browser: typed `[[arch`, popover rendered with 5 ranked matches, pressed Enter, the markdown link inserted on line 184 with cursor at column 38. See [Retro Editor Roadmap](./retro-editor-roadmap.md).

- R6 of retro-editor experiment shipped on `retro-editor-experiment` branch — the Compile-to-Binder workflow that's the audience payoff for the documentarian-who-prints. New `dendrite-wiki binder:export` CLI subcommand (`[--all | --pages a,b,c]`, `[--theme selectric | amber | wordperfect | modern]`, `[--output path]`, `[--title text]`). New `src/wiki/binder-export.ts` reads each wiki page via `readWikiPage`, strips frontmatter, renders the markdown via `markdown-it`, wraps each page in a numbered section with `PAGE NN OF MM` headers, and prepends a double-bordered cover page + numbered TOC. Output is a single self-contained HTML file with embedded CSS (palette per theme, page-break-before/after rules under `@media print`, B/W-friendly print overrides for Amber/WordPerfect). Default output `docs/public/binder.html` (added to `.gitignore`). Smoke tested with `--pages retro-editor-roadmap,architecture` → 45.5KB, cover/TOC/pages all rendered correctly in the browser. Operator opens in browser → File → Print → Save as PDF for the binder workflow. Auto-PDF via Puppeteer is intentionally deferred to optional R6.1 — the HTML-then-print path adds zero deps and works on every machine. New dep: `markdown-it` (+ `@types/markdown-it` dev). See [Retro Editor Roadmap](./retro-editor-roadmap.md).

- R5 of retro-editor experiment shipped on `retro-editor-experiment` branch. The in-browser editor gained a tabbed Body / Frontmatter view. Frontmatter tab renders typed inputs for the standard wiki keys (`lifecycle` and `source-coverage` as selects with the canonical enum values, `owner` as text, `last-reviewed` as a native HTML date picker), plus a free-form key/value extras table with add/remove rows for unknown keys. Form mutations are wired via a `syncFrontmatterToDoc` helper that dispatches CodeMirror transactions replacing only the frontmatter region of the doc — so the editor remains the single source of truth, the dirty flag flips automatically, and the existing R3 save path is unchanged. New `frontmatter-utils.ts` provides a tiny YAML-subset parser/serializer (~80 LOC, no js-yaml dep, ~50KB saved). Verified end-to-end via agent-browser: changed lifecycle active→dormant in the form, added a custom `review-board: design-decisions` row, switched to Body tab and saw the YAML block updated correctly, switched back to Frontmatter and the custom key was still in the extras table with the badge correctly showing "1 non-standard key". 6 of the 7 planned slices now done (R0–R6 minus R7 stretch). See [Retro Editor Roadmap](./retro-editor-roadmap.md).

- R1.1 polish landed on `retro-editor-experiment` branch — nav chrome cohesion across all retro themes. Patched VitePress's hard-coded `.DocSearch-Button` + Ctrl/K kbd chips, the `.VPLocalNav` sub-nav strip ("On this page &gt;"), `.VPMenu` flyouts, the mobile `.VPNavScreen` drawer, page-content scrollbars, and the InboxNavBadge's ochre/red brand colors so they all inherit `--vp-c-bg`/`--vp-c-text-1`/`--vp-c-brand-*` from the active retro theme via `html[data-dendrite-theme] .selector` overrides scoped under retro-only attribute selectors (modern theme behavior unchanged). Added theme-specific touches: WordPerfect inverts the search pill to deep IBM blue with yellow Ctrl/K chips matching the WP 5.1 status-bar aesthetic; Amber Terminal gets a subtle CRT glow on search hover. Hid `.VPNavBarAppearance` (the dark/light toggle) in retro themes since the palette IS the theme — keeping the toggle there was both visually cluttered and conceptually confusing. Verified all four themes (Modern / Amber Terminal / WordPerfect 5.1 / Selectric Print) before/after with agent-browser screenshots; all three retro themes are now fully cohesive end-to-end. See [Retro Editor Roadmap](./retro-editor-roadmap.md).

- R7 of retro-editor experiment shipped — completes all 7 planned slices on `retro-editor-experiment` branch. New floating "New Page" button (always visible, left of EditPageButton) opens a NewPageWizard modal with 6 starter templates (blank, architecture, decision-record, runbook, troubleshooting, roadmap), a title/slug/owner form (slug auto-fills from title on blur; owner remembered across sessions via localStorage), and pre-fills WikiEditor via a new `initialContent` prop. The save endpoint at `POST /__review-bridge/pages/write` was extended with create semantics — the four valid intent/state combinations (file exists ± if-match) are all handled explicitly: create on the fresh path, 409 with current state on slug-collision, 409 with empty current on file-deleted-out-from-under-us, normal precondition check otherwise. Project-log entries now use the verb `Created` (vs `Edited`) for first writes; benchmark events tag `created: true` so future telemetry can distinguish wizard-creates from regular edits. Verified end-to-end: opened wizard, picked decision-record template, filled "Browser Editor R7 Test Page", submitted, editor opened pre-filled, F2 created `docs/wiki/browser-editor-r7-test.md` (637 bytes, hash 976affa), SAVED toast appeared, editor flipped to normal edit mode. See [Retro Editor Roadmap](./retro-editor-roadmap.md).

- R7.1 polish on `retro-editor-experiment` branch — make the wizard's draft-preview-before-commit flow obvious. WikiEditor.vue now distinguishes "draft" state (wizard-created page that hasn't been saved yet) from "edit" state via `isDraft = initialContent !== undefined && !meta.hash`. In draft mode: title prefix flips from `EDIT` to `DRAFT` in warning color, a `NOT SAVED YET` toast appears next to the slug, a full-width banner across the top explains "Draft preview. Nothing has been written to docs/wiki/&lt;slug&gt;.md yet. Review the page, then click Save to wiki to add it, or Discard draft to throw it away." The action buttons relabel to `F2 Save to wiki` (visually promoted to primary action with brand-colored fill) and `Discard draft`, and the Esc confirmation message becomes draft-aware ("Discard this draft? &lt;slug&gt; has not been written to disk and will be thrown away."). Verified end-to-end: opened the wizard, picked the runbook template, filled "Draft Discard Smoke Test", clicked Discard draft, accepted the confirm — file does not exist on disk, project-log unchanged. Also fixed: the wizard's owner placeholder no longer hard-codes the operator's name (now reads "Your name or team"). See [Retro Editor Roadmap](./retro-editor-roadmap.md).

- Print Page button shipped on `retro-editor-experiment` branch — one-click "print this page beautifully" without operator-side print-settings tweaking. New `PrintPageButton.vue` floating pill (rightmost in the floating-action row, next to New Page and Edit Page) calls `window.print()`. The heavy lifting is in a comprehensive theme-INDEPENDENT `@media print` stylesheet in `styles/retro.css` that fires regardless of which retro theme is active: chrome (nav, sidebar, search, theme switcher, all floating buttons) hidden, palette flipped to black-on-white for legibility on any printer, typewriter typography (Special Elite + Cutive Mono with justified body and uppercase letter-spaced headings), `@page` margins (0.7in × 0.6in × 0.8in × 0.6in) sized to play well with default browser print headers, and page-break rules that respect content boundaries (h1 page-break-before, h2/h3 page-break-after avoid, pre/table page-break-inside avoid). The previous Selectric-only `@media print` block that ran only when the user was already in Selectric Print theme has been generalized so the result is identical no matter which theme is active when the operator hits Print. Verified: button mounts on /wiki/* pages, hidden on non-wiki routes, hidden in print preview itself. See [Retro Editor Roadmap](./retro-editor-roadmap.md).

- Skill-promotion modal: fixed the narrow-width responsive layout. Previous pass kept the "fixed-viewport modal with internal scroll regions" pattern even when stacked — at narrow widths, this collapsed each card's content area to ~30px because the rigid header/pills/summary/tags inside each card consumed all the allocated row height before flex:1 on the text body could grow. New pass: at narrow widths (container query &lt;760px or @media &lt;900px fallback), drop the fixed-viewport pattern entirely. Modal panel itself becomes vertically scrollable, cards lose their internal scroll constraints (`flex: none; overflow: visible`), text body keeps a `max-height: 18rem` so super-long memories still cap (and scroll within), but normal-length content shows fully. Standard mobile-modal scroll pattern. Verified at 820x1100 (cards stack, full content visible — source memory text, all 5 scope dimensions, after-applying strip, actions all reachable by scroll) and 1440x900 (side-by-side preserved, nothing regressed). The lesson: nested flex inside a fixed-viewport grid is great for "always visible" pinning but hostile to content that varies in size — flip the pattern when the form factor changes. See [Retro Editor Roadmap](./retro-editor-roadmap.md).

- Promoted project-local memory mem_0cd55447-f84f-4045-be0c-bc37dedd490c into agent-enforcement-architecture.

- Promoted project-local memory mem_dba1952d-1998-4277-abec-a5c1e8c84f87 into architecture.

- Opened `ai-mermaid-charts` branch and drafted [AI-Generated Mermaid Charts Roadmap](./ai-mermaid-charts-roadmap.md). 6 slices (M0–M5) plus a stretch (M6). Two surfaces converging on one insertion module: (1) operator-side via the in-browser editor's Insert Chart wizard (uses local Ollama, mirrors the existing review-board model picker pattern), (2) agent-side via new `wiki_insert_chart` and `wiki_replace_chart` MCP tools (frontier models generate Mermaid, the tool validates + anchors + writes). Mermaid chosen over D2 / PlantUML / Excalidraw because (a) every modern LLM is heavily trained on it, (b) the source is just text in fenced code blocks (git-diffable), and (c) VitePress integration is one plugin. Validation is server-side and non-optional — both surfaces parse the source with `@mermaid-js/parser` before writing, never silently corrupt a page. Anchoring is by heading rather than line number for stability. Idempotency via stable chart-id markers in the inserted block. Branch is built on `retro-editor-experiment` so the operator-side modal can wire into the editor that branch ships; will rebase on main after PR #6 merges. Decision gate: dogfood M1–M3 for a week, ship to paid-tier if 2 of 3 (frontier model uses the tool unprompted via skill, inserted chart actually clarifies the page, modal gets used to fill agent-missed gaps).

- M1 of ai-mermaid-charts experiment shipped — Mermaid rendering surface lit up. Installed `vitepress-plugin-mermaid` + `mermaid` peer dep, wrapped `defineConfig` with `withMermaid()` in `docs/.vitepress/config.ts`. Mermaid security level set to `strict` so LLM-generated diagrams can't accidentally smuggle `&lt;script&gt;` or `&lt;iframe&gt;` tags into the rendered wiki. Smoke test: added a flowchart to the chart roadmap page itself illustrating the "two surfaces, one insertion path" architecture (operator + Ollama on the left, frontier model + MCP on the right, both converging on chart-insert.ts). Verified via Playwright: the markdown ``` ```mermaid ... ``` ``` block renders as a proper `&lt;svg id="mermaid-..."&gt;` element with hexagonal nodes, cylinder storage shapes, dotted edges, and edge labels. Default Mermaid theme picks up VitePress's dark/light mode automatically. M2 (insertion + validation module) is next.

- M2 of ai-mermaid-charts experiment shipped — `src/wiki/chart-insert.ts` is the single insertion module both surfaces (operator wizard + agent MCP tool) call into. 29 unit tests, all passing. Pure-core split: `computeChartInsertion(content, args)` and `computeChartReplacement` are pure string-in/string-out functions tested without any file system or fixture-cwd dance, while `insertChartIntoPage`/`replaceChartInPage` are thin wrappers that read, compute, write, log. Validator uses heuristics (keyword-on-first-line, bracket balance, connection presence, HTML script-tag rejection) instead of `mermaid.parse()` — discovered `mermaid.parse({suppressErrors: true})` returns `false` for almost any non-trivial chart in Node because DOMPurify isn't available, which would cause false-rejects on charts that render fine in the browser. Trade-off documented in the roadmap's Open Questions; if it bites, the path forward is jsdom polyfill before importing mermaid. Anchor by heading via `findSectionEnd` that respects subsection boundaries (chart inserted after Section A's H2 lands AFTER its H3 subsection too, before next sibling). Idempotency via stable chart-ID markers `&lt;!-- chart:auto-{kind}-{hash7} --&gt;` where hash7 is sha256 of the trimmed source — same source + anchor = no-op. Captions render as italic `*Figure: ...*` lines below the chart fence. Three typed errors (`ChartValidationError`, `AnchorNotFoundError`, `ChartNotFoundError`) exposed for callers to discriminate. Full test suite at 407/407 in isolation (3 MCP-server stdio tests flake under parallel load on Windows but pass solo). Build is clean. M3 (MCP tool surface) is next.

- M3 of ai-mermaid-charts experiment shipped — MCP tool surface live. Two new tools registered in `src/server.ts`: `wiki_insert_chart(slug, mermaidSource, anchorKind, anchorHeading?, anchorLine?, chartKind?, caption?, dryRun?)` and `wiki_replace_chart(slug, chartId, newSource, caption?, dryRun?)`. Both call into the M2 chart-insert.ts module. Flat anchor params instead of a nested discriminated union — frontier models reliably produce flat MCP shapes; nested unions get more retry failures in practice. Two new `DendriteBenchmarkEventTrigger` values (`wiki_insert_chart`, `wiki_replace_chart`) so the per-session timeline distinguishes chart insertions from regular edits. Errors return as structured JSON with discriminator codes (`chart-validation-failed`, `chart-anchor-not-found`, `chart-not-found`) so agents can react programmatically — e.g., regenerate on validation-failed, pick a different anchor on anchor-not-found. New MCP integration test in `test/mcp-server.test.ts` exercises the full flow: insert → idempotent re-insert (noop=true, chartId stable) → replace (new chartId, old marker removed) → prose-source validation error → dryRun (no disk change). Saved a project-local skill memory scoped to `docs/wiki/**/*.md` with task keywords (diagram, flow, architecture, system, sequence, state machine, request path, data flow, process) that nudges agents to consider `wiki_insert_chart` when prose alone forces the reader to mentally trace through entities and arrows. All 22 MCP server tests pass; build is clean. M4 (Ollama synthesis endpoint for the operator-side modal) is next.

- M4 of ai-mermaid-charts experiment shipped — Ollama synthesis endpoint live. Three new pieces: (1) `src/wiki/chart-prompts.ts` with per-kind prompt templates for flowchart/sequence/state/class/er/gantt, each pinned to a `firstWord` constraint and including ONE few-shot example. Templates kept ~200 tokens of instructions to stay friendly to small local LLMs (3B-8B); long preambles hurt local-model output quality and increase latency. (2) `synthesizeWikiChart()` in `synthesis.ts` mirrors `synthesizeWikiDriftResolution` — uses the same provider-resolution path so cloud / ollama / agent fallbacks all behave identically. (3) `POST /__review-bridge/synthesize/chart` endpoint validates `chartKind`, `context`, optional `intent` and `model`, calls the synthesizer, returns `{ provider, status, mermaidSource, rawResponse, durationMs }`. Strips fences and conversational preamble from model output via `parseChartResponse` so the editor preview pane can render the result directly. The Ollama-model field is the same convenience shortcut the drift endpoint uses (matches the review-board's existing AI MODEL picker pattern). Three new error codes in the bridge (`synthesize-chart-failed` / `missing-chart-context` / `invalid-chart-kind`). 13 chart-prompts tests cover prompt rendering, intent inclusion, fence stripping, preamble stripping, stateDiagram-v2 hyphen handling. Review-bridge health-shape test updated with the new `synthesizeChartPath` field. 48/48 passing across M4-touched suites; build clean. M5 (the in-editor wizard that calls this endpoint) is next.

- M5 of ai-mermaid-charts experiment shipped — operator-facing Insert Chart wizard live. New `InsertChartWizard.vue` modal opened from a ▣ Chart toolbar button in the editor header. Six-kind diagram picker (flowchart / sequence / state / class / er / gantt) styled as radio cards. Ollama model dropdown reuses the review-board's `/__review-bridge/ollama-models` probe + localStorage persistence pattern (storage key `dendrite-chart-model`). Context textarea pre-fills with the section the cursor is in via a new `extractCurrentSectionContext` helper in `WikiEditor.vue` — walks back from cursor to the nearest preceding heading and forward to the next sibling at the same or higher level. Optional intent + caption inputs. **Generate** calls `POST /__review-bridge/synthesize-chart` (the M4 endpoint), preview renders client-side via `mermaid.render()` with `securityLevel: 'strict'` matching production. Failed renders show the parser error inline so broken Mermaid never reaches the editor — operator can edit the source textarea (preview re-renders on watch) or regenerate with a different model. **Insert at cursor** dispatches a CodeMirror transaction wrapping the source in the same marker-comment + fenced block as `chart-insert.ts` produces server-side; chart-id uses a `manual-` prefix so editor-vs-agent insertions can be distinguished in metrics. Browser-verified at 1440x900: wizard opens, all six kinds render as cards, context auto-fills with the architecture page section, model picker shows the default option, footer Insert button correctly disabled until generation succeeds. End-to-end synthesis path waits on a real Ollama install for visual proof. Build clean. M5 closes the planned roadmap (M0–M5); M6 (inline chart editing on rendered pages) remains a stretch.

- M6 of ai-mermaid-charts experiment shipped — closes the roadmap. Inline chart editing on rendered wiki pages: hover any marker'd Mermaid chart to reveal a ✎ Edit button, click to open a side-by-side source textarea + live preview overlay, edit + Ctrl+S or Save → calls new `POST /__review-bridge/charts/replace` endpoint (which wraps the same `replaceChartInPage` module the M3 `wiki_replace_chart` MCP tool uses, so validation + idempotency + project-log + benchmark side-effects are identical between agent and operator surfaces). Three new pieces: (1) `ChartEditAffordance.vue` global scanner mounted in Layout that runs after every route change, fetches markdown via `/pages/read`, parses out marker'd chart blocks, and dynamically attaches Edit buttons to corresponding rendered SVGs; cached for 30s per slug. (2) `EditChartOverlay.vue` with side-by-side source/preview, Mermaid render via the same `securityLevel: 'strict'` config as production, structured error display when render fails. (3) New endpoint with three new error codes (`chart-replace-failed` / `chart-validation-failed` / `chart-not-found` / `missing-chart-id`). One subtle design decision: hand-authored (non-marker'd) charts are silently SKIPPED by the affordance — only chart-insert.ts-produced charts get the Edit button, since they're the only ones with stable chartIds. Hand-authored charts have to be re-created via the wizard or MCP tool to become editable inline. Promoting hand-authored charts in-place is a future iteration (would auto-insert the marker on first edit). Browser-verified end-to-end on the chart roadmap page: marker'd chart shows the ✎ button on hover, opens the overlay with chartId `auto-flowchart-2sur1ip` and full source loaded, live preview renders the flowchart correctly. Build clean. M6 was the stretch goal — the planned roadmap (M0-M5) is now done plus M6 graduated.

- Fix: chart synthesis now repairs the small-model "all on one line, semicolons as separators" failure mode. Operator hit this with `gemma3:e4b` — model produced `flowchart TD A[Run wiki_proposals] --&gt; B[Read proposal summaries]; B --&gt; C{Want to review pages?}; ...` and Mermaid rejected it with "Expecting NEWLINE, got NODE_STRING". Added `normalizeMermaidLayout()` to `src/wiki/chart-prompts.ts` that detects compact (single-line) output, splits the header keyword onto its own line, and splits any top-level semicolons (those NOT inside `[]`/`()`/`{}`/quotes) into separate indented statements. Called from `parseChartResponse` so both the wizard preview and the agent insert paths benefit. Strengthened the flowchart prompt template with explicit "CRITICAL FORMATTING RULES" section: header on its own line, each node and edge on its own line, NO semicolons as separators. 6 new tests cover the exact failure case + edge cases (multi-line input left alone, semicolons inside labels preserved, trailing semicolon doesn't produce empty statement, single-line without semicolons still gets header split off). 19/19 chart-prompts tests green.

- Fix #2 for small-model Mermaid output: handle "header on own line, body smashed with whitespace separators" failure mode. Operator hit it again with `gemma3:e4b` after the first fix shipped — model correctly put the header on its own line but then concatenated all body statements together with spaces (no semicolons, no newlines). Mermaid: "Expecting NEWLINE, got NODE_STRING". Added `splitOnAdjacentStatements()` to `chart-prompts.ts` that walks char-by-char tracking bracket depth + string state and detects the boundary pattern: top-level closing bracket (`]`/`}`/`)`) followed by leading whitespace, an identifier, then either another node-shape opener (`[`/`{`/`(`) OR a Mermaid arrow operator (`--&gt;`/`---`/`-.-&gt;`/`==&gt;`/`-&gt;&gt;`) OR a pipe (`|`). Critical detail: only advance past the LEADING whitespace (skip just the separator), not past the identifier — the identifier needs to be emitted into the next statement. `normalizeMermaidLayout` now applies BOTH passes (semicolon split AND adjacency split) per body line. Three new tests cover the exact failure case + edge cases (bare node declarations chained without arrows split correctly, arrow chains `A --&gt; B --&gt; C` left as a single statement, semicolons inside labels still preserved). Full check: build clean, 430/430 tests passing (was 421 before, +9 new chart-prompts cases).

- Fix #3 for small-model Mermaid output: handle "two statements glued together where the boundary is between bare identifiers". Operator hit it again — model produced mostly correct output but one line had `G --&gt;|yes| F G --&gt;|no| H[Ready to apply proposal?]`. The boundary is between bare-identifier `F` (destination of first edge) and `G` (source of second edge). Previous splitter only fired on closing-bracket boundaries (`]`/`}`/`)`); bare-identifier-end was untouched. Extended `splitOnAdjacentStatements` to also detect: bare identifier at top-level (next char is whitespace), where the identifier sits immediately after an arrow operator (with optional pipe-wrapped edge label between them). Crucially the "after-arrow" guard prevents false-cuts on the source side of edges (`A --&gt; B C --&gt; D` correctly splits into 2 statements, not 3 — `A` doesn't start its own line). The pipe-label-aware lookback regex handles `--&gt; |yes| F` patterns. Two new tests cover the exact failure case + the false-positive guard. Full suite: 432/432 passing (3 new chart-prompts cases added across the last two fixes).

## 2026-05-10

- - Redesigned BenchmarkReport.vue to match the MaintenanceReviewBoard's tactical-minimal aesthetic. Replaced gradient/shadow card panels with hairline-bordered sections, a stripe-tape "BENCHMARK" header, italic Times serif "*Local*Benchmark" hero, and a 4-stat strip with thin colored ticks instead of giant rounded gradient cards. Trend rows are now compact label/value/sparkline/delta with hairline dividers, sparklines stroke 1.5px in muted text-color (improving green / warning red when toned). Recall + Maintenance sections use the same horizontal stat-strip pattern; Selected/Omitted pages collapsed into a 2-column eyebrow+list layout. Tabular nums + small-caps labels throughout. Status pill (Improving/Steady/Regressing) replaces the "deltaTone" badge; commit shortened to 8 chars; baseline/latest/snapshots presented as a hairline-divided dl window. New tokens scoped under `.bm-report` (bm-color-* + bm-hairline). All visual; script logic unchanged except trendPath gained an optional height arg, plus shortCommit + statusLabel computeds. Verified end-to-end via agent-browser screenshot at 1400×2400.

- - BenchmarkReport.vue: added rich hover tooltips to every data point so the report explains itself. Each metric (orientation, wiki health, recall) now carries a description + direction-of-good + per-snapshot history series (e.g., `Top-1 hits — Probes whose expected memory ranked #1 in recall. Higher is better.\nDirection: higher is better.\nHistory: 6 → 7 → 7 → 7 → 7\nΔ vs. baseline: +1`). Hero stats, status pills (Branch / Commit / Improving·Steady·Regressing / Generated), Baseline/Latest/Snapshots dl entries, recall strip cells (Probes evaluated, Top-1, Top-5, Misses), and maintenance strip cells (Context requests, Wiki updates, State captures, Proposals accepted, Lint findings, Active proposals) all carry titles. Section headers carry a "what is this section" hover hint. Hoverable elements get `cursor: help` and a subtle hover tint (color-mix text-1 3%) so they signal as interactive. Implementation: extended TrendMetric with a required `description` field and added two lookup tables (headlineMetricInfo, recallStripInfo, maintenanceStripInfo); helpers `metricTooltip`, `headlineTooltip`, `stripCellTooltip` build the multi-line title strings; `metricSeriesText` truncates to first + last 6 with an ellipsis when history is long. tsc clean.

- Promoted project-local memory mem_30476154-63f5-4ec2-8ff7-67f2c3d4c7fd into benchmark-report.

- Promoted project-local memory mem_69ab9049-03ba-48d9-947e-f169d9385955 into architecture.

- Promoted project-local memory mem_586d17a2-890d-4f67-9f76-f7422e66cfff into agent-enforcement-architecture.

- Review Board: row removal now visibly sweeps off to the right before the gap closes. Updated `.rb-item-leave-active`/`.rb-item-leave-to` in MaintenanceReviewBoard.vue — translateX(112%) over 520ms with an aggressive ease-in tail, opacity fades 80ms in, and max-height/margin/padding collapse is delayed ~280ms so the row physically leaves before the gap closes. Added `overflow-x: hidden` on `.work-list` to prevent the sweep from extending the page's horizontal scroll. Reduced-motion users still get the existing instant-remove fallback.

- Review Board sweep-away: bug fix follow-up. The first attempt silently no-op'd because `.work-item.just-completed` (specificity 0,2,0) sets a `transition` shorthand and stays on the row during the leave (cleared at 5.5s, well after the leave finishes), overriding the bare-class `.rb-item-leave-active` (0,1,0) leave transition — opacity/transform/max-height snapped instead of animating. Bumped the leave/enter/move/leave-from/leave-to/enter-from selectors to `.work-item.rb-item-*` (0,2,0) so they tie on specificity and win on source order. Reduced-motion `transition: none !important` block already wins via !important and was left untouched.

- Promoted project-local memory mem_100196ba-2315-43f7-8aba-38a540eb0cd1 into ai-memory-companion-roadmap.

- Promoted project-local memory mem_0c9319ea-94c6-4355-ab45-545bfefb70f8 into agent-enforcement-architecture.

- Review Board sweep-away (round 3, verified): switched from fighting TransitionGroup leave specificity to an *imperative* swipe. New `swipingOutItemId` ref → `.is-swiping-out` class binding → CSS transition for transform+opacity. Handler now: bridge POST → set justCompletedItemId (overlay shows for 1.4s) → set swipingOutItemId (row sweeps 110% right + fades over ~600ms) → refreshBoardData (item silently removed) → clear swipingOutItemId. Verified end-to-end against the live dev server via Playwright reactivity injection: overlay renders correctly (opacity 1, ring drawn, label visible) AND the row physically translates 1203px right with final opacity 0. The earlier `.work-item.rb-item-leave-active` specificity gymnastics are reverted; the leave transition is back to the original simpler form (it's now only a *fallback* for items removed by background refresh, not by an explicit action). User-side fix: a hard refresh (Ctrl+Shift+R) was likely needed because Vite HMR can leave Vue script state stale across edits.

- Review Board sweep-away (round 4 — animation curve fix). User reported "still doesn't animate beautifully — just disappears after the green checkmark." Root cause: the previous transition-based design had opacity dropping in parallel with transform (opacity 320ms ease 80ms vs transform 520ms), so the row was already at opacity 0 by ~400ms while transform was only ~30% done. Eye reads that as "fade in place", not "swipe across". Replaced the transition with a CSS keyframe `@keyframes rb-swipe-out` that holds opacity at 1.0 through 55% of the duration (matching the iOS/Gmail/Material swipe-to-dismiss pattern — translate visible at full opacity for the first half, fade only in the latter third). Verified the curve in the live dev server with Playwright sampling at 5 timepoints: opacity is 1.00 at t=100/250/400ms (during which the row visibly translates from +6px to +232px), then drops to 0.79 at 550ms (+512px) and 0.24 at 700ms (+1050px). Total duration 720ms with cubic-bezier(0.55, 0.06, 0.68, 0.19) ease-in (the "flick" curve). Animation `forwards` fill keeps the end-state locked while the subsequent TransitionGroup leave handles the height collapse underneath. No JS animation library needed — the conflict the user was hitting was timing-curve, not technology.

- Review Board sweep-away (final): replaced the homegrown CSS-keyframes/specificity approach with `@formkit/auto-animate` 0.9.0 + a custom KeyframeEffect plugin. This solves the user's "just disappears" report at the root: AutoAnimate's plugin path bypasses prefers-reduced-motion (auto-animate/index.mjs L721-723: `isDisabledDueToReduceMotion = mediaQuery.matches && !isPlugin(config) && !config.disrespectUserMotionPreference` — if config is a plugin function, the gate is skipped), which is the most likely reason the user's browser was showing instant-remove despite my CSS animation working in mine. Replaced `&lt;TransitionGroup&gt;` with a regular `&lt;ol&gt;` bound via `useAutoAnimate` composable (chosen over manual `autoAnimate(el, plugin)` because the `&lt;ol&gt;` is inside a v-if branch that isn't in the DOM at onMounted time — the composable's reactive ref handles late-mount correctly). Removed all the imperative swipe code (swipingOutItemId ref, class binding, handler timing dance, .is-swiping-out CSS, @keyframes rb-swipe-out, the rb-item-* leave/enter/move CSS) — the JS handler simplified back to "set justCompletedItemId, wait 1.4s for the checkmark, refreshBoardData", with AutoAnimate playing the swipe automatically when the item leaves the workItems array. Final keyframe distribution (offsets 0/0.25/0.5/0.8/1 with translates 0/22%/52%/98%/125%) and Material-standard easing cubic-bezier(0.4, 0, 0.2, 1) at 700ms duration produces visible motion immediately: empirically sampled in live page — t=80ms→+29px, t=180ms→+230px, t=280ms→+743px, all at full opacity until t=280ms when the fade kicks in. Verified `position: relative` is set on the parent (AutoAnimate's signature side-effect), the swipe runs on DOM mutation, and the element is cleanly removed after the animation finishes.

- Review Board: switched 'remove' animation from horizontal swipe to vertical squish. Same AutoAnimate plugin, just different keyframes for the 'remove' action: scaleY collapses 1→0 with transform-origin: center, maxHeight crushes from 120px→0, opacity fades 1→0 only in the last third. Duration shortened to 480ms (squish is a tighter motion than the swipe). Verified live: t=63ms→scaleY 0.99 (full opacity), t=187ms→scaleY 0.59 (visible squash), t=240ms→scaleY 0.27 (almost flat), t=342ms→scaleY 0.06 (gone). The squish reads as the row being absorbed back into the list rather than thrown off — quieter affordance that suits frequent administrative actions where a big horizontal sweep per row would feel overdone.

- Review Board: synced the green completion seal with the vertical squish. The 1.4s overlay hold left the row sitting at full size before the squish — operator perceived the overlay as "delaying" the squish. Cut the hold to 360ms and tightened the seal SVG keyframes (ring 30→290ms, check 200→400ms, label 260→480ms) so the seal is fully bloomed by the time refreshBoardData triggers AutoAnimate's squish. Squish then runs 360→840ms with the seal scaling down inside the row (overlay is a position:absolute child of the work-item, so it inherits the parent's scaleY transform). Verified live: at 402ms scaleY is 0.998 (squish just starting, seal at full opacity), at 592ms scaleY 0.31 with row collapsing through height 9px. Total click-to-gone: ~840ms (was ~1880ms).

- Review Board: reverted 'remove' animation from squish back to horizontal swipe per user preference. Same AutoAnimate plugin, just the swipe keyframes (translate3d 0→125% over 700ms with Material standard easing) instead of the squish keyframes. Kept the 360ms hold + tightened seal animations so the seal lands fully bloomed exactly as the swipe begins, then rides off-screen with the row. Total click-to-gone ~1060ms.

- Promoted project-local memory mem_92242961-dc88-4417-aedc-d15179e5d523 into architecture.

- Review Board: ordered the modal-close before the row swipe. Operator reported the row appeared to animate before the modal closed — root cause was the modal unmounted instantly (no leave transition) at the same moment the row's seal+swipe started, so the eye lost its anchor right as the row started moving. Wrapped PromotionPreviewModal in `&lt;transition name="rb-modal"&gt;` with a 220ms opacity fade leave, and bumped the handler hold from 360ms to 620ms. New sequence: t=0 onAccepted → modal starts fading + seal starts blooming → t=220 modal gone → t=480 seal complete → t=620 swipe begins → t=1320 row gone. Verified live with Playwright sampling the modal opacity at 44/127/202/285ms (0.94/0.25/0.03/unmounted) — fade is smooth and complete before the swipe trigger fires.

- Memory review board: restored memory_restore (inverse of memory_forget archive), surfaced 'growing' memories as a new review kind + 'Observe' tab, and shipped the memory_auto_clean_apply/revert/runs MCP triad. Auto-clean batches accept LLM-produced {memoryId, verb (archive | keep-and-watch), reason, confidence} decisions, persist a runId-keyed record to local-data/auto-clean-runs.json, and are fully revertible via the same runId. Server stays LLM-free; the calling agent (Review Board bridge or any other client) is responsible for producing the decisions. Step 4 (UI auto-clean button + LLM round-trip in the review-bridge + summary panel) is deferred — backend is fully shipped and tested.

- Promoted project-local memory mem_f5e1d3eb-8805-48da-a76e-3745416d31f4 into paid-tier-roadmap.

- Auto-clean Review Board feature shipped end-to-end: added synthesizeMemoryAutoCleanDecisions in synthesis.ts (uses Ollama's `format: 'json'` mode with a strict prompt and defensive parser that drops malformed entries), three new bridge routes (/auto-clean/memories, /auto-clean/revert, /auto-clean/runs) wired through the same-origin VitePress plugin, and an "Auto-clean memories" button in the Review Board hero that opens an inline summary modal with per-decision verb/reason/confidence/outcome rows and a single "Revert this run" button. Server stays LLM-free — the bridge owns the round-trip. 97 tests across the affected files green.

- Auto-clean reliability pass for slow hardware: (1) removed the 20-candidate cap in the bridge — the endpoint now walks every available candidate; (2) chunks into batches (default 8 candidates per LLM call) so the model never has to coherently judge 70+ memories in one prompt; (3) Ollama requests now pass keep_alive='15m' so the model stays resident between batches instead of paying a cold-load penalty per call; (4) batch failures are non-fatal — earlier batches' decisions still apply, the run record reports which batches were skipped and why; (5) the result modal shows an elapsed timer during the (multi-minute) wait and refuses to dismiss on backdrop click while running so the result is always visible when it arrives.

- Auto-clean parser tolerance: Ollama's format:'json' mode tends to emit object roots like {"decisions": [...]}, {"results": [...]}, or keyed maps {"mem_abc": {verb, reason}} rather than the bare array the prompt asked for. Replaced the strict array-only parser with a recursive walker that collects every decision-shaped object regardless of wrapper depth, plus a keyed-map detector for the mem_*-as-key shape. Prompt now explicitly requests {decisions: [...]} since that matches what format:'json' favors. 10 new tests in memory-auto-clean-synthesis.test.ts pin down each shape variant the parser must tolerate. UI now shows the raw LLM response in a &lt;details&gt; on parse-failed so operators can debug model misbehavior in-place.

- Auto-clean live progress + locked-while-running modal: the bridge endpoint now responds with NDJSON (one JSON event per line) — started/batch-start/batch-complete/batch-failed/result — so the UI gets real-time per-batch updates instead of waiting for the whole run to finish before any feedback. The Vue modal reads the stream via fetch + ReadableStream, renders a progress bar (batches completed / total), the current decision preview (last verb + reason that came back), elapsed timer, and running batch-failure count. All three close paths are now disabled while a run is in flight: the X button, the footer Close button, and the backdrop click — preventing accidental dismissal of a multi-minute run.

- Strategic pass: analyzed AI memory core state vs brain-simulation goal. Confirmed strong brain-faithful architecture (Hebbian edges, lazy decay, spreading activation, three-tier consolidation) but identified four gaps — no salience/affect field, no sleep-cycle offline consolidation pass, no working-memory current-goal slot, no episodic→semantic auto-clustering. Most actionable gap: agent-drift asymmetry where Stop denies until wiki_log fires but not until memory_remember fires, despite confirmed warnings (mem_7d531792, mem_5480f5cc) that mid-session drift is the dominant failure mode. Memory hygiene state healthy (65 records, 0 stale/dup/contradiction) but 32 unsupported and 9 promote-ready records sit invisible at session start. Captured operator phrasebook proposal: "always/never/from now on/the reason we" detected by ritual:hook should nudge memory_remember; "promote/pin/forget that one" chat verbs should trigger corresponding tool. Ranked 10 improvements by leverage; recommended shipping memory-deposit Stop gate + backlog-aware session-start as the smallest pair that closes drift asymmetry from both ends.

## 2026-05-11

- Brain-faithfulness roadmap: shipped first triple slice. Documented all 10 improvements at new wiki page docs/wiki/brain-faithfulness-roadmap.md with brain-analog map, per-slice acceptance criteria, trade-offs, and recommended ship order; wired into VitePress sidebar and cross-linked from ai-memory-companion-roadmap (as forward-looking track) and agent-enforcement-architecture (as the mid-session memory-deposit gate's home). B1 SHIPPED: pre-stop-block.mjs now denies Stop if edits&gt;=1 and memory_remember was never called this session — mirrors the existing wiki_log gate, closes the drift asymmetry confirmed by warnings mem_7d531792 and mem_5480f5cc. Constant MEMORY_REMEMBER_REQUIRED_EDITS=1 lives in both .claude/hooks/pre-stop-block.mjs and src/install.ts buildPreStopBlockHook (must stay byte-for-byte identical — install.test.ts subtest 8 enforces). SessionStart message updated in both src/install.ts and .claude/settings.json to announce the new gate. B5 SHIPPED: summarizeMemoryBacklog() in src/wiki/memory-store.ts returns {promotionReady, skillPromotionReady, staleUnsupported, total}; buildWikiContext now embeds memoryBacklog in WikiContextResult and prepends "Memory backlog: ... waiting in the inbox" banner to the briefing whenever total&gt;0 — banner suppresses on zero, dual-counting intentional. New test file test/memory-backlog.test.ts with 5 passing tests. B7 SHIPPED: enhanced existing "Tiered Retrieval (L0/L1/L2)" section in architecture.md with per-tier "Use when..." guidance, explicit cross-links to memory-trails and skills-as-memory pages, and bio-inspired audit citation so design rationale survives memory decay. All 65 tests across install/wiki-store/memory-backlog/context-cache/skill-load-and-context/ritual-state/skill-promotion suites pass green. Remaining: B10 (why-linter), B4 (current-goal slot), B2 (salience), B3 (phrasebook+matcher), B6 (auto-archive), B9 (consolidate), B8 (page-trail promotion).

- Brain-faithfulness roadmap: shipped the recommended B10 → B4 → B2 trio cleanly. B10 (why-linter): rejects kind:"lesson" memories whose body lacks causal markers from MEMORY_CAUSAL_LANGUAGE_PATTERNS (32-entry vocab); per-call force=true bypass for legitimate edge cases, suite-wide DENDRITE_DISABLE_WHY_LINTER=1 env var for tests mirroring the existing DENDRITE_DISABLE_RITUAL_GATE pattern. Fixture cleanup across 6 test files using explicit force:true annotations. B4 (current-goal slot): RitualState gained currentGoal:{query,setAt}|null; recordToolCall(name, {query?}) updates it via Jaccard token-overlap &lt;0.5 distinct-task rule; checkpoint footer now shows `Current goal: "..." (set N ago)` whenever a goal is set, even when no reminders fire. tokenizeGoalQuery + jaccardOverlap + formatRelativeAge helpers all exported and tested. B2 (salience): optional salience:0-3 on ProjectMemoryRecord with auto-propagation floor (new memory sharing relatedFiles with existing salience&gt;=2 inherits salience=1 — never higher); recall scoring adds the value as a capped +3 bonus with "salience: pinned (N)" reason; new memory_pin MCP tool for operator surface. CRITICAL schema-migration gotcha captured: normalizeStoredMemoryRecord must explicitly preserve every new optional ProjectMemoryRecord field via the `...(value !== undefined ? { field: value } : {})` pattern — without this, deserialized records silently drop the field on read, breaking auto-propagation and any other read-time consumer. 165/165 tests across 16 test files all green after the trio. Roadmap status updated: B1/B5/B7/B10/B4/B2 all marked Shipped 2026-05-10. Pausing for user review before B3/B6/B9/B8.

- Brain-faithfulness roadmap: shipped the second trio B3 → B6 → B9 on branch feat/brain-faithfulness-roadmap (started from commit 85cf0f7). B3 (operator phrasebook + ritual-hook pattern matcher): new src/wiki/operator-phrasebook.ts with OPERATOR_PHRASE_RULES (19 entries across durable-intent / scope-setting / session-boundary / reviewer-control), matchOperatorPhrases word-boundary matcher, formatOperatorPhraseNudges output formatter; new docs/wiki/operator-phrasebook.md page with phrase tables; ritual:hook AND ritual:cursor-hook CLI subcommands now read stdin.prompt and append a [DENDRITE OPERATOR PHRASEBOOK] block to the additionalContext / agentMessage. B6 (synaptic-pruning auto-archive): new src/wiki/memory-auto-archive.ts mirroring auto-promote.ts shape — deterministic candidates, DENDRITE_AUTO_ARCHIVE=on gate, per-sweep cap=25, salience&gt;0 protection, kind!=skill/handoff exclusion. New `dendrite-wiki memory:auto-archive [--dry-run]` CLI and `memory_auto_archive` MCP tool added to GATED_TOOL_NAMES. B9 (sleep-cycle consolidate): new src/wiki/consolidate.ts that gathers memory_review findings + auto-promote candidates + auto-archive candidates, clusters them by shared relatedFiles/relatedPages/tags via union-find, produces a ConsolidateReport. New `dendrite-wiki consolidate [--apply] [--max-clusters N]` CLI; --apply gated by DENDRITE_AUTO_CONSOLIDATE=on plus the sub-sweep env vars. 171/171 tests across 18 affected test files all green. Remaining: B8 (page-trail bonus promotion) waits on 2+ weeks of post-2026-05-06 recall-benchmark history showing shadowBipartitePotentialRankChangeCount &gt; 0.

- Benchmark-telemetry-database track: opened branch feat/benchmark-telemetry-database off main (post-brain-faithfulness merge). Shipped T1 (Supabase→Turso doc drift fix in telemetry-status.md + privacy-telemetry-disclosure.md), T2 (baked-default fallback via src/wiki/telemetry-defaults.ts + resolveLibsqlUploadTarget env-then-baked resolution order), T2b (scripts/write-telemetry-defaults.ts publish-time injection with --inject/--reset/--check modes, wired into package.json prepublishOnly + postpublish), T5 (src/wiki/telemetry-report.ts module + dendrite-wiki telemetry:report CLI with --format text|json + --since Nd, reads separate REPORT_URL/REPORT_TOKEN env vars for read-scoped access), T6 (scaffold: docs/wiki/aggregate-learnings.md + docs/public/aggregate-learnings.json placeholder + VitePress sidebar wire). New roadmap page docs/wiki/benchmark-telemetry-database-roadmap.md is the canonical track home. 18/18 tests green across telemetry + telemetry-report + install. Operator decisions confirmed via AskUserQuestion: bake write-scoped token (Option A) + build-time injection + ship T5+T6 together. Remaining: T3 (operator action — provision dendrite-wiki-telemetry Turso database at turso.tech), T4 (CI write of baked defaults from secrets), T7 (README + opt-in page polish, gated on T3/T4), T8 (end-to-end smoke test against the live destination).

## 2026-05-12

- Benchmark-telemetry-database track CLOSED on branch feat/benchmark-telemetry-database. 4 commits, 8/8 slices shipped: roadmap doc + T1 (Supabase→Turso doc drift fix) + T2 (baked-default fallback) + T2b (prepublishOnly injection script) + T3 (Turso DB provisioned, schema verified, tokens captured) + T4 (publish workflow threads two new secrets) + T5 (telemetry:report CLI + telemetry-report.ts module) + T6 (aggregate-learnings page + JSON scaffold) + T7 (README + opt-in page polish, worded for pre/post-publish accuracy) + T8 (live smoke test verified end-to-end against the production Turso DB: opt-in → upload posted 1 row HTTP 200 → dashboard SELECT confirms the row → telemetry:report reads it back aggregating to 1 installation, 339 events, 142 wiki updates, weekly bucket 2026-W20). Two T3-uncovered bugs fixed during smoke test: URL regex was too narrow (single-segment hostnames only; Turso provisioned URLs include AWS region segment), and libsql:// URLs now get an explicit conversion error since the dashboard shows that scheme by default. Branch ready to merge to main. Next operator step is `npm publish` via GitHub Actions workflow_dispatch (dry_run first, then live) to ship 0.4.0-alpha.2 with the baked defaults so external opt-in users no longer need env vars. The 8-slice track was the second post-brain-faithfulness merge, intentionally focused on "can we actually measure whether this product helps people?" — answer: yes, the path works end-to-end.

- Design discussion (2026-05-11) on wiki-vs-memory drift: operator flagged docs/wiki/dendritemcp-lessons.md as stale; audit confirmed 5 of 7 "What Is Missing" items are actually shipped. Proposed three-slice fix: per-page memory-pending badge with inline bridge endpoint, ghost-paragraph inline injection preview, and new lint rule wiki/contradicts-shipped-memory matching negative-existence patterns against shipped memories. Awaiting operator decision on build order before implementation.

- Shipped three-slice wiki-drift mitigation (2026-05-12): (1) new src/wiki/page-inbox.ts builds per-page projection of pending memory promotions + lint findings reusing existing reviewProjectMemories + lintWikiPages pipelines and the canonical maintenance-inbox actionId scheme; (2) new src/wiki/contradicts-shipped-memory.ts lint rule scans negation patterns against affirming-keyword memories with ≥2 token overlap and a contradicts-shipped-memory: ignore frontmatter opt-out; (3) Vue layer: docs/.vitepress/theme/components/PageMemoryBadge.vue (floating pill bottom-left) + MemoryGhost.vue (inline ghost cards via doc-after slot) + composables/usePageInbox.ts (shared cache); new /__review-bridge/pages/inbox endpoint and bridge handler wiring. Rewrote dendritemcp-lessons.md to reflect shipped reality (M1 memory store, M8 auto-promote, B6 auto-archive, Memory Trails all called out as shipped). Added contradicts-shipped-memory: ignore to product-vision, privacy-telemetry-disclosure, brain-faithfulness-roadmap where negation prose is intentional design language. 528/528 tests green; 12 new tests across contradicts-shipped-memory + page-inbox. Manual lint run produced 0 findings after opt-outs; manual page-inbox query against architecture returned 1 real pending promotion. Bundle: 4 new src files, 3 new Vue components, 2 new test files, 1 page rewrite, 3 opt-out frontmatter additions.

- Fix (2026-05-12): PageMemoryBadge.vue and MemoryGhost.vue were silently crashing with "Unhandled error during execution of render function" because ref(usePageInbox(...)) auto-unwraps the nested Refs inside the composable result, making inbox.value.snapshot.value throw on the initial null state. Switched both components to shallowRef so nested refs are preserved as-is. Verified end-to-end via agent-browser: floating "✦ 1 pending update" badge renders bottom-left on /wiki/architecture.html with the urgent amber tone, ghost card renders inline at the doc-end via doc-after slot showing the proposed Promoted Lessons block, provenance chip ("lesson · recalled 2×"), and the Approve & insert button. Bridge endpoint /__review-bridge/pages/inbox?slug=architecture returns HTTP 200 with 1 memory item; dendritemcp-lessons returns total:0 (correct — the rewrite removed it as a target).

- Slice 4 (sidebar/nav awareness, 2026-05-12): added buildPageInboxSummary() to src/wiki/page-inbox.ts that aggregates per-slug counts (memoryCount + lintCount + hasUrgent) for every page with pending items, plus new GET /__review-bridge/pages/inbox-summary bridge endpoint. New docs/.vitepress/theme/components/SidebarInboxBadges.vue is a DOM-side-effect-only component that fetches the summary and walks .VPSidebar + .VPNav + .VPLocalNav anchors with /wiki/ in their href, appending a small amber `.dendrite-sidebar-badge` chip with the count next to each link whose slug has pending items. Urgent tone (deep amber) when any lint finding is contradicts-shipped-memory or page-drift. MutationObserver re-decorates on VitePress reactivity (sidebar expand/collapse, route change) with 80ms debounce. Verified live: top nav "Architecture 1" badge + sidebar "Architecture 1" / "Brain-Faithfulness 2" badges visible after HMR reload. All 529 tests pass (1 new for buildPageInboxSummary).

- Promoted project-local memory mem_5b6b0202-f879-4e87-be96-3995e3e9c476 into brain-faithfulness-roadmap.

- Slice 6 (wiki_librarian_audit MCP tool, 2026-05-12): new src/wiki/librarian.ts aggregates promotion-ready memories, contradicts-shipped-memory findings, page-drift findings, and basic lint findings into one structured audit payload with per-item evidence (page intent, recent activity entries, contradicting memory snippets, proposed promotion text) and a per-item `recommendedAction` natural-language sentence the agent can follow. New `wiki_librarian_audit` MCP tool registered in src/server.ts with optional maxPerCategory and categories filter. Worked example: ran the audit, used wiki_write to rewrite the first paragraph of brain-faithfulness-roadmap.md to reflect post-shipping state (improved Jaccard from 5% → 18%), then snoozed the residual structural drift via the existing snooze-page-drift action — demonstrating that the librarian's prose rewrite + snooze form the complete cleanup path for roadmap-style pages where log vocabulary will always exceed any reasonable page intent's vocabulary. 533/533 tests green (4 new librarian tests + 1 mcp-server tool-list expectation update). Bundle: 1 new src module, 1 MCP tool registration, 1 new test file, 1 demo rewrite, 1 snooze.

- Promoted project-local memory mem_96702b25-8621-4979-b018-c0284bc80787 into agent-enforcement-architecture.

- Promoted project-local memory mem_fa609b8a-9726-4b56-8559-c7e97fd8e8df into aggregate-learnings.

- Promoted project-local memory mem_88f849a7-75ca-4ece-9297-b046c70c4e9e into ai-memory-companion-roadmap.

- Promoted project-local memory mem_2cecfa1b-daa0-4904-9ceb-0bae6e6d990c into architecture.

- Promoted project-local memory mem_e65eb5c2-4263-4f04-812a-fc7ed9092480 into commercialization-plan.

- Promoted project-local memory mem_70a27ffd-20e9-4d00-81d0-44f237b0ae11 into agent-enforcement-architecture.

- Promoted project-local memory mem_093ae97c-6bd0-438d-81f2-67d0486405e0 into aggregate-learnings.

- Promoted project-local memory mem_4bf38658-ed8a-4181-b5fe-fb1f04f56710 into ai-memory-companion-roadmap.

- Librarian end-to-end test (2026-05-12): operator asked Claude to organize the wiki. Single `wiki_librarian_audit` call returned 12 items (8 promotion-ready, 4 page-drift). Applied all 8 promotions via memory_promote (mode=apply): 2 into agent-enforcement-architecture (Claude Code PreToolUse + UserPromptSubmit hook protocols), 2 into aggregate-learnings (benchmark-telemetry-database track 2026-05-11 + 2026-05-12 closure), 2 into ai-memory-companion-roadmap (B10 why-linter shipped + wiki drift case study), 1 into architecture (ritual enforcement three layers), 1 into commercialization-plan (shareable export artifacts pattern). For the 4 page-drift findings, rewrote each first paragraph to acknowledge shipped state — improvements from 6%→15% (aggregate-learnings), 10%→27% (api-reference-roadmap), 4%→21% (operator-phrasebook), 3%→10% (privacy-telemetry-disclosure). Residual structural drift (log vocabulary inherently includes commit hashes / slice IDs / file paths that no abstract page intent can match) snoozed for 30 days. Last remaining finding was a self-referential contradicts-shipped-memory hit on ai-memory-companion-roadmap where the just-promoted wiki-drift-case-study lesson contained the literal phrase 'Is Missing' as part of its description of what the detector does — added contradicts-shipped-memory: ignore to frontmatter. Final audit returned 0 items. End-to-end demo: operator command → single MCP tool → 9 page mutations + 4 snoozes + 1 frontmatter edit → clean wiki.

- wiki_context payload trim (2026-05-12): session-start was hitting the MCP 25k-token cap because buildWikiContext emitted 91k chars per call, dominated by omittedPageReasons (96 pages × full match-reason strings = 35k chars / 38% of payload) plus full handoff/memory/skill bodies. Added five caps to WikiContextOptions with defaults — maxOmittedPageReasons=12, maxOmittedReasonChars=80, maxHandoffTextChars=1200, maxMemoryTextChars=600, maxSkillTextChars=800 — and a `truncateForBriefing(text, max)` helper that appends an ellipsis so the agent can tell the body was clipped. The briefing is now actually a briefing; full bodies still available via memory_recall (handoffs and memories by id) and wiki_skill_load (skills). 533/533 tests still green. Measured against this dogfood repo: 91k → 38k chars (58% reduction), omitted-reasons 35k → 1k, handoffs 12.5k → 2.4k, memories 10k → 1.2k, skills 16k → 4k. Briefing prose also smaller because the omitted-page slice it references is now bounded.

- Library Extraction Roadmap shipped (2026-05-12, Phase 0): new docs/wiki/library-extraction-roadmap.md is the canonical plan for splitting the codebase into @rarusoft/dendrite-memory (brain), @rarusoft/dendrite-wiki (markdown-wiki adapter), and @dendrite/mcp-server (agent surface) under the @dendrite umbrella. Audited all 43 files in src/wiki/: 15 brain-pure (35%) extract cleanly, 5 brain-leaky (12%) need decoupling first, 18 wiki (42%) stay in the adapter, 5 shared (12%) live in brain and are re-exported. Identified three hard couplings (memory-promotion.ts → writeWikiPage/readWikiPage/appendProjectLog/pagePathFromSlug; auto-promote.ts and consolidate.ts inherit transitively; context-cache.ts caches WikiContextResult) and six vocabulary leaks (WikiClaimSourceKind enum, relatedPages field name, the `## Promoted Lessons` markdown format, librarian audit categories with wiki-specific kinds, hardcoded `docs/wiki/` path). Six migration phases laid out — Phase 0 (this audit, done), Phase 1 storage adapter (1 week), Phase 2 CanonicalTarget abstraction (3-4 days), Phase 3 vocabulary rename (2 days), Phase 4 monorepo split (1 week), Phase 5 publish & dogfood elsewhere (2-3 days). Cross-linked from product-vision.md and architecture.md so the strategic shift is discoverable from the canonical positioning + technical-overview surfaces. Page carries `contradicts-shipped-memory: ignore` frontmatter because it intentionally describes a future-state split using forward-looking negation language.

- Phase 1 slice 1 shipped on feat/library-extraction (2026-05-12): new src/wiki/memory-storage.ts introduces the `MemoryStorage` interface (`readMemoryStore` + `writeMemoryStore`) and `FilesystemMemoryStorage` default backend. src/wiki/memory-store.ts now delegates its read/write helpers to the adapter while keeping the public API byte-identical — every existing entry point keeps the `root: string = process.cwd()` parameter and async signature; `resolveProjectMemoryStorePath` is now a legacy alias that calls into the new single-source-of-truth `resolveMemoryStorePath` in memory-storage.ts. `ProjectMemoryStoreFile` was promoted from internal interface to a public export so the adapter can reference the schema without re-declaring it; type-only import in memory-storage.ts keeps the dependency graph acyclic at runtime. 6 new adapter tests (round-trip, missing file, whitespace file, mkdir-on-write, DENDRITE_WIKI_DATA_DIR override, direct-path construction). Total: 539/539 green. Next slice migrates memory-edges.ts to the adapter — its memory-trails JSONs live under local-data/memory-edges/ today and have their own internal read/write helpers.

- Phase 1 slice 2 shipped on feat/library-extraction (2026-05-12, commit a409baf): migrated src/wiki/memory-edges.ts to the MemoryStorage adapter. Interface grew readMemoryEdges/writeMemoryEdges; the atomic tmp+rename write strategy + per-path in-process write queue moved out of memory-edges.ts and into FilesystemMemoryStorage as an adapter implementation detail. FilesystemMemoryStorage constructor now takes a data directory rather than a store-file path so all sibling persistent files (memory store, edges, future raw-observations / ritual-state / page-drift-snoozes) share one configured location. New resolveMemoryDataDir(root) + resolveMemoryEdgesPath(root) exports mirror the existing resolveMemoryStorePath(root). ProjectMemoryEdgesFile promoted from internal interface to public export. memory-edges.ts imports nothing from `fs` directly anymore — its readEdgesFile/writeEdgesFile delegate to a per-call adapter, while normalizeStoredEdge (schema migration) and sort-by-id (diff-friendliness) stay at the brain layer because they're domain knowledge. 4 new tests added: edges round-trip, empty default, concurrent writes serialized through the queue without leaving tmp stragglers, DENDRITE_WIKI_DATA_DIR override for data dir + edges path. Slice 1's direct-constructor test updated to match the new data-dir constructor. 543/543 green. Phase 1 remaining: raw-observations.ts (append-only JSONL — design call on appendLine vs read-whole/write-whole), ritual-state.ts, page-drift-snoozes.ts.

- Phase 1 slice 3 shipped on feat/library-extraction (2026-05-12, commit b0eca34): migrated src/wiki/raw-observations.ts to the MemoryStorage adapter. Interface grew three methods this slice — appendObservationLine (O(1) via fs.appendFile, the right shape for an append-only JSONL stream; coarse-grained would have been O(N) at the 5000-line cap and dominated per-tool-call cost), readObservationLines (returns raw non-empty string lines; brain owns JSON parse), writeObservationLines (coarse rewrite used by lazy retention). POSIX O_APPEND atomicity for sub-PIPE_BUF payloads means appendObservationLine needs no tmp+rename — matches legacy semantics exactly. raw-observations.ts now imports nothing from `fs` or `path` directly; captureRawObservation, readRawObservations, enforceRawObservationsRetention all delegate to a per-call adapter while classify / clip / parse passes remain at the brain layer. resolveRawObservationsPath in raw-observations.ts is now a legacy alias to the new single-source-of-truth in memory-storage.ts. 3 new adapter tests (round-trip preserves order, empty default on missing/whitespace file, compose pattern modeling append-trim-append with no blank-gap regression). 546/546 green. Branch is 3 commits ahead of main. Phase 1 remaining: ritual-state.ts (uses SYNC fs — async-everywhere policy means slightly bigger change) and page-drift-snoozes.ts.

- Phase 1 slice 4 shipped on feat/library-extraction (2026-05-12, commit 384312a): migrated src/wiki/ritual-state.ts to the MemoryStorage adapter and flipped the brain to async-everywhere. ritual-state was the only sync-fs holdout in the brain; it now uses readRitualState/writeRitualState through the adapter. Functions flipped async: persistState, recordToolCall, resetRitualState, readPersistedRitualState. Cascade was tractable because every caller was already inside an async function — server.ts wrapToolResponse flipped from sync to Promise-returning (MCP tool handlers `return wrapToolResponse(...)` continued working unchanged), runGated's rejection-path recordToolCall became fire-and-forget via `void`, cli.ts ritual:cursor-hook + ritual:hook callers added await. External hook scripts under .claude/hooks/*.mjs that read via readFileSync are unchanged — they run in PreToolUse/Stop contexts that need sync semantics, which is the right boundary. 3 new ritual-state adapter tests (round-trip preserves every field including currentGoal; null on missing or invalid file; env-var override for path resolver) plus every existing ritual-state.test.ts case rewritten to await the now-async API. 549/549 tests green (546 prior + 3 new). Branch is now 4 commits ahead of main. Phase 1 remaining: page-drift-snoozes.ts (final slice), then add the no-direct-fs assertion test.

- Phase 1 of the Library Extraction Roadmap COMPLETE on feat/library-extraction (2026-05-12, commit fe8bdd1). Final slice migrated src/wiki/page-drift-snoozes.ts to the MemoryStorage adapter (readPageDriftSnoozes/writePageDriftSnoozes added; adapter swallows JSON parse errors because drift snoozes are always re-snooze-recoverable). PageDriftSnoozesFile promoted to public export. page-drift-snoozes.ts imports nothing from `fs` or `path` directly. New test file test/brain-no-direct-fs.test.ts pins the Phase 1 contract: the five migrated brain-state modules (memory-store, memory-edges, raw-observations, ritual-state, page-drift-snoozes) MUST NOT import from node:fs directly. A second contract test guards against accidentally deleting the adapter's own fs import (which would silently break the whole contract). Scope is intentionally narrow — the assertion lists exactly the five migrated modules rather than the whole brain, because other brain modules (embedding-provider, recall-benchmark, skill-matching, skill-portability, memory-auto-clean) still import fs for separate persistence concerns (embedding cache, benchmark history, skill markdown export) that aren't part of the MemoryStorage interface. Phase 2+ decides case-by-case whether to fold them in. 3 new snoozes adapter tests + 2 contract tests; 554/554 green (549 prior + 5 new). Branch is 5 commits ahead of main: 134819f memory-store, a409baf memory-edges, b0eca34 raw-observations, 384312a ritual-state, fe8bdd1 page-drift-snoozes + contract test. Next: Phase 2 (CanonicalTarget abstraction) starts with memory-promotion.ts, which currently calls writeWikiPage/readWikiPage/appendProjectLog directly.

- Phase 2 slice 1 shipped on feat/library-extraction (2026-05-12, commit ad97c4c): introduced CanonicalTarget abstraction so memory-promotion is backend-agnostic. New src/wiki/canonical-target.ts defines the 10-method interface (read/write/append-log storage triad + display + target-id resolution + format-specific operations) and ships WikiCanonicalTarget as the default. memory-promotion.ts now imports nothing from store.ts — draft/preview/apply functions go through createWikiCanonicalTarget() and call interface methods. Public API byte-identical; every existing consumer (memory_promote MCP tool, maintenance-actions apply-memory-promotion handler, auto-promote, consolidate, page-inbox, librarian, maintenance-inbox) keeps working with no source changes. resolvePromotionTargetSlug + DEFAULT_PROMOTION_TARGET_SLUG kept as legacy aliases pointing at the new module. The wiki formatting that used to live inline (## Promoted Lessons heading selection, _Provenance:_ bullet, VitePress &lt; → &lt; escape, compose rules) all moved into WikiCanonicalTarget. 18 new format-coupled tests in test/canonical-target.test.ts. 572/572 tests green (554 prior + 18 new). Branch is now 6 commits ahead of main: 5 Phase 1 slices + Phase 2 slice 1. The brain has zero direct wiki references on the canonical-state path (Phase 1) AND on the promotion-output path (Phase 2 slice 1).

- Phase 2 of the Library Extraction Roadmap COMPLETE on feat/library-extraction (2026-05-12, commit d02d5a8). Slice 2 wrapped the promotion-path decoupling: CanonicalTarget grew one more method (listAvailableTargetIds()) so auto-promote.ts and consolidate.ts could replace their listWikiPages() calls. Both modules now reach the wiki only through createWikiCanonicalTarget(); their store.ts imports are gone. New test/brain-no-wiki-coupling.test.ts pins the Phase 2 contract: memory-promotion.ts, auto-promote.ts, consolidate.ts MUST NOT import from `./store.js` directly. Paired with a sanity check that canonical-target.ts itself still imports from store.ts (guards against accidentally removing the wiki coupling that's the whole point of the adapter). 574/574 tests green (572 prior + 2 contract). Branch is 7 commits ahead of main: 5 Phase 1 slices + Phase 2 slice 1 (CanonicalTarget interface + memory-promotion refactor) + Phase 2 slice 2 (transitive cleanup + contract). The brain has zero direct wiki references on the canonical-state path (Phase 1) AND the promotion-output path (Phase 2), both locked by per-phase contract tests. Next: Phase 3 vocabulary rename (WikiClaimSourceKind → MemorySourceKind, relatedPages → relatedDocuments with legacy aliases).

- Phase 3 of the Library Extraction Roadmap COMPLETE on feat/library-extraction (2026-05-12, commit e6a3908). Scoped down to the only TYPE-level vocabulary leak the brain still carried: WikiClaimSourceKind moved to memory-store.ts as MemorySourceKind. WikiClaimSourceKind kept as a type alias in store.ts for one release so existing imports compile unchanged. ProjectMemorySource.kind now references MemorySourceKind locally. Deferred to Phase 4 (recorded in docs/wiki/library-extraction-roadmap.md): relatedPages → relatedDocuments rename (touches MCP schema + Vue + persisted JSON; better with per-package boundary), synthesis.ts split into brain provider core vs wiki synthesis (one brain consumer, genuinely a package boundary), chart-prompts.ts move (small follow-up alongside synthesis split). Skipped: librarian.ts audit split (Phase 0 audit classifies it as wiki-tier already). 574/574 tests green. Branch is 8 commits ahead of main: 5 Phase 1 slices + 2 Phase 2 slices + 1 Phase 3 slice. Next: Phase 4 monorepo split bundles the deferred Phase 3 items into the package-boundary refactor where deprecation aliases are clean.

- Phase 4 of the Library Extraction Roadmap reached its midpoint on feat/library-extraction (2026-05-12, 11 commits ahead of main). Three slices shipped this session: slice A (commit 9036b44) set up npm workspaces under packages/* with @rarusoft/dendrite-memory as a private workspace package; slice B wave 1 (commit 8a8008c) physically moved 9 brain-pure leaves into packages/memory/src/ (memory-storage, raw-observations, session-outcome, observation-compressor, embedding-provider, operator-phrasebook, page-drift-snoozes, ritual-state, skill-portability); slice B wave 2 (commit e45447c) moved the memory core itself — memory-store, memory-edges, memory-auto-archive, memory-auto-clean, skill-matching, recall-benchmark — after first inverting the two real wiki couplings the brain still carried. The tokenizer (tokenizeSearchQuery) was extracted into a new brain-owned packages/memory/src/tokenize.ts; src/wiki/search-index.ts now re-exports from there so the reverse dependency is gone. The wiki_context cache invalidation was inverted from a direct call inside memory-store.ts into a callback registry: brain exports onMemoryMutation(listener), wiki side context-cache.ts registers its invalidator at module load. After wave 2 the brain owns its full canonical state and reinforcement / promotion pipelines; only memory-promotion.ts, auto-promote.ts, and consolidate.ts remain in src/wiki/, blocked on the CanonicalTarget interface/implementation split scheduled for slice B wave 3. The 38-file consumer rewire ran via a one-shot scripts/migrate-memory-imports.ts that was deleted in the same commit. Two contract tests updated to track per-module post-move locations (test/brain-no-direct-fs.test.ts uses BRAIN_WIKI_DIR + BRAIN_MEMORY_DIR; brain-no-wiki-coupling.test.ts still pins the wiki-side trio). 574/574 tests green at every commit. Next: slice B wave 3 splits CanonicalTarget interface (to brain) from WikiCanonicalTarget impl (stays wiki) with a DI surface (setDefaultCanonicalTarget + getDefaultCanonicalTarget OR explicit target parameter — decision pending), then moves the last three brain modules into packages/memory/. After wave 3 the brain is fully extracted; slices D-F handle packages/wiki/ scaffold, deferred Phase 3 renames (relatedDocuments etc.), and per-package test/CI split. Roadmap page docs/wiki/library-extraction-roadmap.md updated with the Phase 4 progress section.

- Phase 4 slice B wave 3 of the Library Extraction Roadmap COMPLETE on feat/library-extraction (2026-05-12, commit d8c9d00). The brain is fully extracted. CanonicalTarget split: interface + module-level default-target registry (setDefaultCanonicalTarget / getDefaultCanonicalTarget / clearDefaultCanonicalTarget / hasDefaultCanonicalTarget) live in packages/memory/src/canonical-target.ts; WikiCanonicalTarget implementation + createWikiCanonicalTarget factory + DEFAULT_WIKI_PROMOTION_TARGET_SLUG constant stay in src/wiki/canonical-target.ts, which registers itself as the default via a top-level setDefaultCanonicalTarget(createWikiCanonicalTarget()) side effect at module load. The DI surface was chosen over explicit `target: CanonicalTarget` parameters on every brain function because keeping brain function signatures byte-identical with the pre-extraction API let the slice ship without rewriting every consumer's call shape. Three brain modules moved into packages/memory/src/: memory-promotion.ts, auto-promote.ts, consolidate.ts. Every createWikiCanonicalTarget() call inside them was replaced with getDefaultCanonicalTarget(). Auto-wire pattern: every wiki-tier module that calls into brain promotion (maintenance-actions, librarian, page-inbox, review-bridge, maintenance-inbox) does `import './canonical-target.js'` at the top for the registration side effect; src/server.ts, src/cli.ts, and scripts/refresh-wiki.ts also carry direct belt-and-suspenders side-effect imports. The contract test brain-no-wiki-coupling.test.ts rewritten for the post-wave-3 layout — three brain modules scanned at packages/memory/src/, forbidden-imports regex broadened to catch any wiki-store back-reference, and a third test added that forbids direct createWikiCanonicalTarget() calls from brain modules. After wave 3 packages/memory/src/ contains every module the Phase 0 audit classified as brain-pure or brain-leaky (20 modules + the new tokenize + the new canonical-target interface). src/wiki/ retains only wiki-tier code. 575/575 tests green (574 prior + 1 new contract test). Branch is 13 commits ahead of main. Next: slice D scaffolds packages/wiki/ and moves the remaining wiki-tier modules; slice E lands the deferred Phase 3 renames (relatedDocuments, synthesis split, chart-prompts); slice F splits tests into packages/*/test/ and updates CI.

- Phase 4 slice D of the Library Extraction Roadmap COMPLETE on feat/library-extraction (2026-05-12, 2 commits — slice D wave 1 scaffold 68884e5, slice D wave 2 wiki-tier moves a824ad4). The wiki tier is now fully extracted. packages/wiki/src/ holds every wiki-tier module from the Phase 0 audit: store, search-index, context-cache, canonical-target impl, maintenance-actions/maintenance-inbox/maintenance-runner, page-drift, contradicts-shipped-memory, page-inbox, librarian, review-bridge, api-reference + api-extractor/ subdir (TypeScript / tree-sitter / Python language extractors plus walk + render + types), chart-insert, chart-prompts, synthesis, telemetry + telemetry-defaults + telemetry-report, benchmark + benchmark-events, report-export, binder-export, doctor, diff-context, generated-docs, i18n. src/wiki/ is gone; src/ retains only the umbrella surface (cli, server, index, install). 28 .ts files plus the api-extractor/ subdir relocated. The barrel packages/wiki/src/index.ts puts canonical-target first so its top-level setDefaultCanonicalTarget side effect fires before anything else when @rarusoft/dendrite-wiki is imported — this lets the previous belt-and-suspenders side-effect imports in src/server.ts and src/cli.ts go away. Consumer rewires ran via a one-shot scripts/migrate-wiki-imports.ts deleted in the same commit: all ./wiki/X.js and ../src/wiki/X.js imports across src/, test/, scripts/, and docs/.vitepress/ switched to @rarusoft/dendrite-wiki. Hand-corrected: deep paths into api-extractor/ sub-files (6 tests), dynamic pathToFileURL(path.join(repoRoot, 'src', 'wiki', '&lt;module&gt;.ts')) URLs (10 tests + 1 script — all updated to point at packages/wiki/src/), tests that exercise brain promotion without a wiki-tier import (4 tests added import '@rarusoft/dendrite-wiki' for the side-effect registration). New contract test test/wiki-no-brain-internals.test.ts pins the package boundary the way brain-no-direct-fs and brain-no-wiki-coupling pin the brain side: no .ts under packages/wiki/src/ may reach into packages/memory/src/ internals via a relative deep path. The 4-package layout is realized: @rarusoft/dendrite-memory + @rarusoft/dendrite-wiki + root src/ umbrella. 576/576 tests green at the wave 2 commit (575 prior + 1 new contract test). Branch is now 16 commits ahead of main. Next: slice E for the deferred Phase 3 renames (relatedPages → relatedDocuments with normalize-on-read backward compat, synthesis.ts split into synthesis-provider brain + wiki-synthesis wiki); chart-prompts move was absorbed into slice D since it now naturally lives wiki-side. Then slice F splits tests into packages/*/test/ and updates CI workflow.

## 2026-05-13

- Phase 4 slice E scoped-down on feat/library-extraction (2026-05-12, commit 9427a8e). The two deferred Phase 3 items were re-scoped after the package boundary itself resolved most of the brain-vs-wiki distinction the slice was originally meant to address. Shipped: packages/wiki/src/synthesis.ts renamed to wiki-synthesis.ts so the filename matches the wiki-specific nature of every symbol in the module (every export is named Wiki* — WikiSynthesisProviderKind, WikiSynthesisProviderInfo, synthesizeWikiClaims, synthesizeWikiDriftResolution, etc.). Three consumer updates: packages/wiki/src/index.ts barrel re-export, packages/wiki/src/review-bridge.ts internal import, 4 dynamic pathToFileURL strings in test/drift-resolution.test.ts. Deferred with rationale: (1) the symbol-level brain-vs-wiki split (move resolveWikiSynthesisProvider, listOllamaModels, WikiSynthesisProviderInfo into a brain-side synthesis-provider.ts) is deferred because every caller of the provider surface is wiki-tier today (review-bridge, drift-resolution, chart, claim/guidance/proposal synthesis) — the split makes sense only when a non-wiki canonical target wants the same LLM provider plumbing, and there is no such consumer; (2) relatedPages → relatedDocuments rename is deferred entirely because it is a real schema change touching MCP tool schemas, Vue components, and persisted local-data/project-memories.json files in operator projects in the wild — the lossless approach (normalize-on-read forever, stop emitting relatedPages on new writes after one release) is right but warrants a dedicated slice with explicit migration testing; (3) chart-prompts.ts move was already absorbed into slice D wave 2. Branch is now 18 commits ahead of main. 576/576 tests green. The 4-package layout (@rarusoft/dendrite-memory + @rarusoft/dendrite-wiki + root src/ umbrella) is fully realized; only slice F remains for Phase 4 — split test/ into packages/*/test/ per the tier classification and update CI workflow to run npm test -w @rarusoft/dendrite-memory and npm test -w @rarusoft/dendrite-wiki in isolation. After slice F, Phase 5 begins: publish @rarusoft/dendrite-memory to npm and dogfood it in a second project.

- Phase 4 of the Library Extraction Roadmap COMPLETE on feat/library-extraction (2026-05-12 through 2026-05-13, 20 commits ahead of main, 576/576 tests green). Slice F shipped per-workspace standalone build scripts on commit bcc445e: packages/memory/package.json and packages/wiki/package.json each got `\"build\": \"tsc -p tsconfig.json\"` so `npm run build -w @rarusoft/dendrite-memory` compiles the brain standalone (proves zero back-references into src/wiki/ at the tsc level — already enforced by the brain-no-wiki-coupling contract, now verified at the build-system level too). `packages/wiki/package.json` declares `\"dependencies\": { \"@rarusoft/dendrite-memory\": \"*\" }` so the brain → wiki workspace relationship is discoverable by npm ls / npm publish / the eventual Phase 5 npm release. Both workspaces exit 0 on `npm run build -w ...`. Per-workspace dist/ outputs land independently. Slice F's test reorganization piece (split test/ into packages/memory/test/ + packages/wiki/test/ + root cross-cutting integration test/) was explicitly deferred with rationale: the contract tests already enforce the package boundary at npm test time (moving files is organizational not functional); many tests use process.cwd() or relative paths to test/fixtures/ so a physical move requires careful re-rooting of fixture references and would risk flaky tests during the reshuffle; the Phase 5 npm publish path doesn't depend on test layout (npm publish reads \"files\" from each workspace package.json and never ships test/). The reorganization is real work that deserves a dedicated session. Phase 4 status across the eight slices: slice A scaffold (9036b44), slice B wave 1 leaves (8a8008c), slice B wave 2 memory core (e45447c), slice B wave 3 CanonicalTarget split + last brain modules (d8c9d00), slice D wave 1 wiki scaffold (68884e5), slice D wave 2 wiki-tier moves (a824ad4), slice E synthesis.ts rename + deferrals (9427a8e), slice F per-workspace standalone builds (bcc445e). Plus 4 roadmap progress doc commits. The 4-package layout is realized: packages/memory/src/ holds the full brain (20 modules), packages/wiki/src/ holds the full wiki adapter (28 modules + api-extractor/), src/ retains only the umbrella surface. Three contract tests pin the boundary: brain-no-direct-fs, brain-no-wiki-coupling, wiki-no-brain-internals. Phase 5 begins next: claim @dendrite scope on npm, publish @rarusoft/dendrite-memory at 0.1.0, dogfood in a second active project.

## 2026-05-14

- Supervision-panel slice 2b complete (commit 6f10190 on feat/library-extraction, 33 commits ahead of main). Review-bridge GET /cortex endpoint shipped: the embedded VitePress bridge now serves the full CortexSnapshot payload buildCortexSnapshot() produces — current goal, every active memory as a typed node, file/page anchor nodes, memory-to-file/memory-to-page structural edges, the pending supervision-proposals queue, and the recent supervision-changes audit tail. Optional query params: ?recentChangesLimit=N caps the audit-tail size, ?includeArchived=true|1 broadens past active-only memories. Pattern follows the three-place endpoint extension skill (route handler + ReviewBridgeHandlerOptions/Handler types + health-response payload field + plugin path constant + plugin createReviewBridgeHandler arg). Pre-existing recall surfaced two important skill memories that named the exact missing-step pattern that broke the snapshot test (cortexPath needed to be added to the GET /health response payload too, not just the handler's returned shape) — saved real time. test/review-bridge.test.ts deep-equal health-response snapshot updated to include cortexPath: '/cortex'. Slice 2c (NEXT) is the Vue cortex view rendering this payload as a force-directed graph — d3-force or vis-network is the open design call. Note: full npm test run shows the recurring api-cli/mcp-server tree-sitter MCP-timeout flake on slow-CPU runs (60s default request timeout cold-loading 15+ language grammars); review-bridge tests pass 9/9 in isolation.

- Supervision-panel slice 2c.5 complete on feat/library-extraction (commits e20f817, 38 commits ahead of main). Cortex view time-window filter shipped per option #3 from the design discussion: scrubber is a window FILTER not a historical reconstructor. Selecting "Last 24h" filters the recent-activity panel + paints a persistent amber outer ring on every node touched inside that window. Polling auto-pauses when a non-Live window is selected so the browse window stays stable. Cumulative visual encoding now stacks three independent channels: static stroke for current supervision state (slice 2c.2), brief indigo pulse glow for just-mutated nodes (slice 2c.4), persistent amber outer ring for touched-in-window (slice 2c.5). New composable surface: timeWindowMs ref, CORTEX_TIME_WINDOW_OPTIONS constant, setTimeWindow() entry point, changesInWindow + touchedInWindow computeds. Why option #3 over options 1/2: avoids the schema-change headache of keeping mutation history per memory (option 2) and avoids the inverse-mapper-per-tool complexity (option 1). The graph stays current; the window-highlight is enough to deliver the "what changed this week" UX moment the operator originally asked for. Slice 2c.6 (final cortex polish) is next: per-kind filter chips so the cortex can be narrowed to one slice of the cognitive surface, and a lobe-clustering toggle that groups nodes by primary tag or relatedPages so domains resolve into visually-separated regions.

- Cortex view stability fix shipped on feat/library-extraction (commit add71b1, 40 commits ahead of main). Two operator-reported bugs from real use in slice 2c.4/2c.5 fixed in one commit. (1) Live-mode flicker — every 5s poll was calling startSimulation which threw away node positions and fully re-laid out the force graph. Fixed with a topology fingerprint short-circuit in rebuildFromSnapshot: hash sorted node-id set + edge keys + clustering mode; when an incoming snapshot has the same fingerprint as the prior one (typical case: memory.recallCount bumped, no new nodes), Object.assign the fresh attributes onto existing PositionedNode instances in place, bump tickCounter, return without touching the simulation. d3 keeps running with its current positions and alpha schedule. Topology-changed path still preserves prior x/y/vx/vy by id so only genuinely new nodes seed from scratch. (2) Time-window filter showed "0 changes across 0 nodes" — touchedInWindow only sourced from snapshot.recentChanges (supervision-changes.jsonl audit), and the operator's project has never used any of the eight supervision-panel tools so the audit was empty. Broadened the touched set to two sources: source 1 = supervision-changes inside window (existing path, ids extracted via extractPulseTargets); source 2 = any memory node whose updatedAt or lastRecalledAt falls in the window. Now the amber outer ring lights up for normal memory_remember/memory_recall activity too. Status banner copy reworked: "Last 24h: M memories touched · N supervision changes" instead of "N changes across M nodes" — supervision-changes count only renders when &gt; 0 so a fresh project doesn't see the awkward "· 0" tail. Both fixes ship together because they share a root cause: the cortex view was treating the supervision audit log as the canonical activity feed when it's really just one of two activity feeds (the other being normal memory mutation timestamps).

- Cortex view live updates switched from 5s polling to SSE push, shipped on feat/library-extraction (commit 10301f0, branch 41 commits ahead of main). Pattern mirrors the existing inbox push channel (/__review-bridge/events): new /__review-bridge/cortex/events endpoint streams `cortex-stale` events whenever a brain-state JSON/JSONL file under local-data/ changes. Cross-process safe because the file-watch fires regardless of which Node process wrote (the MCP server writes from a different process than the dev-server bridge; in-process callback registries like brain's onMemoryMutation can't see across that boundary, but the filesystem-watch does). Watched files: project-memories.json, supervision-changes.jsonl, supervision-proposals.json, ritual-state.json, project-memory-edges.json — covers every cortex-affecting mutation. Unrelated artifacts (recall probes, telemetry) don't trigger spurious broadcasts. Client side: useCortex composable opens an EventSource on startPolling, listens for `cortex-stale`, calls fetchCortex(50) on each. Polling interval bumped from 5s to 60s and demoted to the safety-net fallback path — runs alongside SSE for resilience against proxies that strip SSE. When the brain is idle: zero events, zero stutter. When the brain mutates: single event arrives within ~200ms of the file write, snapshot refetches once, topology-fingerprint short-circuit (add71b1) applies the attribute delta in place, the SVG flips the affected node's encoding without re-laying out. The visual now moves ONLY when the brain actually moves — the trust-surface property the cortex was supposed to deliver from the start.

- Cortex view pan + zoom shipped on feat/library-extraction (commit 712746f, branch 42 commits ahead of main). Not by design — the SVG was static in slice 2c with a fixed 960×640 viewBox and no way to navigate around it. Added d3-zoom (new dep d3-zoom ^3.0.0 + @types/d3-zoom ^3.0.8) attached to the SVG; viewport &lt;g&gt; wraps both the cortex-edges and cortex-nodes groups, transform attribute driven by the zoom behavior's reactive viewportTransform ref. Wheel-to-zoom centered on cursor (scaleExtent [0.25, 4]); click-and-drag empty canvas to pan; clicks on circles still open the per-node drawer because the zoom filter checks event.target.tagName and only starts a pan-gesture when the origin is svg or g. New &lt;rect class="cortex-pan-surface"&gt; at the top of the SVG (transparent fill, sized to viewBox) catches drag-pan gestures that start on truly empty pixels — some browsers don't fire the SVG element itself as event target there. New "Reset view" button in toolbar next to Refresh, 400ms d3-transition back to zoomIdentity. CSS adds cursor: grab + :active grabbing on the SVG so the affordance is visually clear without a tooltip. The pulse, ring, and topology-fingerprint stable-poll machinery all keep working because the encoding lives inside the viewport group and rides the zoom transform automatically.

- Cortex view drag-pan fix shipped (commit 1a57cd2 on feat/library-extraction, branch 43 commits ahead of main). Wheel zoom shipped clean in 712746f but click-and-drag pan was inert. Root cause: my own .filter() whitelist on the d3-zoom behavior allowed only event.target.tagName === 'svg' or 'g'. The same commit added a &lt;rect class="cortex-pan-surface"&gt; sized to the viewBox to catch drags that start on truly empty pixels — but its tag is 'rect', and my whitelist rejected exactly that surface. Self-inflicted. Fixed by inverting the policy: instead of whitelisting valid pan-origin tags, blacklist only `target.closest('.cortex-node-group')` — clicks that originated inside a clickable node continue to bubble up so the per-node drawer opens, but every other origin (background rect, viewport g, svg root, edge lines) now starts a drag-pan. The pattern lesson: when filter logic exists to distinguish "interactive vs background" elements in SVG, write the filter as "is this interactive?" not "is this background?" — the interactive surface is enumerable and small (named with a class), the background surface is everything else and adding more background elements (like the pan-surface rect) silently breaks an inclusion-list filter.
- Fixed the local Dendrite Wiki MCP verification path after `npm run check` exposed VitePress dead links. `docs/wiki/architecture.md` now points at the generated `api/server` page instead of the removed `api/wiki/i18n` page, and `docs/wiki/benchmark-telemetry-database-roadmap.md` now treats the repo-root README mention as code text instead of an invalid docs-root relative link. Verified the MCP stdio server directly with a temporary client: it exposed 45 tools and `wiki_context` returned a valid briefing. Full `npm run check` passes with 621/621 tests and a clean docs build.

- Phase 5 publish prep advanced: Cortex was already present in the VitePress sidebar, so the session focused on extracted package readiness. `@rarusoft/dendrite-memory` and `@rarusoft/dendrite-wiki` are now public-publishable (`private: false`), their READMEs describe publish-prep alpha status, and `.github/workflows/publish-package.yml` now has a package selector for root/memory/wiki publishes with workspace build verification. Local dry-runs succeeded for both workspace packages after adding `src/` to the package `files` arrays so the declared `types` / `source` export paths exist in the tarballs (`@rarusoft/dendrite-memory`: 52 files / 166.7 kB; `@rarusoft/dendrite-wiki`: 74 files / 313.9 kB). Release Process and Library Extraction Roadmap were updated to match. Full `npm run check` passes with 621/621 tests and a clean docs build.

- API Reference package-split fix: `wiki:refresh` had deleted generated `api/wiki/*` pages because the TypeScript API walker still defaulted to root `src/**` after Phase 4 moved wiki/brain modules into `packages/wiki/src` and `packages/memory/src`. The walker now scans root `src` plus `packages/*/src`, package slugs strip `packages/&lt;workspace&gt;/src` so wiki pages remain stable under `api/wiki/*`, and memory package APIs now appear under `api/memory/*`. Regenerated manifest/sidebar/search artifacts to 110 wiki pages, fixed one generated telemetry-defaults link, added workspace scan/slug tests plus MCP timeout headroom, and verified full `npm run check` passes with 622/622 tests and a clean VitePress build.

- Verified local Dendrite Wiki MCP server health from the repo root: TypeScript build passed, a direct stdio MCP client listed 45 tools, and wiki_context returned a valid briefing with ranked pages, handoffs, and skills. The live Codex session still did not expose mcp__dendrite-wiki-mcp__* tools, so the remaining issue is Codex/VS Code session mounting or refresh rather than server startup.

- Adjusted Codex MCP startup to use the compiled server entrypoint (`node dist/src/index.js`) instead of `npm run dev`, both in the workspace Codex config and the user-level Codex fallback. The exact built command lists 45 MCP tools and `wiki_context` succeeds, reducing VS Code/Codex startup risk from npm/tsx wrapper behavior.

- Added a Codex plugin wrapper for Dendrite Wiki MCP and patched initialize negotiation to echo DRAFT-2026-v1 when requested. Fresh codex exec now mounts the dendrite-wiki-mcp MCP server and attempts wiki_context; non-interactive exec cancels the MCP call because it lacks an approval path, so VS Code should prompt/allow the call after restart.

- Hardened npm install path for Codex users: pinned @modelcontextprotocol/sdk to a semver range instead of latest, changed generated Codex feature flag from deprecated codex_hooks to hooks, taught init to write a local Codex plugin wrapper and marketplace entry, documented the VS Code restart/approval flow, and added installer tests covering the plugin MCP config.

- Publishing/dogfood follow-up: fixed npm-script benchmark labels so repo-local snapshots use positional labels while the direct CLI keeps --label, refreshed README/MCP installation docs for the current 45-tool surface, and expanded unreleased notes for package-split + Codex plugin publish prep. Verified direct MCP stdio wiki_context, npm run build, npm test (622/622), and npm run docs:build.

- Session-start churn fix: changed the product contract so agent orientation is read-only by default. Removed the default benchmark hook, updated live and installer-seeded guidance to forbid benchmark snapshots, wiki refreshes, and API generation as session-start rituals, made docs:build/check non-mutating unless the operator runs check:generated, and added installer coverage proving the benchmark hook is no longer written.
